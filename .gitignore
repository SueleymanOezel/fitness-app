# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Claude Code worktrees live here; vitest would otherwise glob their test copies too
.claude/

# Scratch space for subagent-driven development (ledger, briefs, review packages)
.superpowers/

# Firebase CLI's local deploy cache (file-hash cache, not project config)
.firebase/

# impeccable's local live-server runtime state (pid/port/token) — not the critique snapshots,
# those live in .impeccable/critique/ and are meant to be committed
.impeccable/live/
