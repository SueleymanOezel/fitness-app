# Projekt: VitaLoop (Fitness- und Ernährungs-App, PWA)

## Kurzbeschreibung

**VitaLoop** (App-Name seit 05.09.2026) ist eine Progressive Web App, die Training, Ernährung und Körper-Tracking kombiniert. Installierbar über Browser-Link auf iOS, Android und Windows, kein App-Store nötig. Vollständige Details siehe docs/architektur.md im selben Verzeichnis.

## Tech-Stack

- Frontend: React + Vite, TypeScript
- Backend/Datenbank: Supabase (Postgres), inklusive Auth und Row-Level-Security
- Barcode-Daten: Open Food Facts API
- Übungsdatenbank: free-exercise-db (einmaliger Import in eigene Tabelle)
- Foto-Analyse (spätere Phase): Gemini API
- Hosting: Firebase Hosting, Projekt `fitness-app-3e8f5` (Projekt-ID kann nicht mehr umbenannt werden, spielt aber sonst keine Rolle), Hosting-Site `vitaloop` — **live unter `https://vitaloop.web.app`**, seit 05.09.2026. Deploy-Target `vitaloop` in `.firebaserc` auf diese Site gemappt; `firebase.json` trägt `"target": "vitaloop"`. Der ursprüngliche Default-Site-Link `fitness-app-3e8f5.web.app` existiert noch (letzter Deploy davor), wird aber nicht mehr aktiv bespielt.
  **Deploy ist nicht automatisiert** — `ci.yml` baut und testet nur, enthält keinen Firebase-Schritt. **Nach jedem Merge nach `master` deployen**: im Haupt-Checkout (nicht im Worktree — der hat kein `.env`) `npm ci && npm run build && firebase deploy --only hosting:vitaloop`. Ohne echte `.env` fehlt im Build das komplette Barcode-Scanner-Subsystem. Diese Regel gilt ab sofort für jeden gemergten PR, nicht nur für Design-Pläne — nach dem PR-Merge kurz sanity-checken (`curl -s https://vitaloop.web.app/ | grep -o 'index-[A-Za-z0-9_-]*\.js'` gegen den frisch gebauten Hash aus `dist/assets/`). **Seit 10.09.2026 kann Claude `firebase deploy --only hosting:vitaloop` selbst ausführen** — Permission-Regel in `G:\Code Projekte\fitness-app\.claude\settings.local.json` (`permissions.allow`) freigeschaltet, nachdem der Auto-Mode-Classifier den Befehl trotz Nutzer-Zustimmung im Chat blockiert hatte. Davor musste der Nutzer den Deploy-Befehl selbst ausführen.

## Namenskonventionen

- Alles in Englisch
- Dateinamen: kebab-case (z. B. workout-session.ts)
- React-Komponenten: PascalCase (z. B. WorkoutSession.tsx)
- Standard-React-Projektstruktur verwenden

## Design-Workflow

Ab 06.09.2026: Jede design-bezogene Aufgabe für diese App (neue UI, Redesign, visuelle Politur, Interaktion/Animation, 3D-Elemente) läuft über fünf installierte Claude-Code-Skills, nicht freihändig mit selbst erfundenen Tokens/Layouts. Grund: Das Referenzvideo aus Phase 6 war eine gute Vorlage, das umgesetzte Ergebnis wirkt dem Nutzer aber zu langweilig und zu wenig interaktiv — Phase 6 wird mit diesen Skills überarbeitet, sobald das entschieden ist (siehe Status weiter unten).

- **impeccable** (`~/.claude/skills/impeccable`) — Haupt-Skill für Redesign, Kritik, Audit, Politur: Modi wie `redesign`, `critique`, `audit`, `bolder`, `delight`, `distill`, `harden`, `clarify`, `animate`, `colorize`, `adapt`. Erster Anlaufpunkt für jede Design-Aufgabe.
- **taste-skill** (installiert als Plugin `taste-skill@taste-skill`) — gegen generisches/langweiliges KI-Design, liefert konkrete Stilrichtungen (brutalist, minimalist, soft, ...) statt eines gemittelten Standardlooks.
- **awesome-design-md** (Referenz-Checkout unter `~/.claude/skill-sources/awesome-design-md/design-md/<marke>/DESIGN.md`) — echte Marken-Designsysteme als Ausgangspunkt/Inspiration statt komplett selbst erfundener Tokens.
- **img2threejs** (`~/.claude/skills/img2threejs`) — für interaktive, prozedurale 3D-Elemente, falls gewünscht.
- **playwright-cli** (`~/.claude/skills/playwright-cli`, CLI `@playwright/cli` global installiert) — Browser-Automatisierung zur visuellen Iteration/Verifikation während der Design-Arbeit.

## Wettbewerber-Referenzen

Keine Namen von Drittanbieter-Apps/-Produkten (z. B. konkrete Konkurrenz-Apps aus Fitness/Ernährung) in Dokumentation, Code, Commit-Messages oder dem GitHub-Wiki erwähnen — bleibt intern zwischen Nutzer und Claude. In Docs stattdessen neutral umschreiben (z. B. "etablierte Ernährungs-Tracking-Apps").

## Secrets-Handling

- Alle API-Keys (Supabase, Gemini) ausschließlich in .env
- .env ist in .gitignore, wird niemals committet
- .env.example mit Platzhaltern im Repository pflegen

## App-Struktur

Vier Hauptbereiche, jeder mit eigenem Dashboard:
1. Home-Dashboard – Übersicht, Kalender, Kalorienstand
2. Trainings-Dashboard – Trainingspläne, Live-Session mit Pausen-Timer
3. Ernährungs-Dashboard – Barcode-Scan, manuelle Eingabe, später Foto-Analyse
4. Körper-Dashboard – Gewicht, Umfänge, Fortschrittsfotos

Details zu jedem Bereich, den Datenbanktabellen und der REST-API-Struktur stehen in docs/architektur.md.

## Arbeitsweise / Phasen

Das Projekt wird in klar getrennten Phasen entwickelt, nicht alles auf einmal:

1. Grundgerüst & Security-Basis (Setup, Auth, DB-Schema, CI/CD mit Semgrep und OWASP ZAP)
2. Ernährungsbereich (Barcode-Scan, manuelle Eingabe, Ernährungs-Dashboard)
3. Trainingsbereich (Trainingspläne, Live-Modus, Kalorienberechnung über MET-Werte)
4. Körperbereich & Health-Integration (Körper-Dashboard, Apple-Shortcuts-Sync)
5. Analysebereich (interaktive Graphen über alle Bereiche, konfigurierbare Dashboards) — nachträglich eingeschoben, siehe eigenen Abschnitt
6. Design (komplettes visuelles Design der App) — nachträglich eingeschoben, vor die Härtung, siehe eigenen Abschnitt
7. Härtung & Feinschliff (vollständiger OWASP-Durchlauf, Penetration-Tests, PWA-Feinschliff)

**Vor Phase 6:** Hosting einrichten (Firebase Hosting oder Supabase Hosting), damit die App eine dauerhafte URL bekommt statt nur über `npm run dev` lokal erreichbar zu sein. Kein eigener Phasen-Schritt, aber Voraussetzung dafür, dass Design und Härtung gegen eine echte deployte Instanz statt nur lokal geprüft werden können.

Wichtig: Nach jeder Phase soll eine Zusammenfassung gegeben und Tests durchgeführt werden (statisch und dynamisch), bevor die nächste Phase begonnen wird. Nicht mehrere Phasen gleichzeitig anfangen.

## Sicherheitsanforderungen

- OWASP Top 10 (2025) durchgängig beachten, siehe Checkliste in docs/architektur.md
- Row-Level-Security auf allen Supabase-Tabellen
- Keine Secrets im Code, keine Secrets in Logs
- Automatisierte Tests: Semgrep (SAST) und OWASP ZAP (DAST) in CI/CD-Pipeline

## Referenzdokument

Das vollständige Architekturkonzept mit Datenbankschema, REST-API-Endpunkten pro Bereich, UI/UX-Konzept und OWASP-Checkliste liegt in docs/architektur.md. Bei Unklarheiten dort nachschlagen, bevor Annahmen getroffen werden.

## Codex/GPT als Zweitmeinung und Kontingent-Entlastung

Seit 06.09.2026: Codex CLI (`npm install -g @openai/codex`) ist lokal installiert und über den ChatGPT-Account des Nutzers angemeldet — läuft auf dessen eigenem OpenAI-Kontingent, komplett unabhängig von Claudes Nutzungslimit. `codex doctor` zeigt Auth-/Verbindungsstatus; aktuell bestes verfügbares Modell ist `gpt-5.6-sol` (Slug bestätigt am 06.09.2026 — ein neues Modell kann eine neuere CLI-Version voraussetzen, die Fehlermeldung nennt das dann explizit, `npm install -g @openai/codex` aktualisiert).

**Zweitmeinung bei Reviews:** Nach jedem Whole-Branch-Review (und optional bei größeren Task-Reviews) zusätzlich `codex review -c model="gpt-5.6-sol" --base <base-branch>` laufen lassen, als unabhängige zweite Meinung neben dem eigenen Review — ersetzt den bestehenden Review-Prozess aus `subagent-driven-development` nicht (Task-Reviewer, Whole-Branch-Review, Scoped Re-Review bleiben wie gehabt), ergänzt ihn nur. Ergebnis kurz im Status-Abschnitt des jeweiligen Plans festhalten (siehe „Plan 2b" als Beispiel).

**Bei Claude-Nutzungslimit:** Codex/GPT für die Code-*Generierung* einsetzen, das Ergebnis danach selbst reviewen (Review kostet deutlich weniger als Generierung) — spart Claude-Kontingent, ohne die Qualitätskontrolle aufzugeben. Sinnvoll vor allem für mechanische, klar spezifizierte Tasks (vergleichbar mit den „cheap model"-Tasks in `subagent-driven-development`), weniger geeignet für Architektur-/Design-Entscheidungen mit viel Projektkontext.

Befehle:
- `codex exec --sandbox read-only -m gpt-5.6-sol "<Prompt>"` — nur lesen/antworten, keine Dateiänderungen.
- `codex exec --sandbox workspace-write -m gpt-5.6-sol "<Auftrag>"` — darf Dateien im Arbeitsverzeichnis ändern.
- `codex review -c model="gpt-5.6-sol" --base <branch> --title "<Titel>"` — eigenständiger Review-Lauf gegen einen Base-Branch.

## Status / Fortschritt (laufend aktuell halten)

Diese Sektion nach jedem abgeschlossenen Schritt aktualisieren, damit ein neuer Chat sofort weiß, was gemacht wurde und was als Nächstes ansteht.

**Aktueller Stand:** Phase 1, 2 und 3 sind gemerged, deployed und manuell gegen Produktion verifiziert; die kosmetischen Nacharbeiten am Layout ebenfalls (PR #22/#23, Merge-Commit `5b79651`). **Phase 4 (Körperbereich) ist gemerged und gegen Produktion verifiziert** (PR #26, Merge-Commit `63aced2`) — Details im eigenen Abschnitt weiter unten. **Phase 5 (Analysebereich) ist ebenfalls vollständig gemerged und verifiziert** — Details im eigenen Abschnitt weiter unten. Dazu gibt es eine Profilseite unter `/profile`, erreichbar über das Icon im Header. Der Ernährungsbereich hat eine eigene Eintragsliste unter `/nutrition/entries`, nach Mahlzeiten-Abschnitten gegliedert (PR #20, Merge-Commit `752587c`; Phase 3: PR #21, `7420145`).

**Genau hier weitermachen (Stand 07.09.2026):** Hosting ist erledigt, App heißt jetzt **VitaLoop** (siehe „Hosting" im Tech-Stack oben) — dauerhaft erreichbar unter `https://vitaloop.web.app`. **Phase 6 (Design) ist vollständig abgeschlossen — alle fünf Pläne (Fundament, Training, Ernährung, Körper, Analyse-Seiten) umgesetzt, reviewt, gemerged, live deployed und im Wiki dokumentiert.** Details zu Plan 2d im eigenen Abschnitt weiter unten. **Phase-6-Nachschärfung Plan 1 (Design-Tokens) ist gemerged, deployed und gegen Produktion verifiziert** (PR #45, Merge-Commit `cb6edcb`) — Details im Absatz „Phase-6-Nachschärfung Plan 1 (Design-Tokens)" weiter unten. **Plan 2 (Profil nachziehen) ist gemerged und deployed** (PR #46, Merge-Commit `04fbc0a`) — Details im Absatz „Phase-6-Nachschärfung Plan 2 (Profil nachziehen)" weiter unten. **Plan 3 (Wall-of-Options) ist gemerged, deployed und live verifiziert** (PR #47, Merge-Commit `830d408`) — Details im Absatz „Phase-6-Nachschärfung Plan 3 (Wall-of-Options)" weiter unten. **Damit sind inhaltlich alle drei Nachschärfungs-Pläne fertig.** Der `/impeccable critique`-Re-Run gegen die Live-App ist ebenfalls gelaufen — Details und **die als Nächstes zu klärenden Fragen** stehen im Absatz „Critique-Re-Run + Feature-Wünsche (offen, hier weitermachen)" direkt im Anschluss.

### Critique-Re-Run + Feature-Wünsche (offen, hier weitermachen — Stand 07.09.2026)

**Ausgangslage für einen neuen Chat:** Es ist noch nichts Neues implementiert worden. Dieser Absatz ist reine Bestandsaufnahme + offene Entscheidungen, damit nichts davon neu erarbeitet werden muss.

**1. Critique-Re-Run (abgeschlossen, dual-agent, gegen die echte Live-App):**
- Score: **14/40 (Poor)**, vorheriger Lauf (06.09., vor allen drei Nachschärfungs-Plänen) war 19/40. **Kein Rückschritt** — dieser Lauf hat zusätzlich `/` und `/login` bewertet, die beim vorherigen Lauf nicht im Scope waren und am schlechtesten abschneiden. Trend-Datei: `.impeccable/critique/2026-09-07T15-17-15Z__vitaloop-web-app.md`.
- Die drei Nachschärfungs-Pläne sind alle als **funktionierend bestätigt** (Chip-Filter, Button-Rangfolge, Kontrast-Fixes) — das Problem liegt an unberührten Stellen.
- **Funde, nach Priorität:**
  - **[P0] Home-Dashboard (`/`)** zeigt in Produktion nur „Platzhalter – Inhalt folgt in Phase 2/3." — ist Startroute und Ziel des ersten Nav-Icons.
  - **[P0] `/login`** wurde von keinem Redesign-Durchgang angefasst — kein Logo, keine Karte, drei optisch identische Buttons (Login/Registrieren/Google).
  - **[P1] Layout ist app-weit zentriert** (`text-align: center` auf `#root` in `src/index.css:71`) — dadurch hat nichts in der App visuelle Hierarchie; das ist die technische Hauptursache für „langweilig". Betrifft u. a. `/body` (`Gewicht (kg)—` ohne Abstand/Größenunterschied zwischen Label und Wert), `/nutrition` (Kalorienstand als Fließtext-Satz statt Zahl/Ring), `/profile` (Formularfelder unterschiedlich eingerückt).
  - **[P1] Bottom-Nav verdeckt echte interaktive Elemente** — Logout-Button auf `/profile` ist per `getBoundingClientRect` zu 58 % von der Nav-Leiste überdeckt und ragt ~4px über den Viewport hinaus (kaum klickbar); kleinerer Fall (2,5px) bei „Fortschrittsfotos" auf `/body`. Fix: `padding-bottom: calc(nav-height + env(safe-area-inset-bottom) + 16px)` auf `main`.
  - **[P1] Leerzustände sind Sackgassen** — „Kein aktiver Plan.", drei „Noch nicht genug Daten"-Karten, sieben Gedankenstriche auf `/body` — nirgends ein Call-to-Action.
  - **[P2] Akzent-Lila scheitert an WCAG AA als Text-/Icon-Farbe** — Links `4,30:1` (nötig 4,5:1), aktives Nav-Icon `2,45:1` (nötig 3:1 für UI-Grafik) — systemisch, betrifft jede Seite mit diesen Tokens.
  - **[P2] Englische Muskelgruppen-Werte in der deutschen UI** — alle 17 Filter-Chips unübersetzt (`lats`, `traps`, `middle back`), alphabetisch nach den englischen Strings sortiert.
- **Offene Entscheidung, noch nicht vom Nutzer beantwortet:** Reihenfolge/Scope der Fixes — Home-Dashboard zuerst, Login zuerst, oder das grundsätzliche Layout-Problem (`text-align: center`) zuerst? Nur die zwei P0-Punkte in dieser Runde, oder alles?
- **Nutzer-Entscheidung (bereits getroffen, 07.09.2026):** Critique-Punkte werden **vor** den Feature-Wünschen (Punkt 2) angegangen.

**2. Feature-Wünsche des Nutzers (Rechercheergebnisse fertig, noch nicht gebrainstormt/geplant):**

Nutzer-Wunsch wörtlich: Bilder pro Übung anzeigen, auf eine Übung klicken können und dann Beschreibung + Ausführung sehen, ein Geräte-Filter (Kurzhantel/Langhantel/etc.) analog zum Muskelgruppen-Filter, und ob eine deutsche Übersetzung möglich ist.

**Bereits recherchiert (Fakten, müssen nicht neu geprüft werden):**
- **Bilder:** `exercises.bild_url` ist bereits importiert (`scripts/import-exercises.ts`, aus `free-exercise-db`, GitHub-gehostet) — Beispiel-URL getestet, liefert 200. Wird aber **aktuell nirgends im UI angezeigt** (nur `exercise.name` in der Liste).
- **Beschreibung/Ausführung:** Die Rohquelle (`scripts/free-exercise-db.json`) hat ein `instructions`-Feld (Array von Schritt-für-Schritt-Sätzen) pro Übung — wird aber beim Import **verworfen**, keine Spalte dafür in `exercises` (Schema: `id, name, kategorie, equipment, muskelgruppen_primaer, muskelgruppen_sekundaer, bild_url, met_wert, created_by, created_at` — siehe `supabase/migrations/0001_initial_schema.sql:80-91`). Bräuchte: neue Migration (z. B. `anleitung text[]`) + Erweiterung von `import-exercises.ts` + Re-Import (unkritisch, ist laut Kommentar im Skript als „one-off seed" mit `created_by = null`-Filter gedacht) + neue Detailansicht (Route oder Dialog).
- **Geräte-Filter:** Daten sind schon da, `equipment`-Spalte bereits importiert. 12 Werte in der Quelle: `bands, barbell, body only, cable, dumbbell, e-z curl bar, exercise ball, foam roll, kettlebells, machine, medicine ball, other`; 77 von 873 Übungen ohne Wert (analog zu `muskelgruppen_primaer`, gleiches Nullable-Handling). Technisch identisch zum gerade gebauten Muskelgruppen-Filter umsetzbar — keine neue Abfrage, kein neues Schema.
- **Level/Schwierigkeitsgrad** (`beginner/intermediate/expert`) existiert ebenfalls in der Quelle, wird auch verworfen — war schon vorher als offener Punkt vermerkt (siehe „Offene Folgevorhaben" oben, Punkt 4), könnte im selben Rutsch wie die Ausführung mitgenommen werden.
- **Übersetzung:** Quelldaten komplett Englisch (Namen, Muskelgruppen, Geräte, Instructions). Für die kleinen festen Vokabulare (17 Muskelgruppen, 12 Geräte) reicht eine statische Mapping-Tabelle (`Record<string,string>`, keine API nötig) — das deckt sich mit dem P2-Fund aus dem Critique oben („Englische Muskelgruppen-Werte"), sollte also ohnehin passieren. Für Freitext (873 Namen + ca. 4000 Instruction-Sätze) bräuchte es eine einmalige Maschinenübersetzung beim Import — dafür gibt es noch keine Anbindung im Projekt; Gemini ist für die spätere Foto-Analyse-Phase vorgesehen und könnte wiederverwendet werden, ist aber eine echte Kosten-/Qualitäts-Abwägung, kein Ein-Klick-Fix. **Noch nicht entschieden, welcher Übersetzungs-Umfang gewünscht ist.**

**Vorgeschlagene Aufteilung (dem Nutzer vorgeschlagen, noch nicht bestätigt):**
- **Geräte-Filter** als eigenes, schnelles **bounded** Vorhaben (kein Schema, bestehender Flow wird nur erweitert — gleiches Muster wie der Muskelgruppen-Filter aus Plan 3).
- **Bilder in der Liste + Klick → Detailseite mit Beschreibung/Ausführung + Übersetzung** zusammen als ein **architektonisches** Vorhaben (Migration, Import-Skript-Änderung, neue UI, Übersetzungs-Entscheidung betrifft beide Teile).

**Nutzer-Entscheidung zur Reihenfolge der Critique-Fixes (07.09.2026):** Jede Priorität einzeln, streng in Reihenfolge P0 → P1 → P2 → ... — nicht alles auf einmal.

**P0/1 (Home-Dashboard) ist gemerged, deployed und live verifiziert** (PR #48, Merge-Commit `086ad96`) — Details im Absatz „Home-Dashboard" weiter unten. **P0/2 (`/login`) ist gemerged, deployed und live verifiziert** (PR #49, Merge-Commit `d1fe15c`) — Details im Absatz „Login-Redesign (P0/2)" weiter unten.

**Zwischendurch (Nutzer-Wunsch, außerhalb der P0→P2-Reihenfolge):** Der Nutzer hat ein eigenes Icon-/Symbol-System liefern lassen (`vitaloop-symbol-system/`, außerhalb des Repos unter `G:\Code Projekte\fitness-app\`) und um app-weite Integration gebeten. **Ist gemerged, deployed und live verifiziert** (PR #50, Merge-Commit `e8fc9e8`) — Details im Absatz „Icon-System-Integration" weiter unten. Lief parallel zu P0/2, war nicht Teil der Critique-Prioritätenliste.

**P1 (zentriertes Layout, Bottom-Nav-Überdeckung, Leerzustand-CTAs) ist gemerged, deployed und live verifiziert** (PR #55, Merge-Commit `d290256`) — Details im Absatz „P1 — Layout-Hierarchie, Nav-Überdeckung, Leerzustand-CTAs" weiter unten.

**P2 (WCAG-Kontrast beim Akzent-Lila, unübersetzte Muskelgruppen-Werte) ist gemerged, deployed und live verifiziert** (PR #56, Merge-Commit `1081eff`) — Details im Absatz „P2 — WCAG-Kontrast Akzent-Lila, Muskelgruppen-Übersetzung" weiter unten. **Damit ist die gesamte P0→P2-Prioritätenliste aus dem Critique-Re-Run abgearbeitet.**

**Wiki-Rückstand nachgezogen (10.09.2026, Commit `54c272a` im Wiki-Repo):** seit der Phase-6-Nachschärfung Plan 1 waren sieben Vorhaben nie ins GitHub-Wiki gespiegelt worden (Nachschärfung Plan 2/Profil, Plan 3/Wall-of-Options, Home-Dashboard, Login-Redesign, Icon-System-Integration, P1, P2). Alle sieben haben jetzt eigene Wiki-Seiten (für die drei ohne eigenes Plan-Dokument — Login-Redesign, Icon-System, P2 — als Zusammenfassung statt als Plan-Mirror), `Home.md`s Statusabschnitt ist auf den aktuellen Stand gebracht, `_Sidebar.md` verlinkt, `Domain-Model.md` um den fehlenden „Home-Bereich"-Absatz ergänzt.

**Geräte-Filter (erste der beiden Feature-Wünsche) ist gemerged, deployed und live verifiziert** (PR #57, Merge-Commit `da739b6`, Stand 10.09.2026) — Details im Absatz „Geräte-Filter" weiter unten.

**Übungsdetails — Bild, Anleitung, Schwierigkeitsgrad (zweite der beiden Feature-Wünsche, ohne die Freitext-Übersetzung) ist gemerged, deployed und live verifiziert** (PR #58, Merge-Commit `4f48a74`, Stand 10./11.09.2026) — Details im Absatz „Übungsdetails" weiter unten.

**Übungs-Übersetzung (Namen + Anleitung ins Deutsche) — gemerged, Merge-Commit `c185b2c` (PR #59), noch nicht deployed/produktiv übersetzt (Stand 11.09.2026):** Spec: `docs/superpowers/specs/2026-09-11-uebungsuebersetzung-design.md`. Plan: `docs/superpowers/plans/2026-09-11-uebungsuebersetzung-plan.md`. Umgesetzt per Subagent-Driven Development in Worktree/Branch `worktree-uebungsuebersetzung`, fünf Tasks (Migration `0009` + Domänenmodell, reine Übersetzungs-Matching-Funktionen, DeepL/Supabase-Wiring in `scripts/translate-exercises.ts`, `Exercise`-Typ + `ExerciseDetailDialog`, `ExercisesPage`-Liste/Suche), alle einzeln reviewt (clean beim ersten Versuch).

**Finale Whole-Branch-Review (Opus) fand drei Important-Findings, in einer Fix-Runde behoben** (Scoped-Re-Review bestätigte alle drei ADDRESSED, keine neue Breakage): (1) die Übungsauswahl im Trainingsplan-Editor (`ExercisePicker` in `TrainingPlanEditPage.tsx`) durchsuchte/zeigte noch den rohen englischen Namen, obwohl dieselben übersetzten Daten schon geladen waren — echte Sackgasse beim Suchen nach dem gerade in der Übungsliste gesehenen deutschen Namen; (2) die Übungsliste war weiterhin serverseitig nach dem englischen `name` sortiert, zeigte aber `name_de` — jetzt clientseitig nach dem angezeigten Namen sortiert (`localeCompare('de')`); (3) die DeepL-Anfrage hatte keinen `formality`-Parameter (App ist durchgängig informell/Du-Form, DeepL übersetzt standardmäßig formell) und keine `source_lang`/Längen-Prüfung — ergänzt um `formality: 'prefer_less'`, `source_lang: 'EN'` und einen Schutz gegen eine längenabweichende DeepL-Antwort, wichtig weil ein bereits abgeschlossener Übersetzungslauf sich nicht günstig korrigieren lässt (Wiederaufnehmbarkeit greift nur bei noch-`null`-Feldern).

**Bewusst zurückgestellt (Ruling der finalen Review, nicht stillschweigend fallen gelassen):** vier weitere Stellen lesen den Übungsnamen weiterhin aus einem eingebetteten Supabase-Select ohne `name_de` — `TrainingPlanEditPage.tsx`s Tag-Zeile (`row.exercise?.name`), `WorkoutSessionPage.tsx`, `TrainingHistoryDetailPage.tsx`, die Legenden der Trainingsanalyse-Charts. Bräuchten neue Spalten in den Embeds von `use-workout-plans.ts`/`use-workout-session.ts`/`use-training-analysis.ts` plus Fallback am Render-Ort — außerhalb des Scopes dieser Spec, die die Frontend-Anzeige explizit nur auf `ExercisesPage.tsx`/`ExerciseDetailDialog.tsx` festgelegt hatte. Eigenes künftiges Vorhaben.

**Stand: 844/844 Tests grün, Lint sauber, `tsc -b --noEmit` sauber, `npm run build` erfolgreich.** Kein DeepL-Account/-Key wird für die Implementierung gebraucht, nur für den späteren Produktivlauf des Skripts.

**Nächster Schritt für einen neuen Chat:** PR #59 ist gemergt (`c185b2c`), Haupt-Checkout ist synchronisiert (`git pull`, 844/844 Tests grün nach einem Kaltstart-Flake im ersten Lauf, Build erfolgreich). **Noch offen:** einmalig `npm run translate-exercises` im Haupt-Checkout laufen lassen (braucht `VITE_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` und neu `DEEPL_API_KEY` als Shell-Env-Variablen — für Letzteres muss der Nutzer zuerst einen kostenlosen DeepL-API-Account anlegen, falls noch nicht vorhanden; keiner dieser Werte wurde in dieser Session gesetzt), danach Deploy (`npm run build && firebase deploy --only hosting:vitaloop`) + manuelle Verifikation laut Manual-Verification-Abschnitt in `docs/superpowers/plans/2026-09-11-uebungsuebersetzung-plan.md`. Optional parallel/danach (unverändert offen): das oben genannte Vier-Stellen-Folgevorhaben, der geparkte Codex-P2-Fund am `measurement`-Icon (siehe „Icon-System-Integration" weiter unten), die in P1 bewusst zurückgestellten Kleinfunde (siehe Absatz „P1" weiter unten), oder ein erneuter `/impeccable critique`-Re-Run.

### Übungsdetails — Bild, Anleitung, Schwierigkeitsgrad (gemerged, deployed, live verifiziert, PR #58, Merge-Commit `4f48a74`, Stand 10./11.09.2026)

Architektonisches Vorhaben (voller `superpowers:brainstorming`-Pfad: Fragen einzeln, Ansätze, Design in Abschnitten, Spec geschrieben und committed). Spec: `docs/superpowers/specs/2026-09-10-uebungsdetails-design.md`, Plan: `docs/superpowers/plans/2026-09-10-uebungsdetails-plan.md`. Umgesetzt per Subagent-Driven Development in Worktree/Branch `worktree-uebungsdetails`, sechs Tasks, jeder einzeln reviewt (alle beim ersten Versuch clean).

**Was gemacht wurde:** Neue Migration `0008_exercise_details.sql` (additiv, nullable, kein Check-Constraint: `anleitung text[]`, `schwierigkeitsgrad text` — `bild_url` existierte bereits seit `0001`, war aber nie im ERD dokumentiert, jetzt nachgezogen). Neues `src/lib/level-labels.ts` (gleiches Muster wie Muskelgruppen-/Geräte-Labels) für die drei `beginner`/`intermediate`/`expert`-Werte. `scripts/import-exercises.ts` liest `instructions`→`anleitung` (leeres Array → `null`, 5 von 873 Übungen ohne Instructions in der Quelle) und `level`→`schwierigkeitsgrad` zusätzlich aus der Rohquelle. `ExercisesPage.tsx`: jede Zeile ist jetzt ein klickbarer Button mit 48×48-Thumbnail (`bild_url`, `alt=""`, `loading="lazy"`, `referrerPolicy="no-referrer"`) oder `VitaIcon`-Platzhalter, Klick öffnet ein zweites `Dialog` mit der neuen `ExerciseDetailDialog`-Komponente (Bild, übersetzte Muskelgruppen-/Geräte-Tags als reine `<span>`s statt `Chip` — read-only Fakten, keine Toggle-Filter —, übersetzter Schwierigkeitsgrad, nummerierte Anleitung oder „Keine Anleitung hinterlegt."). „Eigene Übung anlegen" um drei optionale Textfelder erweitert (Bild-URL, Schwierigkeitsgrad, Anleitung als Textarea mit Zeilenumbruch-Split), bewusst kein Dropdown — konsistent zum bestehenden `Kategorie`-Feld.

**Finaler Whole-Branch-Review (Opus) fand zwei Important-Findings**, in einer Fix-Runde behoben (Scoped-Re-Review bestätigt, keine offenen Funde): fehlendes `loading="lazy"` auf dem Listen-Thumbnail (bis zu ~873 Zeilen unvirtualisiert, sonst laden alle Bilder parallel vom GitHub-Raw-Host), und die klickbare Zeile hatte nur `cardClass` ohne `interactiveClass` (kein Hover/Fokus-Ring, keine visuelle Klickbarkeits-Andeutung) — dabei gleich `text-left` (Button-Default ist zentriert), `shrink-0` auf dem Platzhalter-Icon und `referrerPolicy="no-referrer"` auf beiden Übungsbildern ergänzt (die geteilte `exercises`-Tabelle hat eine Select-All-RLS-Policy, ein nutzereigener `bild_url`-Wert wird also im Browser jedes Betrachters geladen — `no-referrer` verhindert, dass dabei die aufrufende Seiten-URL an den fremden Bild-Host geleakt wird). Mehrere Minor-Funde bewusst zurückgestellt (u. a. fehlender Test für „nur Leerzeilen in der Anleitung" — ein Plan-Defekt, die Spec hatte den Fall vorgesehen, der Plan hat ihn beim Schreiben verloren; Verhalten läuft vermutlich korrekt über denselben Codepfad wie der bereits getestete „keine Anleitung"-Fall, nur nicht direkt abgesichert).

**Stand: 824/824 Tests grün**, Lint sauber, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. **PR #58 gemergt** (Merge-Commit `4f48a74`). **Import-Skript einmalig gegen Produktion gelaufen** (`npm run import-exercises` im Hauptcheckout, mit `VITE_SUPABASE_URL` und `SUPABASE_SERVICE_ROLE_KEY` als Shell-Env-Variablen — Node lädt `.env` nicht automatisch, nur Vite selbst; der Service-Role-Key wurde vom Nutzer selbst gesetzt, nie im Chat geteilt): „imported 873 exercises". **Deployed**, Hash-Sanity-Check bestanden (`index-DSc6Pg1T.js` lokal = live via `curl`). **Live-Verifikation** (Chrome, `/training/exercises`): 873 klickbare Zeilen mit ladenden Thumbnails (Bild-DOM-Check bestätigt `complete: true`, `naturalWidth: 750`), Klick auf „3/4 Sit-Up" öffnet den Dialog mit „Bauch" (übersetzte Muskelgruppe), „Eigengewicht" (übersetztes Gerät), „Anfänger" (übersetzter Schwierigkeitsgrad) und der — wie geplant noch englischen — Anleitung. Konsole ohne Fehler.

### Geräte-Filter (gemerged, deployed, live verifiziert, PR #57, Merge-Commit `da739b6`, Stand 10.09.2026)

Bounded-Task (kein Spec/Plan-Dokument, direkt per `superpowers:brainstorming`-Bounded-Pfad im Chat abgestimmt). Direkt auf `master` umgesetzt (kein Worktree), Branch `geraete-filter`.

**Was gemacht wurde:** Geräte-Filter für `/training/exercises`, exakt analog zum bestehenden Muskelgruppen-Filter aus Plan 3 — `exercises.equipment` war bereits importiert, aber nirgends im UI angezeigt. Neues Modul `src/lib/equipment-labels.ts` (`Record<string,string>` + `equipmentLabel()`-Helfer mit Fallback), gleiches Muster wie `muscle-group-labels.ts`, für die 12 Quellwerte. Unterschied zur Vorlage: `equipment` ist ein einzelner Wert (`string | null`), kein Array — Filtervergleich per `===` statt `.includes()`. Zweite Chip-Zeile in `ExercisesPage.tsx`, eigene Reset-Chip „Alle Geräte" (bewusst nicht „Alle" wie bei der Muskelgruppen-Zeile — zwei gleichnamige Buttons wären nicht eindeutig ansprechbar/testbar). Beide Filter kombinieren sich per AND mit der Namenssuche.

**TDD durchlaufen** (RED bestätigt vor jeder Implementierung): `equipment-labels.test.ts` (12 Werte + Fallback), vier neue Fälle in `ExercisesPage.test.tsx` (Filtern, Reset, Kombination mit Muskelgruppen-Filter, Ausblenden der Zeile ohne Geräte-Werte). **Stand: 808 Tests gesamt** (+6 gegenüber der P2-Baseline von 802), davon 3 vorbestehende, unter Volllast flockige `NutritionPage.test.tsx`-Fälle (isoliert grün, nicht von dieser Änderung betroffen). Lint sauber, `tsc -b --noEmit` sauber, Build erfolgreich. Kein Migration nötig (`docs/domaenenmodell.md` unverändert, letzte Migration bleibt `0007`).

**Eigener Whole-Diff-Review** (keine Funde) **plus unabhängige Codex-Gegenprüfung** (`gpt-5.6-sol`, inkl. eigener Auszählung der Equipment-Werte aus `scripts/free-exercise-db.json`) — „No actionable defects were identified."

**PR #57 gegen `master` gemergt** (Merge-Commit `da739b6`, vom Nutzer selbst gemergt). **Deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop` im Hauptcheckout — erstmals von Claude selbst ausgeführt, siehe Permission-Regel im Hosting-Abschnitt oben; Hash-Sanity-Check bestanden, `index-CnlwF4MQ.js` lokal = live via `curl`). **Live-Verifikation im echten Chrome-Tab gegen Produktion:** `/training/exercises` zeigt alle 12 übersetzten Geräte-Chips alphabetisch sortiert unter der Muskelgruppen-Zeile; Klick auf „Langhantel" filtert korrekt auf Barbell-Übungen, Chip wird aktiv hervorgehoben.

### P2 — WCAG-Kontrast Akzent-Lila, Muskelgruppen-Übersetzung (gemerged, deployed, live verifiziert, PR #56, Merge-Commit `1081eff`, Stand 10.09.2026)

Bounded-Task (kein Spec/Plan-Dokument, direkt per `superpowers:brainstorming`-Bounded-Pfad mit dem Nutzer abgestimmt — Scope-Frage zur T6-Grafik per Rückfrage geklärt, siehe unten). Umgesetzt in Worktree/Branch `p2-kontrast-uebersetzung`, direkt in einer Sitzung (kein Multi-Task-SDD-Loop nötig).

**Kontrast-Fix:** `--color-accent` (#8766ed) scheiterte als Text-/Icon-Farbe an WCAG AA — Links 4,3:1 gegen `surface` (nötig 4,5:1), aktives Bottom-Nav-Icon 2,45:1 gegen `surface-raised` (nötig 3:1 für UI-Grafik). Neuer Token `--color-accent-text: #a288f1` (gleicher Farbton/Sättigung wie `--color-accent`, H254,6°/S78,8%, nur von L66,5% auf L74% aufgehellt) — rechnerisch bestätigt 6,11:1 gegen `bg`, 5,39:1 gegen `surface`, 3,49:1 gegen `surface-raised`, jeweils mit Puffer (`src/lib/contrast.test.ts`). `--color-accent` selbst bleibt unverändert (Hintergrundfarbe für Buttons/Chips/Charts) — dasselbe Muster wie beim `--color-on-bright`-Fix aus der Phase-6-Nachschärfung. `index.css`s globale `a { color }`-Regel nutzt jetzt `--color-accent-text`. `VitaIcon.tsx`s `tone="brand"` hatte den Akzentwert literal als `#8766ed` hartkodiert (SVG-Stroke-Attribut kann keine CSS-Variable lesen, analog zu `chart-colors.ts`) — auf `#a288f1` geändert, behebt in einem Zug sowohl das aktive Bottom-Nav-Icon als auch alle ~30 „Zurück"/Abschnitts-Icons neben Links im Code. `BottomNav.tsx`s Klassenname von `text-accent` auf `text-accent-text` nachgezogen (aktuell ohne sichtbaren Effekt, da `tone="brand"` die Klasse überschreibt, aber wieder korrekt für den Fall, dass das Icon je auf `mono` wechselt).

**Übersetzung:** Neues Modul `src/lib/muscle-group-labels.ts` mit einer statischen `Record<string,string>`-Mapping-Tabelle für alle 17 Werte aus `exercises.muskelgruppen_primaer` (`abdominals`…`triceps`, Quelle: free-exercise-db), plus Helfer `muskelgruppeLabel(value)` mit Fallback auf den Rohwert. `ExercisesPage.tsx`: Filterlogik/State bleiben auf den englischen Rohwerten (keine Verhaltensänderung), Sortierung und Chip-Text wechseln auf die deutschen Labels. **Nutzer-Entscheidung (Rückfrage vor der Umsetzung):** die T6-Grafik „Volumen je Muskelgruppe" (`MuscleVolumeChart.tsx`, Trainingsanalyse) zeigte dieselben rohen englischen Werte auf der X-Achse — nicht Teil des ursprünglichen Critique-Funds (nur die 17 Filter-Chips waren benannt), aber derselbe Root-Cause und dieselbe Mapping-Tabelle behebt beides — auf Wunsch mitgenommen (Achsen-`tickFormatter` und Tooltip-`labelFormatter`, Sortierung/`Cell`-Key bleiben unverändert auf dem Rohwert).

**Bewusst nicht per Achsentext-Test geprüft:** Ein erster Testentwurf assertete direkt auf den übersetzten Achsentext von T6 — widerspricht der dokumentierten Projekt-Konvention „Graph-Tests prüfen gezeichnete Marken, nie Achsentexte" (Recharts' Tick-Skipping ist eine Layout-Heuristik, die in jsdom anders ausfällt; Plan 2b hatte genau diesen Fehler schon einmal im Review gefunden). Test wieder entfernt — die Übersetzung selbst ist über `muscle-group-labels.test.ts` (alle 17 Werte + Fallback) abgedeckt, die Chart-Verkabelung über eine manuelle Browser-Verifikation (siehe unten).

**Whole-Diff-Review (selbst durchgeführt, kein Subagent) und ein kleiner Nachtrag:** ein Doku-Kommentar in `VitaIcon.tsx` referenzierte noch die alte Klasse `text-accent` statt `text-accent-text` — korrigiert. Keine weiteren Funde: kein zweiter `#8766ed`-Literal als Text-/Icon-Farbe übersehen (`--color-chart-violet`/`CHART_VIOLET` bleiben unverändert, sind kein Fund dieses Tasks), `muskelgruppen_sekundaer` wird nirgends im UI angezeigt (kein zweiter Übersetzungs-Ort nötig). **Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** — „No actionable correctness regressions were identified." Deckt sich mit dem eigenen Review.

**Stand: 802/802 Tests grün** (+5 gegenüber der P1-Baseline von 797: 2 neue Modul-Tests für die Mapping-Tabelle, 3 neue Kontrast-Tests), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Kein Migration (`docs/domaenenmodell.md` unverändert, letzte Migration bleibt `0007`).

**Manuelle Browser-Verifikation (echter, bereits eingeloggter Chrome-Tab, erst gegen `npm run dev` mit echter `.env` aus dem Hauptcheckout kopiert — nicht committet —, danach erneut gegen die echte Live-App nach dem Deploy):** `getComputedStyle` bestätigt `rgb(162,136,241)` = `#a288f1` sowohl auf dem aktiven Bottom-Nav-Icon als auch auf standalone „Zurück"-Links (vorher `#8766ed`); `/training/exercises` zeigt alle 17 Muskelgruppen-Chips mit deutschen Labels, korrekt alphabetisch sortiert, Klick auf „Brust" filtert weiterhin richtig (Filterlogik arbeitet unverändert auf dem englischen Rohwert). T6 („Volumen je Muskelgruppe") hatte auf dem Test-Account keine Trainingssätze (Leerzustand) — über eine Wegwerf-Testseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx`, beide nicht committet, nach der Prüfung vollständig entfernt) mit Fixture-Sätzen geprüft: Achse zeigt „Brust", „Quadrizeps", „Latissimus", „mittlerer Rücken" statt der rohen englischen Werte. Konsole auf allen geprüften Seiten ohne Fehler.

**PR #56 gegen `master` gemergt** (Merge-Commit `1081eff`, alle vier CI-Checks grün: build-test, npm-audit, semgrep, zap-baseline, vom Nutzer selbst gemergt). **Live deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop` im Hauptcheckout — der Deploy-Befehl selbst lief über den Nutzer, da der Auto-Mode-Classifier sowohl `gh pr merge` als auch `firebase deploy` als schreibende Aktionen auf geteiltem/Produktions-Zustand blockiert hat, unabhängig von einer im Chat erteilten Freigabe; für künftige Deploys gilt das genauso), Hash-Sanity-Check bestanden (`index-Bn-ZqrCX.js` lokal = live via `curl`). **Live-Verifikation im echten, eingeloggten Chrome-Tab gegen Produktion:** siehe oben, identische Bestätigung wie im Dev-Server-Durchlauf.

### P1 — Layout-Hierarchie, Nav-Überdeckung, Leerzustand-CTAs (gemerged, deployed, live verifiziert, PR #55, Merge-Commit `d290256`, Stand 10.09.2026)

Spec: `docs/superpowers/specs/2026-09-10-p1-layout-haerten-design.md`, Plan: `docs/superpowers/plans/2026-09-10-p1-layout-haerten-plan.md`. Umgesetzt per Subagent-Driven Development in Worktree/Branch `p1-layout-haerten`, sechs Tasks:

1. **Foundation:** `text-align: center` aus der `#root`-Regel in `src/index.css` entfernt (linksbündig ist jetzt der App-weite Standard, mit gezielten Ausnahmen wie `/login`, das schon vorher jedes Element explizit `text-center`/`text-left` gesetzt hatte und daher unverändert blieb); `main`s Padding-Bottom auf `calc(4rem + env(safe-area-inset-bottom) + 16px)` erhöht, damit die schwebende Bottom-Nav-Pille nichts mehr dauerhaft verdeckt.
2. **`ChartFrame`** bekam einen optionalen `leerCta?: { label: string; to: string }`-Prop — ein Link zur passenden Eintragsaktion statt eines toten Endes im Leerzustand.
3. **Trainings-, 4. Ernährungs-, 5. Körper-Bereich:** `leerCta` durch alle 19 Chart-Komponenten und die drei `*ChartList`-Komponenten durchgereicht, mit `{ label: 'Training starten', to: '/training' }` / `{ label: 'Eintragen', to: '/nutrition' }` / `{ label: 'Heute eintragen', to: '/body' }` befüllt. Dazu die drei im Critique benannten Hierarchie-Fixes: `TrainingPage`s „Kein aktiver Plan." bekam einen „Trainingsplan anlegen"-Button, `DailySummary` zeigt den Kalorienstand jetzt als große Zahl mit kleineren Sekundärzeilen statt als Fließtext-Satz, `BodyPage`s Kennzahlen-Karten haben jetzt eine Label/Wert-Hierarchie (Label klein/gedämpft, Wert groß/betont).
6. **App-weite Verifikation** (vom Controller selbst ausgeführt statt per Subagent, siehe Ruling unten): echter Chrome gegen einen frischen Test-Account (`p1-verify-test@example.com`, per Registrierungsformular angelegt — existiert noch im Supabase-Projekt, keine automatische Löschmöglichkeit über die App; bei Bedarf manuell im Supabase-Dashboard entfernen). Bottom-Nav-Überdeckung an den beiden im Critique benannten Stellen per `getBoundingClientRect` gemessen — am tatsächlichen Seitenende hat der Logout-Button auf `/profile` **70px** Abstand zur Nav-Pille (vorher 58 % überdeckt + 4px über den Viewport hinausragend), „Fortschrittsfotos" auf `/body` ebenso **70px** Abstand (vorher 2,5px überdeckt). **Wichtig für die spätere Nutzung:** Bei einer Seite, die länger als der Viewport ist (z. B. `/nutrition` mit vielen Chart-Karten), überdeckt die schwebende Nav-Pille beim Scrollen durch die Seitenmitte trotzdem kurzzeitig Inhalte — das ist eine inhärente Eigenschaft eines `position: sticky`-Floating-Buttons (kein Fixed-Overlay mit dauerhafter Content-Aussparung) und war schon vor P1 so; behoben ist nur das dauerhafte Überdecktbleiben am echten Seitenende, was der einzige vom Critique benannte Fall war. Alle sechs Task-Reviews liefen clean (keine Critical/Important-Findings), vier kleinere Minor-Funde je einzeln nicht behoben (Doku-Ungenauigkeiten in Implementer-Reports, kosmetische Zeilenumbrüche) — keiner davon hat Code-Auswirkung, Details in den Commit-Messages der jeweiligen Task-Commits (siehe `git log` auf dem Merge-Commit `d290256`, das SDD-Ledger selbst war git-ignorierte Worktree-Scratch und ist mit dem Worktree entfernt).

**Whole-Branch-Review (Opus) fand danach zwei Important-Findings** — der `leerCta`-Link auf den drei Dashboards verlinkte auf die aktuelle Seite selbst (toter Klick, widersprach einer bereits im Home-Dashboard-Abschnitt getroffenen Entscheidung gegen genau dieses Muster), und auf den drei Analyse-Seiten wiederholte er sich bis zu 8× pro leerem Chart (widersprach dem bestehenden „eine Meldung pro Bereich, nicht pro Chart"-Prinzip). **In einer Fix-Runde behoben** (Commit `89efce4`): `leerCta` komplett von den drei Dashboard-Aufrufstellen entfernt, in den drei `*ChartList`-Komponenten auf höchstens den ersten Chart der Liste begrenzt (`index === 0`) — bewusster Kompromiss statt einer „ersten wirklich leeren Chart"-Logik: zeigt auf der Analyse-Seite jetzt maximal einen CTA statt bis zu acht, live gegen Produktion bestätigt (genau 1 Link auf `/training/analyse`). Scoped Re-Review bestätigte beide Findings behoben, keine neue Regression. **Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft:** „No actionable correctness regressions were identified." — deckt sich mit dem Opus-Verdict. Vier weitere kleinere Minor-Funde (verbleibende zentrierte Listenzeilen über `li`/`a`s `justify-content: center`, ungenutzte `gap-2`-Klasse auf dem Chart-CTA-Link, visueller Gewichtsunterschied zwischen den beiden CTA-Stilen, `leerCta`-Typ 23× inline statt als Type-Alias) plus eine Test-Lücke (kein Test prüft die Index-0-Begrenzung selbst) bewusst zurückgestellt für P2/Phase 7 — nicht Teil dieses PRs.

**Stand: 797/797 Tests grün, Lint sauber, Build erfolgreich.** Keine Migration (`docs/domaenenmodell.md` unverändert, letzte Migration bleibt `0007`). **PR #55 gemergt** (Merge-Commit `d290256`), **deployed** (Hash-Sanity-Check bestanden: `curl https://vitaloop.web.app/` liefert `index-DgSzAX-H.js`, identisch zum lokalen Build). **Live-Verifikation im echten, eingeloggten Chrome-Tab gegen Produktion:** `/training` zeigt „Trainingsplan anlegen"-Button und keinen toten Chart-CTA mehr auf dem Dashboard; `/training/analyse` zeigt genau einen „Training starten"-Link (nicht mehr bis zu acht) mit Ziel `/training`.

### Login-Redesign (P0/2) — gemerged, deployed, live verifiziert (PR #49, Merge-Commit `d1fe15c`, Stand 08.09.2026)

Bounded-Task (kein Spec/Plan-Dokument, direkt per `superpowers:brainstorming`-Bounded-Pfad mit dem Nutzer abgestimmt: Logo-Frage per Rückfrage geklärt — Nutzer hat ein eigenes Logo geliefert, `Logo.png` im übergeordneten `fitness-app`-Ordner). Umgesetzt in Worktree/Branch `worktree-login-redesign`, direkt (keine Multi-Task-SDD-Schleife nötig für eine einzelne Seite).

**Was gemacht wurde:** `src/pages/LoginPage.tsx` (zuvor komplett unstyled: kein Logo, keine Karte, drei optisch identische Buttons) auf das Phase-6-Designsystem gehoben. VitaLoop-Wordmark oben (vom Nutzer als PNG geliefert, auf 360×360 WebP verkleinert/konvertiert, `public/logo.webp`, 22 KB statt ursprünglich 474 KB). `buttonPrimaryClass` für Login/Registrieren (Haupt-Aktion), `buttonSecondaryClass` für „Mit Google anmelden" (gleichwertige Alternative, optisch zurückhaltender), reiner Text-Button mit explizitem `border-0 bg-transparent`-Reset für den Login/Registrieren-Modus-Wechsel (keine Aktion mit Seiteneffekt). Kein `cardClass`-Wrapper um das ganze Formular — sitzt direkt auf der Seiten-Hintergrundfarbe, analog dazu, wie `HomePage`/`ProfilePage` `cardClass` nur für gruppierte Unterabschnitte nutzen, nie als Vollseiten-Wrapper.

**Neue geteilte Recipe `inputClass`** in `src/lib/ui-classes.ts`, nach demselben Muster wie `cardClass`/`buttonPrimaryClass`/`buttonSecondaryClass`. Bewusst `bg-surface-raised` statt `bg-surface`: ein Eingabefeld muss sich auch abheben, wenn es (wie überall außer `/login`) später in einer `cardClass`-Karte verwendet wird — `bg-surface` wäre dort mit der Karte selbst identisch und unsichtbar.

**Ein echter Bug während der eigenen Verifikation gefunden und selbst behoben, bevor er in den Review ging:** die erste Fassung wrappte das Formular doch in `cardClass` und nutzte `buttonSecondaryClass` unverändert für den Google-Button — im echten Chrome-Screenshot zeigte sich, dass der Button dadurch optisch unsichtbar wurde (`cardClass` und `buttonSecondaryClass` teilen beide `bg-surface`, identische Füllfarbe). Behoben durch Entfernen des Karten-Wrappers, bevor überhaupt ein Review lief — **derselbe Fehlertyp, der in Phase 6 Plan 2b bereits als Lehre dokumentiert ist** (Toast-hinter-Dialog), hier aber selbst gefunden statt vom Reviewer.

**Whole-Diff-Review (Opus) und eine Fix-Runde:** 3 Important — (1) `inputClass` fehlte `m-0` gegen `index.css`s globale `button, input, select { margin: 4px }`-Basisregel, wodurch beide Felder 8px breiter als die Buttons darunter liefen (derselbe Bleed-Fehlertyp wie schon zweimal zuvor im Projekt, nur diesmal bei `margin` statt `padding`); (2) `inputClass`s `bg-surface`-Füllung wäre in jeder künftigen Wiederverwendung innerhalb einer `cardClass`-Karte unsichtbar gewesen — derselbe Bug wie beim Google-Button, aber in der geteilten Recipe selbst statt an einer Aufrufstelle; (3) `public/logo.png` mit 474 KB bei ~126px Darstellungsgröße, mit Abstand die größte Asset-Datei im Repo. Dazu mehrere Minor (h1-Basis-Margin, fehlender Fokus-Ring auf dem Toggle-Button, durch `#root`s `text-align:center` zentrierte Feld-Label, `min-h-screen` statt `min-h-dvh`, fehlendes `autoComplete`). Alle in einer Fix-Runde behoben (Logo zusätzlich auf 360×360 WebP verkleinert, 22 KB). **Scoped Re-Review bestätigte alle 8 Findings ADDRESSED**, keine neue Regression, eigene Kontrastrechnung bestätigte `bg-surface-raised` (`#414249`) sichtbar unterscheidbar von sowohl `bg-bg` (`#181920`, 1,88:1) als auch `bg-surface` (`#23242b`, 1,66:1) — unterschreitet zwar WCAG 1.4.11s 3:1-Schwelle für Non-Text-Kontrast, das ist aber eine vorbestehende Design-System-Eigenschaft (`Dialog.tsx`/`BottomNav.tsx` nutzen `surface-raised` auf denselben Untergründen bereits), kein neuer Regressionspunkt dieses Tasks — für Phase 7 vorgemerkt, nicht in diesem Task behoben.

**Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** (siehe „Codex/GPT als Zweitmeinung" oben) — „No actionable regressions were identified in the changed code." Deckt sich mit dem Opus-Verdict.

**Stand: 795/795 Tests grün** (ein erster Lauf zeigte den bekannten Kaltstart-Flake bei den React-lazy-Chart-Tests in `TrainingPage.test.tsx` u.a., siehe „Fünf Dinge, die beim Weiterbauen gelten" in Phase 5 — sauberer Re-Run direkt danach war grün), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Kein Migration in diesem Task, `docs/domaenenmodell.md` unverändert (reine Frontend-Styling-Änderung, letzte Migration bleibt `0007`).

**Manuelle Browser-Verifikation (echter Chrome, `npm run dev` mit Dummy-`.env` — `/login` ist unauthentifiziert erreichbar, kein echter Supabase-Zugriff nötig):** Logo, Karten-lose Formular-Anordnung, primärer/sekundärer/tertiärer Button-Look wie geplant bestätigt; Validierungsfehler zeigt sichtbaren Danger-Text; per `getBoundingClientRect`/`getComputedStyle` bestätigt: Eingabefeld und Submit-Button exakt gleiche Breite und Startposition (`m-0`-Fix wirkt, kein 8px-Versatz mehr), Feld-Hintergrund `rgb(65,66,73)` = `#414249` = `bg-surface-raised`.

**PR #49 gemergt** (`worktree-login-redesign` → `master`, Merge-Commit `d1fe15c`, alle vier CI-Checks vorher grün). Noch offen: Deploy + Hash-Sanity-Check gegen `https://vitaloop.web.app` — bewusst zurückgestellt, bis auch PR #50 (Icon-System) gemergt ist, um nicht zweimal hintereinander zu deployen.

### Icon-System-Integration (gemerged, deployed, live verifiziert, PR #50, Merge-Commit `e8fc9e8`, Stand 08.09.2026)

Kein Plan-Dokument (mechanische, vom Symbol-System selbst schon vollständig spezifizierte Integration — Icon-Namen, Platzierung je Datei und Priorität standen bereits in dessen README, keine offene Design-Entscheidung). Umgesetzt in Worktree/Branch `worktree-icon-system`, direkt in einer Sitzung (kein Multi-Task-SDD-Loop nötig).

**Was gemacht wurde:** `VitaIcon.tsx` (24 Icons, gleiche Formensprache wie das Logo) byte-identisch aus `vitaloop-symbol-system/VitaIcon.tsx` als `src/components/icons/VitaIcon.tsx` übernommen. `lucide-react` komplett ersetzt (nur noch in `BottomNav.tsx` verwendet) und als Dependency entfernt. `Dialog.tsx`s „✕"-Schließen-Button nutzt jetzt das close-Icon (wirkt app-weit in jedem Dialog). Icons auf allen vier Bereichs-Dashboards, deren Unterseiten und den universellen Aktionen (edit/delete/save/search/scan/retry/logout/back) gesetzt, nach der im Symbol-System mitgelieferten Platzierungs-Tabelle. Bewusst ausgelassen: Google-„G" auf dem OAuth-Button (Symbol-System verlangt explizit das offizielle Google-Logo) und der VitaLoop-Schriftzug auf `/login` (eigenes, separates P0/2-Vorhaben, PR #49).

**Ein echter Bug während der Umsetzung gefunden und selbst behoben, bevor er in den Review ging:** `NutritionEntriesPage.tsx`s Öffnen-Button je Mahlzeiten-Abschnitt hieß „+ Hinzufügen"; das Ersetzen des „+" durch ein Icon hätte den Text auf bloßes „Hinzufügen" verkürzt — identisch mit dem Namen von `AddEntryFlow`s eigenem Absenden-Button, der bei offenem Dialog im selben `<section>` mitgemountet bleibt (`Dialog.tsx` hält Kinder immer gemountet). Das brach sofort mehrere Tests mit „mehrdeutiges `getByRole`". Behoben durch Umbenennen des Öffnen-Buttons auf „Eintrag hinzufügen" (Component und alle 7 betroffenen Testzeilen).

**Whole-Diff-Review (Opus) und eine Fix-Runde:** 2 Important — (1) alle 31 (Commit-Message nennt fälschlich 29, tatsächlich 31, alle korrekt behoben) neuen `<Link>`-Elemente mit Icon nutzten `inline-flex`, verloren damit aber gegen `index.css`s `@layer base`-Regel `main a:not(li a):not(p a) { display: flex }` (die Regel, die Standalone-Links ihre eigene volle Zeile gibt) — Tailwinds `@layer utilities` gewinnt immer gegen `@layer base`, unabhängig von Spezifität. **Dritte Wiederholung dieser index.css/Tailwind-Layer-Fehlerklasse im Projekt** (nach Plan 1s Cascade-Bug und Plan 2d/Task 6s Karten-Abstand). (2) `HomePage`s zwei Karten-Überschriften (`<h2 className="flex items-center gap-2">`) verloren durch `flex` ohne `justify-center` die von `#root` geerbte Zentrierung. Beide behoben (`flex items-center justify-center gap-2`), im echten Chrome-Tab verifiziert (`getComputedStyle` bestätigt `display: flex`, volle Breite von 1078px pro Link). Dazu 2 Minor im selben Rutsch: Icon-Größe auf den beiden Karten-Überschriften laut Spec-Tabelle auf 28px angehoben (war Default 24px); `ManualProductForm.tsx`s „Produkt speichern" bekam als einziger primärer Speichern-Button im ganzen Formular-Bestand noch kein Save-Icon.

**Scoped Re-Review bestätigte beide Important-Findings exakt** (31/31 Stellen per `grep` bestätigt, keine halb umgesetzte Stelle), zusätzlich unabhängig in den gebauten CSS-Bytes gegengeprüft (`@layer base` vor `@layer utilities`, `main a:not(li a):not(p a){display:flex}` vs. `.flex{display:flex}` — Utilities-Layer gewinnt, Fix ist jetzt layer-redundant aber konsistent). Keine neue Regression, 794/794 Tests bestätigt (ein Kaltstart-Flake bei React-lazy-Chart-Tests im ersten Lauf, sauberer Re-Run grün).

**Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** — fand einen eigenständigen P2-Fund: das `measurement`-Icon-Pfadende liegt exakt auf `x=0` der 24×24-viewBox (`M4 8.2c3.8-4.1…-2.4-6.8` endet rechnerisch bei `(0, 8.2)`), wodurch die 2,2px-Rundkappen-Strichbreite dort leicht über den Viewport hinausragt und abgeschnitten wird. **Bewusst nicht in dieser Fix-Runde behoben:** Das ist eine geometrische Eigenschaft der Original-Pfaddaten aus dem vom Nutzer gelieferten Symbol-System (byte-identisch übernommen, keine Regression dieser Integration selbst), und eine Korrektur würde die Geometrie der user-eigenen Grafik verändern — eine Design-Entscheidung, keine reine Code-Korrektur. Für eine künftige Politur-Runde vorgemerkt, bei Bedarf mit dem Nutzer abstimmen (z. B. Pfadendpunkt um ~1px nach rechts verschieben).

**Stand: 794/794 Tests grün**, Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Keine Migration, `docs/domaenenmodell.md` unverändert (reine Frontend-Änderung, letzte Migration bleibt `0007`).

**Manuelle Browser-Verifikation (echter Chrome, `npm run dev` mit Dummy-`.env` über eine Wegwerf-Testseite, nicht committet):** alle 24 Icons rendern korrekt in Marken-Farben; `BottomNav` zeigt alle vier Icons; Primär-Button-Icon (mono) liest sich klar gegen den Accent-Hintergrund; Sekundär-Button-Icon (mono) klar gegen die Karten-/Seiten-Fläche; `Dialog`s neues Close-Icon funktioniert und schließt den Dialog. Nach dem Link-Fix separat verifiziert: drei Beispiel-Links rendern als eigene volle Zeilen (`display: flex`, 1078px Breite je Link, `getBoundingClientRect` bestätigt), nicht mehr zusammengeschoben.

**PR #49 wurde zuerst gemergt** — dieser Branch wurde danach auf den neuen `master` rebased (einziger Konflikt: dieser Status-Absatz in `CLAUDE.md`, beide Seiten manuell zusammengeführt) und CI lief nach dem Rebase sauber erneut durch, bevor gemergt wurde. **PR #50 gemergt** (Merge-Commit `e8fc9e8`, alle vier CI-Checks grün). **Gemeinsamer Deploy im Hauptcheckout** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop`), Hash-Sanity-Check bestanden (`index-BrTugB-h.js` lokal = live via `curl`). **Live-Verifikation im echten, eingeloggten Chrome-Tab:** `/login` zeigt Logo/Karten-loses Formular korrekt; `/` (Home) zeigt Profil-Icon im Header, alle vier Bereichs-Links als eigene volle Zeile mit Icon (der Link-Layout-Fix wirkt live), Home-Icon in der Bottom-Nav aktiv/lila; `/training` zeigt „Meine Pläne"/„Übungen"/„Trainingshistorie"/„Analyse" je mit Icon, Training-Icon in der Bottom-Nav korrekt aktiv/lila während Home inaktiv/grau ist. Konsole ohne Fehler. Beide Worktrees (`login-redesign`, `icon-system`) und lokalen Branches entfernt.

**Mahlzeiten-Abschnitte (gemerged, alle 9 Tasks fertig):** Einträge auf `/nutrition/entries` sind nach Mahlzeiten gegliedert — sechs feste Slots, vier davon vorbelegt (Frühstück, Mittagessen, Abendessen, Snacks), die restlichen zwei optional und nur sichtbar, sobald sie einen Namen oder Einträge haben. Die Namen stehen im Profil unter „Mahlzeiten"; welchem Abschnitt ein Eintrag zugeordnet ist, ergibt sich daraus, in welchem Abschnitt er erfasst wurde. Alt-Einträge von vor der Migration stehen unter „Ohne Zuordnung" und lassen sich über „Bearbeiten" nachträglich einsortieren. Das Ernährungs-Dashboard zeigt die Kalorien je Abschnitt als Link zur Eintragsliste. Enthält Migration `0003_meal_sections.sql` (fügt nur Spalten hinzu; bestehende Zeilen bekommen `mahlzeit = null`). Spec: `docs/superpowers/specs/2026-08-20-mahlzeiten-abschnitte-design.md`, Plan: `docs/superpowers/plans/2026-08-20-mahlzeiten-abschnitte-plan.md`.

**Einträge und Produkte bearbeiten (Branch `feat-entry-editing`, Task 8 von 8 fertig):** Über einen „Bearbeiten"-Button in der Eintragsliste unter `/nutrition/entries` lassen sich bei einem Eintrag Menge, Zeitpunkt, das verknüpfte Produkt und dessen Nährwerte ändern. Eigentümerregel in einem Satz: Ein Produkt, das dem Nutzer gehört, wird direkt aktualisiert und behält seinen Barcode; ein fremdes Produkt wird kopiert (Kopie ohne Barcode) und der Eintrag auf die Kopie umgehängt — geschrieben wird nur, wenn sich Name oder Nährwerte tatsächlich geändert haben. Spec: `docs/superpowers/specs/2026-08-19-eintraege-bearbeiten-design.md`, Plan: `docs/superpowers/plans/2026-08-19-eintraege-bearbeiten-plan.md`.

**Phase-6-Nachschärfung Plan 1 (Design-Tokens) — gemerged, deployed, verifiziert:** Spec: `docs/superpowers/specs/2026-09-06-phase6-nachschaerfung-design.md`, Plan: `docs/superpowers/plans/2026-09-06-phase6-nachschaerfung-plan1-tokens.md`. WCAG-AA-Kontrast-Fix für `--color-text-muted` und neuer `--color-on-bright`-Token; Hover-/Press-/Focus-visible-Motion auf Buttons, `Chip`, `BottomNav` und der Einblend-Animation des Toasts ergänzt, über einen geteilten `interactiveClass`-Export in `src/lib/ui-classes.ts`. 7 Tasks, alle Reviews clean (ein Fix-Round bei Task 3 — ein `ring-offset`-Widerspruch im Plan selbst, vor Dispatch korrigiert). Whole-Branch-Review (Opus) fand danach 2 Important (BottomNav-Fokusring unterschritt 3:1 gegen die Nav-Pille; `active:scale` lief trotz `prefers-reduced-motion`) — in einer gebündelten Fix-Welle behoben (Commit `9de1d4e`), Scoped Re-Review bestätigte beide ADDRESSED ohne neue Breakage. Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft: "No actionable regressions or correctness defects were identified in the diff." — deckt sich mit dem Opus-Verdict. Gemerged über PR #45 (Merge-Commit `cb6edcb`), Worktree/Branch entfernt.

Bundle (echter Build im Hauptcheckout mit `.env`, Hash `index-DIU4O_-Q.js`): Entry-Chunk 1.000,21 kB (272,72 kB gzip) — deutlich größer als der Zwischenstand im `.env`-losen Worktree (235,74 kB), weil das komplette Barcode-Scanner-Subsystem hier mitgebaut wird (siehe Deploy-Regel oben), keine Regression. CSS `index-Dav0h-KN.css` 18,84 kB.

**Deployed** (07.09.2026) und Hash-Sanity-Check bestanden (`curl https://vitaloop.web.app/` liefert denselben `index-*.js`-Hash wie `dist/assets/`). **Re-Verifikation gegen die Live-App abgeschlossen:** `impeccable detect` per Browser-Overlay auf `/training`, `/nutrition`, `/body`, `/training/analyse`, `/profile` — die 6 vorher gemeldeten `low-contrast`-Rohfunde sind auf 0 zurückgegangen (nur der bekannte `line-length`-Fund auf `/profile` bleibt, Scope von Plan 2). Manuelle Browser-Verifikation: aktiver Chip zeigt `color: rgb(13,14,18)` auf `background: rgb(135,102,237)` (exakt `#0d0e12` auf `#8766ed`, per `getComputedStyle` bestätigt), inaktiver Chip- und Bottom-Nav-Text `rgb(144,145,154)` (exakt `#90919a`, vorher kaum sichtbar, jetzt visuell klar lesbar), Hover auf „Heute eintragen" zeigt `filter: brightness(1.1)`, Tab-Fokus zeigt einen sichtbaren Ring mit Abstand zum Button (echter Tastatur-Tab, nicht nur `.focus()`), reduced-motion-Gating im gebauten CSS bestätigt (`@media (prefers-reduced-motion:no-preference)` umschließt Skalierung und Toast-Animation, `@media (prefers-reduced-motion:reduce)` entfernt nur die Transition). Konsole ohne Fehler/Warnungen. Ein Fehler-Toast live auszulösen war ohne Netzwerk-Manipulation nicht praktikabel — hier verlässt sich die Verifikation auf die bereits grünen Unit-Tests (Task 5) plus die bestätigte CSS-Auslieferung. `docs/domaenenmodell.md` geprüft, keine Änderung nötig (letzte Migration bleibt `0007`).

**Phase-6-Nachschärfung Plan 2 (Profil nachziehen) — gemerged, deployed:** Spec: `docs/superpowers/specs/2026-09-06-phase6-nachschaerfung-design.md`, Plan: `docs/superpowers/plans/2026-09-06-phase6-nachschaerfung-plan2-profil.md`. `ProfilePage.tsx` und `CalorieGoalEditor.tsx` — bei allen vier Phase-6-Bereichsplänen bewusst ausgespart (siehe Plan 2b) — auf `cardClass`/`buttonPrimaryClass`/`buttonSecondaryClass` gehoben, nach dem `BodyEntryForm`-Vorbild. Dazu der Detector-Fund „zu lange Zeile" (~122 Zeichen) im Mahlzeiten-Hilfetext auf `/profile` behoben (`max-w-prose` auf dem Container, nicht der Text selbst ist zu lang). `CalorieGoalEditor`s beide Modus-Umschalter-Buttons und `ProfilePage`s Logout-Button bekommen `buttonSecondaryClass` (keiner ist eine Haupt-Aktion; kein „destruktiver" Stil existiert im Design-System, Logout ist nicht irreversibel). 2 Umsetzungs-Tasks, umgesetzt per `superpowers:subagent-driven-development` in isoliertem Worktree/Branch `worktree-phase6-nachschaerfung-plan2-profil`, beide Reviews clean, keine Fix-Runde:
- Task 1 (`ProfilePage.tsx`): Commit `cae0dbd`.
- Task 2 (`CalorieGoalEditor.tsx`): Commit `9271765`.
- Task 3 (Abschluss): volle Suite/Lint/Typecheck/Build lokal grün (siehe unten), `docs/domaenenmodell.md` geprüft (keine Änderung — reine Frontend-Klassenänderung, letzte Migration bleibt `0007`), Status hier nachgezogen.

**Stand nach Task 3: 757/757 Tests grün** (106 Dateien — ein erster Lauf zeigte 8 Timeouts in den React-`lazy`-Chart-Listen, bekannter Kaltstart-Flake dieses Rechners, siehe „Fünf Dinge, die beim Weiterbauen gelten" in Phase 5; ein sauberer Re-Run direkt danach war grün), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-DtH_WmzS.js` 235,74 kB (75,68 kB gzip), CSS `dist/assets/index-D-x15hfq.css` 18,87 kB — kein echter Vergleichswert zur `master`-Baseline (Worktree ohne `.env`, wie bei allen vorherigen Phase-6-Plänen).

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? With fixes" — kein Critical, 2 Important, 4 Minor. Beide Important-Funde waren echte Lücken, keine Übertreibung: (1) die vom Spec-Dokument geforderte manuelle Browser-Verifikation war in Task 3 versehentlich auf reine Build/Test/Doku-Schritte verkürzt worden und nie gelaufen — genau die Art Lücke, die in Plan 1 (unzentrierter Dialog) und Plan 2d (Karten ohne Abstand) schon einmal reale Bugs versteckt hat, die kein Test sieht; (2) `max-w-prose` auf dem Mahlzeiten-Hilfetext hätte auf dem zentrierten `/profile`-Layout (`#root` hat `text-align:center`) ohne `mx-auto` sichtbar linksbündig gewirkt statt den Text zu zentrieren — der Fix hätte sein eigenes Problem erzeugt. Behoben (Commit folgt): `mx-auto` ergänzt, dazu im selben Rutsch zwei Minor-Funde mitgenommen (`ProfilePage.tsx`s „Erneut versuchen"-Button bekommt `buttonSecondaryClass` — war im Plan-Text schlicht vergessen; eine tautologische Test-Assertion in `CalorieGoalEditor.test.tsx` durch eine echte `rounded-2xl`-Prüfung ersetzt, plus eine neue `rounded-2xl`-Assertion für den Retry-Button). Danach echte manuelle Browser-Verifikation nachgeholt — im Worktree ohne `.env` per Wegwerf-Testseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx` + temporärer Export von `LoadedProfileForm`, dazu eine lokale Dummy-`.env` nur für den Dev-Server-Start, keine echten Zugangsdaten — alles danach vollständig entfernt, nicht committet), `npm run dev` + `playwright-cli`: Karte rendert als eine abgerundete Fläche ohne Rahmen, „Speichern" voller Hauptbutton, „Logout" sekundär mit sichtbarer Pille, Hilfetext-Box jetzt exakt zentriert (`getBoundingClientRect` bestätigt Mittelpunkt bei 639,99px von 640px Viewport-Mitte — die `mx-auto`-Korrektur wirkt), Konsole ohne Fehler/Warnungen. Der vom Reviewer als Minor geparkte Befund (beide `CalorieGoalEditor`-Toggle-Buttons sind farblich nicht von der Karte zu unterscheiden, `getComputedStyle` bestätigt identisches `background-color`) ist eine bereits in Plan 2b geparkte, vorbestehende Design-System-Eigenschaft, keine Regression dieses Plans — bewusst nicht in diesem Plan behoben. **Scoped Re-Review bestätigte alle vier Findings ADDRESSED, keine neue Breakage** — unabhängig verifiziert, dass die Wegwerf-Testseite wirklich vollständig entfernt wurde (kein `TestPlayPage`/`test-play` mehr im Repo, `LoadedProfileForm` wieder unexportiert, `git status` sauber), `mx-auto` mechanisch korrekt gegen das `text-align:center`-Erbe von `#root` begründet, Parität der beiden `CalorieGoalEditor`-Tests bestätigt (beide jetzt `rounded-2xl` **und** `not.toHaveClass('bg-accent')`), selbst 757/757 Tests/Lint/Typecheck/Build grün gefahren (Bundle unverändert, wie erwartet bei reinen Utility-Klassen). **Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** (siehe „Codex/GPT als Zweitmeinung" oben) — `codex review -c model="gpt-5.6-sol" --base origin/master` auf dem finalen, bereits gefixten Stand: „The changes consistently apply the shared card and button styles without altering existing profile or calorie-goal behavior. The accompanying tests cover the affected rendering paths, and no actionable regression was identified." Deckt sich mit dem Opus-Verdict.

**PR #46 gegen `master` gemergt** (Merge-Commit `04fbc0a`, alle vier CI-Checks grün: build-test, npm-audit, semgrep, zap-baseline — vom Nutzer selbst gemergt), Worktree und Branch entfernt. **Live deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop` im Hauptcheckout, Asset-Hash `index-CuJ5iG5j.js` auf `https://vitaloop.web.app` verifiziert — der erste `curl` traf noch eine gecachte Antwort mit dem alten Hash, ein zweiter mit Cache-Busting bestätigte den neuen). Bundle mit echter `.env`: Entry-Chunk 1.000,38 kB (272,75 kB gzip), praktisch identisch zur Plan-1-Baseline (1.000,21 kB), erwartungsgemäß bei einer reinen Utility-Klassen-Änderung.

**Live-Re-Verifikation nachgeholt (07.09.2026, nach Plan 3 gemerged):** Die Chrome-Erweiterung war zu Beginn dieser Session nicht verbunden (`npx impeccable detect https://vitaloop.web.app/profile` lief deshalb nur gegen die unauthentifizierte `/login`-Weiterleitung, technisch durch aber nicht aussagekräftig). Später in derselben Session hat sich die Erweiterung verbunden — mit dem bereits eingeloggten echten Browser des Nutzers. Damit direkt gegen die echte Produktions-App mit echten Daten verifiziert: **`/profile`** zeigt die Karte korrekt, der Mahlzeiten-Hilfetext ist zentriert (`getBoundingClientRect` bestätigt Mittelpunkt bei 952,5px von 960px Viewport-Mitte — die `mx-auto`-Korrektur aus Plan 2 wirkt live). **`/training/exercises`** zeigt die Muskelgruppen-Chip-Reihe mit allen 17 in der echten 873-Übungen-Bibliothek vorkommenden Gruppen (`abdominals` bis `triceps`), ein Klick auf „chest" filtert korrekt auf die tatsächlichen Brust-Übungen (Alternating Floor Press, Around The Worlds, Barbell Bench Press – Medium Grip, Barbell Guillotine Bench Press, …), „chest" wird aktiv (lila), „Alle" inaktiv. **`/nutrition/entries`** zeigt „Frühstück" mit vollem lila Hauptbutton, „Mittagessen"/„Abendessen"/„Snacks" mit kleineren sekundären Buttons — visuell klar unterschieden, exakt wie geplant. Konsole auf allen drei Seiten ohne Fehler. Damit sind sowohl Plan 2s als auch Plan 3s Live-Verifikations-Lücken jetzt geschlossen, nicht nur die vor dem Merge gelaufene Komponenten-/Fixture-Verifikation.

**Phase-6-Nachschärfung Plan 3 (Wall-of-Options) — gemerged, deployed, live verifiziert (PR #47, Merge-Commit `830d408`):** Spec: `docs/superpowers/specs/2026-09-06-phase6-nachschaerfung-design.md`, Plan: `docs/superpowers/plans/2026-09-07-phase6-nachschaerfung-plan3-walloptions.md`. Zwei Befunde behoben: `ExercisesPage.tsx` (`/training/exercises`) bekommt eine Muskelgruppen-`Chip`-Filterreihe über der Namenssuche — Daten kommen aus dem bereits geladenen `exercises.muskelgruppen_primaer` (keine neue Abfrage), die verfügbaren Gruppen werden dynamisch aus den geladenen Übungen dedupliziert und alphabetisch (de-Locale) sortiert, kein hartkodiertes Enum. `NutritionEntriesPage.tsx` (`/nutrition/entries`): von den vier gleichrangigen „+ Hinzufügen"-Buttons bekommt nur der erste sichtbare Abschnitt (`index === 0` in der `sections.map`, durch `visibleSections`' feste Slot-Reihenfolge immer Frühstück bei einem Standardprofil) `buttonPrimaryClass`, alle weiteren `buttonSecondaryClass` — die unzugeordnete Gruppe hat ohnehin nie einen Button. 2 Umsetzungs-Tasks, umgesetzt per `superpowers:subagent-driven-development` in isoliertem Worktree/Branch `worktree-phase6-nachschaerfung-plan3-walloptions`, beide Reviews clean, keine Fix-Runde:
- Task 1 (`ExercisesPage.tsx`, Chip-Filter): Commit `e05d98b`. **Zwischenfall (behoben, kein Datenverlust):** derselbe Fehler wie bei Plan 2a — der Implementer-Subagent arbeitete versehentlich im Haupt-Checkout statt im Worktree und committete direkt auf `master` (`77aee3f`). Bemerkt, bevor irgendetwas gepusht wurde (`git rev-list origin/master..master` zeigte genau den einen Fremd-Commit). Behoben: Commit per `git cherry-pick` in den Worktree-Branch übernommen, `master` per `git reset --hard origin/master` zurückgesetzt, der ebenfalls im Haupt-Checkout gelandete Report in den Worktree verschoben. Seit diesem Fund bekommt jeder folgende Implementer-Dispatch eine explizite Verzeichnis-Warnung samt Selbst-Check (`git branch --show-current`) vor jedem Commit — bei Task 2 bestätigt wirksam.
- Task 2 (`NutritionEntriesPage.tsx`, Button-Rangfolge): Commit `c8fe0a5`, Review clean — der Reviewer hat die `isPrimaryAdd`-Prop-Schnittstelle (Name/Typ/Index-Quelle) explizit auf beiden Seiten gegengeprüft und die `index === 0`-Sicherheit gegen `visibleSections`' Slot-Reihenfolge unabhängig verifiziert.
- Task 3 (Abschluss): volle Suite/Lint/Typecheck/Build lokal grün (siehe unten), `docs/domaenenmodell.md` geprüft (keine Änderung — reine Frontend-Änderung, letzte Migration bleibt `0007`), manuelle Browser-Verifikation (siehe unten), Status hier nachgezogen.

**Stand nach Task 3: 762/762 Tests grün** (106 Dateien, +5 gegenüber Plan 2s Stand — vier neue Filtertests, ein neuer Button-Hierarchie-Test — kein Kaltstart-Flake diesmal), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-DtH_WmzS.js` 235,74 kB (75,68 kB gzip) — **byte-identisch zu Plan 2s Eintrag, aber das ist kein Beleg für irgendetwas:** der Whole-Branch-Review fand, dass diese Zahl bisher fälschlich als (schwacher) Hinweis auf einen erfolgreichen Build gelesen wurde. Tatsächlich verwirft der `.env`-lose Worktree-Build praktisch die **gesamte App**. `src/lib/supabase.ts` wirft beim Modul-Laden sofort, wenn `VITE_SUPABASE_URL` fehlt; Vite ersetzt `import.meta.env.VITE_SUPABASE_URL` beim Build statisch mit `undefined`, und der Bundler eliminiert daraufhin den kompletten Importbaum, der von diesem garantierten `throw` abhängt — also `App.tsx`, jede Seite, jedes Routing. Gegengeprüft nach dem Review-Fund: weder „Übungen" noch „Passwort" noch „Einträge heute" noch `createClient` kommen in irgendeinem gebauten Chunk vor; nur der wörtliche String `VITE_SUPABASE` (aus der Fehlermeldung selbst) taucht auf. Die vorherige Doku-Zeile hier hatte behauptet, ein `grep` auf „Alle"/`flatMap` in den Bytes hätte bestätigt, dass der neue Code mitgebaut wurde — das war ein eigener Fehler beim Verifizieren (zwei benachbarte, aber nicht zusammenhängende Prüfungen fälschlich als eine gelesen), jetzt korrigiert. **Diese Zahl ist damit nicht nur „kein Vergleichswert zur `master`-Baseline", sondern schlicht bedeutungslos** — sie hat mit den beiden Task-Änderungen dieses Plans nichts zu tun. Dieselbe Einschränkung gilt rückwirkend für jede frühere „Bundle nach Worktree ohne `.env`"-Zahl in diesem Dokument (Plan 1, 2a–2d, Plan 2/Profil) — dort nicht rückwirkend korrigiert (kein Funktionsrisiko, die echten Deploys liefen immer mit echter `.env` im Hauptcheckout), aber ab hier gilt: **die einzige aussagekräftige Bundle-Messung ist die im Hauptcheckout nach dem Merge, mit echter `.env`.**

**Manuelle Browser-Verifikation (beide Punkte grün, keine Funde):** da beide geänderten Seiten hinter `ProtectedRoute` liegen und ihre echten Hooks im `.env`-losen Worktree gegen eine nicht auflösbare Dummy-Domain liefen (`net::ERR_NAME_NOT_RESOLVED`, Ladefehler-Zustand statt echter Daten — geprüft und verworfen), lief die Verifikation über eine reine Fixture-Wegwerfseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx`, beide nicht committet, nach der Prüfung vollständig entfernt): die Muskelgruppen-Chip-Reihe zeigt „Alle" aktiv (violett) plus drei abgeleitete Gruppen-Chips mit sichtbarem Abstand (`flex flex-wrap gap-2`, keine Wiederholung der Plan-2d-Regression); ein Klick auf „chest" filtert die Liste korrekt auf die einzige Chest-Übung, „Alle" wird inaktiv; die Button-Rangfolge zeigt „Frühstück" als vollen violetten Hauptbutton, „Mittagessen"/„Abendessen"/„Snacks" als kleinere sekundäre Buttons — visuell klar unterschieden. Konsole auf der Fixture-Seite durchgängig ohne Fehler oder Warnungen.

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? With fixes" — kein Critical, 1 Important, 2 Minor. Der Important-Fund war eine echte Doku-Falschaussage, kein Code-Fehler: der vorherige Absatz hier hatte behauptet, ein `grep`/`flatMap`-Fund in den gebauten Bytes hätte bestätigt, dass der neue Task-1/2-Code mitgebaut wurde — tatsächlich verwirft der `.env`-lose Worktree-Build praktisch die **gesamte App** (statisch `undefined` ersetztes `VITE_SUPABASE_URL` führt zu einem garantierten `throw` in `supabase.ts`, der Bundler eliminiert den kompletten Importbaum dahinter; gegengeprüft per `grep` über alle Chunks: kein Seitenstring wie „Übungen"/„Passwort" vorhanden, nur der wörtliche String `VITE_SUPABASE` aus der Fehlermeldung). Behoben (Commit `41a5ad5`): richtige Erklärung hier **und** als neue geteilte Notiz unter „Ein Ding, das beim Weiterbauen gilt" oben festgehalten, damit kein künftiger Plan denselben Fehlschluss wiederholt. Dazu zwei Minor-Funde im selben Rutsch behoben: zwei tautologische `not.toHaveClass('bg-accent')`-Assertions in `NutritionEntriesPage.test.tsx` durch echte `bg-surface`-Prüfungen ersetzt; eine Chip-Aktivzustand-Assertion in `ExercisesPage.test.tsx` ergänzt (`bg-accent` auf dem gewählten Chip, `bg-surface` auf „Alle" nach dem Wechsel). **Scoped Re-Review bestätigte alle drei Findings ADDRESSED, keine neue Breakage** — unabhängig selbst nachgebaut (kein `.env` vorhanden, identischer Hash `index-DtH_WmzS.js`) und den Doku-Claim gegen die tatsächlich gebauten Bytes verifiziert, 23/23 betroffene Tests plus Lint/Typecheck/Build grün gefahren. **Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** (siehe „Codex/GPT als Zweitmeinung" oben) — „The changes implement the specified muscle-group filtering and button hierarchy without introducing an identifiable functional regression. The added tests cover the principal filtering, reset, combined-search, empty-group, and styling scenarios." Deckt sich mit dem Opus-Verdict.

**PR #47 gegen `master` gemergt** (Merge-Commit `830d408`, vom Nutzer selbst gemergt, alle vier CI-Checks vorher grün: build-test, npm-audit, semgrep, zap-baseline), Worktree und Branch entfernt. **Live deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop` im Hauptcheckout, Asset-Hash `index-DwedKRZ7.js` auf `https://vitaloop.web.app` verifiziert, Entry-Chunk 1.000,81 kB / 272,85 kB gzip mit echter `.env`). **Live-Re-Verifikation abgeschlossen** (Details im Absatz „Live-Re-Verifikation nachgeholt" oben) — Chip-Filter und Button-Rangfolge beide gegen die echte Produktions-App mit echten Daten bestätigt, Konsole ohne Fehler.

**Noch offen:** laut Spec, „Reihenfolge und Abhängigkeiten" — ein Re-Run von `/impeccable critique` gegen die Live-App, um den Score-Fortschritt über alle drei Nachschärfungs-Pläne zu dokumentieren (eigener, umfangreicherer Schritt: Dual-Agent-Review mit Heuristik-Scoring, Snapshot-Persistierung, danach Rückfrage an den Nutzer — bewusst nicht ungefragt im selben Rutsch mitgemacht). Danach ist die Phase-6-Nachschärfung inhaltlich abgeschlossen; das separate, unveränderte Home-Dashboard-Vorhaben (`docs/superpowers/plans/2026-09-06-home-dashboard-plan.md`) und Phase 7 (Härtung) sind eigene künftige Schritte.

Offene Folgevorhaben (noch nicht umgesetzt):
1. **Portionen statt reiner Gramm-Eingabe.**
2. **Trainingstag/Restday-Kalender** (Integration ins Home-Dashboard).
3. **Kalorienberechnung je Übung mit eigener Dauer** statt eines MET-Durchschnitts über die ganze Session.
4. **Schwierigkeitsgrad-Import** aus free-exercise-db (`level`-Feld wird beim Import derzeit verworfen).

## Home-Dashboard (P0/1 aus dem Critique-Re-Run — gemerged, deployed, live verifiziert, PR #48, Merge-Commit `086ad96`)

**Ziel:** `HomePage.tsx` bekommt echten Inhalt statt des Produktions-Platzhalters „Platzhalter – Inhalt folgt in Phase 2/3." — eine feste Statusübersicht (Kalorien heute, aktiver Trainingstag, Gewichtsänderung) plus drei neue, optionale Graphen (H1 Aktivitätsraster, H2 Wochen-Kurzform, H3 Trends), gebaut mit denselben Phase-6-Design-Bausteinen wie Training/Ernährung/Körper. Spec: `docs/superpowers/specs/2026-09-06-home-dashboard-design.md`, Plan: `docs/superpowers/plans/2026-09-06-home-dashboard-plan.md` (bereits am 06.09.2026 geschrieben und freigegeben, bewusst zurückgestellt bis nach der Phase-6-Nachschärfung — jetzt als P0-Fix aus dem Critique-Re-Run umgesetzt, unverändert wie ursprünglich geplant).

**Keine neue Migration.** `day_status`/`health_sync_data` bleiben ungenutzte Tabellen — Trainingstag/Restday wird live aus `workout_sessions.beendet_am` abgeleitet, nicht aus einer vorab gepflegten Kalendertabelle.

8 Tasks, umgesetzt per `superpowers:subagent-driven-development` in isoliertem Worktree/Branch `worktree-home-dashboard`:
- Task 1 (`home-charts.ts`/`use-home-analysis.ts`, Grundfunktionen): Commit `405aa54`, Review clean — beide Risiko-Punkte (All-Time-Raster-Start mit/ohne Sessions, Gewichtsänderungs-Regel „letzter minus erster Messwert der Woche") explizit gegen Tests verifiziert.
- Task 2 (H1 Aktivitätsraster): Commit `856f3d2`, Review clean — der Implementer fand einen echten Fehler im Plan-Text selbst (die vorgegebene Test-Assertion widersprach Task 1s eigener Logik) und korrigierte ihn eigenständig, vom Reviewer als richtige Entscheidung bestätigt.
- Task 3 (H2 Wochen-Kurzform): Commit `0da203f`, Review clean.
- Task 4 (H3 Trends/Sparklines): Commit `6029a33`, Review clean — einziger Home-Graph mit Recharts, Farben literal aus `chart-colors.ts` (Gewicht Blau `#4f6ca5`, Kalorien Mint `#6efde6`), beide Datenquellen (`gewichtsTrend`/`kalorienJeTag`) korrekt zugeordnet verifiziert.
- Task 5 (`HomeChartList`): Commit `e26ad08`, Review clean — Prop-Verkabelung je Graph-Typ (H1/H2/H3 haben unterschiedliche Props) explizit gegen die echten Komponenten-Signaturen verifiziert, nicht nur gegen die Plan-Behauptung.
- Task 6 (`HomePage.tsx`, Statuskarten + Dashboard — der eigentliche P0-Fix): Commits `ddc50de`→`4af8c0b`, **1 Fix-Runde** (Review auf Opus, extra Sorgfalt weil App-Startseite). Verdict „Needs fixes" — kein Critical, 4 Important: (1) drei Karten stapelten sich ohne Abstand — dieselbe Fehlerklasse wie der bereits einmal gefundene Plan-2d-Bug; (2) die „kein Häkchen → keine Abfrage"-Testassertion war tautologisch (React.lazy+Suspense macht den Chart-Titel synchron unsichtbar, unabhängig vom Gating); (3) ein aktiver Plan ohne Tage rendert eine leere Trainingskarte (Leerzustand fehlte); (4) ein Nutzer mit genau einer Gewichtsmessung bekam „Keine Messwerte." statt des tatsächlichen Werts (`changeSince` braucht zwei Messungen, `latestValue` nur eine). Alle vier behoben (Commit `4af8c0b`) — echte `use-home-analysis`-Mock-Testabdeckung ergänzt (Implementer verifizierte selbst, dass der Test bei entferntem Gating tatsächlich fehlschlägt), Leerzustand für „Plan ohne Tage" ergänzt, `latestValue` vor `changeSince` geprüft. **Scoped Re-Review bestätigte alle vier ADDRESSED**, keine neue Breakage, 791/791 Tests grün.
- Task 7 (`HomeAnalysisPage` + Route `/home/analyse`): Commit `915c56a`, Review clean — schließt den von Task 6 bewusst offen gelassenen Dead-Link; byte-treue Anwendung des `TrainingAnalysisPage`/`NutritionAnalysisPage`/`BodyAnalysisPage`-Musters, Chart-Gating korrekt NICHT repliziert (Analyse-Seite zeigt immer alle drei Graphen, nur das Dashboard filtert nach Auswahl).
- Task 8 (Abschluss): volle Suite/Lint/Typecheck/Build lokal grün, `docs/domaenenmodell.md` um einen „Home-Bereich"-Absatz ergänzt (keine neue Migration, `useHomeAnalysis` komponiert nur), manuelle Browser-Verifikation (siehe unten), Status hier nachgezogen.

**Stand nach Task 8: 793/793 Tests grün** (114 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-Bpk4Wuv0.js` 236,50 kB (75,83 kB gzip) — **kein aussagekräftiger Vergleichswert** (Worktree ohne `.env`, siehe „Ein Ding, das beim Weiterbauen gilt" in Phase 6 — der `.env`-lose Build enthält praktisch keine Seite). Neue lazy Chunks: `HomeAnalysisPage` 0,95 kB, `ActivityGridChart` 0,69 kB, `WeeklySummaryList` 0,66 kB, `HomeSparklines` 0,86 kB, dazu ein `home-charts`-Sammel-Chunk mit 1,21 kB für die reinen Rechenfunktionen — durchgängig winzig, wie bei jedem anderen Graphen seit Phase 5. Keine Migration, `docs/domaenenmodell.md` um den Home-Bereich-Absatz ergänzt (letzte Migration bleibt `0007`).

**Manuelle Browser-Verifikation (Fixture-Ansatz, alle geprüften Punkte grün):** da `HomePage.tsx` vier echte Hooks komponiert und der Worktree kein `.env` hat, lief die Prüfung über eine Wegwerf-Testseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx`, beide nicht committet, nach der Prüfung vollständig entfernt), die die echten Karten-JSX-Blöcke aus `HomePage.tsx` mit Fixture-Daten nachbaut plus `HomeChartList` mit echten H1/H2/H3-Komponenten und Fixture-Sessions/-Einträgen/-Messwerten rendert (`npm run dev` + Chrome-Erweiterung, da in dieser Session verbunden). Bestätigt: die drei Statuskarten stapeln sich jetzt mit sichtbarem Abstand (`space-y-4`-Fix wirkt); Training-Karte zeigt „Push/Pull/Legs — Push"; Gewicht-Karte zeigt „82,5 kg (−0,5 kg seit dem letzten Eintrag)" (der Task-6-Fix für den Ein-Messwert-Fall wirkt); H1 (Aktivitätsraster) zeigt per `getComputedStyle` bestätigt 91 Zellen, davon 2 korrekt als Trainingstag erkannt (`background-color: rgb(135,102,237)` vs. `rgb(35,36,43)` für Restdays); H2 (Wochen-Kurzform) zeigt „2026-KW34 0 Trainingseinheiten · −0,5 kg" und „2026-KW35 2 Trainingseinheiten · Ø 300 kcal" (rechnerisch nachvollzogen: 200 kcal/100g × 150 g = 300 kcal); H3 (Trends) zeichnet die Gewichtslinie in Blau (`stroke: #4f6ca5`, per `.recharts-line-curve` bestätigt) und den Kalorien-Punkt in Mint (`#6efde6` — nur ein Datenpunkt in der Fixture, deshalb Punkt statt Linie, korrektes Recharts-Verhalten); Konsole beim Laden durchgängig ohne Fehler oder Warnungen. **Nicht geprüft in diesem Durchlauf** (aus Zeitgründen, aber bereits durch die Task-Reviews hart abgedeckt): das „kein Häkchen → keine Abfrage"-Gating selbst (Task 6s Scoped Re-Review hat eigens verifiziert, dass der neue Mock-Test bei entferntem Gating tatsächlich fehlschlägt) und `/home/analyse` mit `ZeitraumSwitch` (Task 7s Review hat die Routen-/Ladeverkabelung direkt gegen `App.tsx` und die drei Schwesterseiten verifiziert). **Die eigentliche Live-Verifikation nach Merge + Deploy steht noch aus** — anders als bei den letzten beiden Plänen kann sie diesmal direkt über die in dieser Session verbundene, bereits eingeloggte echte Chrome-Session laufen, sobald gemergt und deployed ist.

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? With fixes" — kein Critical, 2 Important, mehrere Minor. Beide Important-Funde echte, unabhängig verifizierte Defekte auf genau der Seite, die dieser Plan reparieren soll: (1) die Kalorien-Karte (`DailySummary`) verlinkte — anders als Training und Gewicht — in keinen Bereich, obwohl die Spec ausdrücklich verlangt, dass alle drei Karten in ihren Bereich verlinken; (2) H1s Grid-Zellen (`<li className="block border-b-0">`) erben `padding: 8px 0` aus der globalen `li`-Übergangsregel in `index.css` — `block`/`border-b-0` neutralisieren nur `display`/`border-bottom`, nicht `padding` — dieselbe index.css-Durchschlag-Fehlerklasse wie schon zweimal zuvor in diesem Projekt (Plan 1s Cascade-Layer-Bug, Plan 2d/Task 6s Karten-Abstand). Behoben (Commit folgt): `<Link to="/nutrition">` neben `DailySummary` ergänzt (nicht in der geteilten Komponente selbst, die auch unverändert auf `NutritionPage` läuft, wo ein Link auf sich selbst sinnlos wäre); `p-0` auf H1s `<li>` ergänzt. Im selben Rutsch vier Minor-Funde mitgenommen: `WeeklySummaryList`s Gewichtsänderung nutzte `${Math.abs(wert)}` statt `toLocaleString('de-DE')` — produzierte einen englischen Dezimalpunkt statt Komma (**hier korrigiert:** die Zeile „−0,5 kg" im Absatz oben stammt aus dieser Session-eigenen Verifikation, transkribiert aber fälschlich mit Komma, obwohl der Code zu dem Zeitpunkt noch einen Punkt produziert hätte — derselbe Fund, den der Reviewer unabhängig fand, jetzt behoben und die Doku stimmt wieder mit dem tatsächlichen Rendering überein); „1 Trainingseinheiten" statt „1 Trainingseinheit" bei genau einer Einheit; ein Testname widersprach seiner eigenen Assertion (`ActivityGridChart.test.tsx`, aus Task 2 bereits als Minor bekannt, jetzt umbenannt); H1s Testsuite prüfte nur das `data-status`-Attribut, nie die tatsächliche `bg-accent`/`bg-surface`-Klasse — beide Farbklassen jetzt zusätzlich mit `toHaveClass` gesperrt. Vier weitere Minor-Funde bewusst geparkt (Ladegate blockt alle drei Karten statt nur eine bei Fehlerfall — der Kommentar beschreibt nur den Fehler-, nicht den Lade-Pfad; uneinheitliche Fehleroberfläche zwischen den drei Karten; H3s Sparklines ohne zugängliche Beschriftung; `zeitraum: 'alles'` auf `/home/analyse` kann bei einer sehr langen Historie viele DOM-Knoten in H1 erzeugen) — keiner davon merge-blockierend, alle für Phase 7 (Härtung) vorgemerkt.

**Scoped Re-Review bestätigte alle sechs Findings ADDRESSED, keine neue Breakage** — unabhängig `WeeklySummaryList.test.tsx` selbst laufen lassen, um die Komma-Ausgabe wirklich am Code zu bestätigen (nicht nur den Diff gelesen), die `bg-accent`/`bg-surface`-Assertions als echt regressionsfest verifiziert (würden bei vertauschter Ternary tatsächlich fehlschlagen), `NutritionPage.tsx`/`DailySummary.tsx` als unberührt bestätigt. 794/794 Tests, Lint, Typecheck sauber (ein erster Testlauf zeigte den bekannten Kaltstart-Flake in `HomeAnalysisPage.test.tsx`, ein sauberer Re-Run war grün).

**Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** — fand einen eigenständigen P2-Fund, den auch das gründlichere Opus-Whole-Branch-Review nicht hatte: H1s Aktivitätsraster hält die Wochentag-Spalten nicht ein — `grid-cols-7` reiht Zellen einfach in Dokumentreihenfolge ab `rangeStart`, wodurch eine Spalte je nach Start-Wochentag des gewählten Zeitraums für unterschiedliche Wochentage steht, entgegen der Design-Spec („eine Spalte je Wochentag", `docs/superpowers/specs/2026-09-06-home-dashboard-design.md:48`). **Geprüft und bewusst nicht in dieser Fix-Welle behoben:** Der Plan-Text selbst (`docs/superpowers/plans/2026-09-06-home-dashboard-plan.md:550`) sieht nur `grid grid-cols-7 gap-1` ohne jede Wochentag-Offset-Logik vor — das ist eine Spec-vs-Plan-Inkonsistenz, die vor dieser Session entstand, keine Implementer- oder Reviewer-Lücke. Bewusst geparkt: Codex selbst stuft es als P2 ein, das Opus-Review hat es trotz Lesens derselben Datei nicht eigenständig gefunden, es ist rein kosmetisch (Trainingstag/Restday wird pro Tag weiterhin korrekt angezeigt, nur nicht kalendarisch spaltenrein), und ein korrekter Fix bräuchte eine kleine Design-Entscheidung (führende Platzhalter-Zellen, deren Styling/Testabdeckung) — das wäre eine Erweiterung über „behebe was ein Reviewer schon konkret benannt hat" hinaus. Für eine künftige Politur-Runde vorgemerkt.

**PR #48 gegen `master` gemergt** (Merge-Commit `086ad96`, alle vier CI-Checks grün: build-test, npm-audit, semgrep, zap-baseline), Worktree und Branch entfernt. **Live deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop` im Hauptcheckout, Asset-Hash `index-8zAq7yMV.js` auf `https://vitaloop.web.app` verifiziert, Entry-Chunk 1.004,41 kB / 273,57 kB gzip mit echter `.env`).

**Echte Live-Verifikation abgeschlossen** (08.09.2026, über die in dieser Session verbundene, bereits eingeloggte echte Chrome-Session — kein Fixture-Ersatz mehr nötig): `/` zeigt die echte Statusübersicht statt des Platzhalters, alle drei Karten stapeln sich mit sichtbarem Abstand, „Zum Ernährungsbereich"-Link (der Important-Fix) ist da. **Ein echter, aber transienter Fund:** beim allerersten Laden zeigte die Gewicht-Karte kurzzeitig „Gewichtsdaten konnten nicht geladen werden." gleichzeitig mit „Keine Messwerte." — Netzwerk-Log zeigt aber durchgängig `200` auf `body_metrics`; ein Reload zeigte danach durchgehend nur noch „Keine Messwerte." (das Konto hat aktuell 0 Zeilen `body_metrics`). Eingeordnet als derselbe Kaltstart-Flake-Typ, der schon einmal in Phase 5 dokumentiert wurde („ein reproduzierbarer Kaltstart-Flake … kein Code-Bug"), nicht als neuer Fehler dieses Plans — nicht reproduzierbar bei wiederholtem Laden. H1s Zellen sind jetzt per `getComputedStyle` bestätigt exakte Quadrate (`padding: 0px`, 144,7×144,7px) — der `p-0`-Fix wirkt live. `/home/analyse` zeigt `ZeitraumSwitch` (90 Tage vorbelegt) und alle drei Graphen; H1 über die Checkbox „Auf dem Dashboard zeigen" angeheftet, Erscheinen auf `/` nach vollem Reload bestätigt (Häkchen-Persistenz), danach wieder abgehakt, um den Konto-Zustand unverändert zu lassen. Konsole auf allen geprüften Seiten ohne Fehler oder Warnungen.

**Damit ist das Home-Dashboard (P0/1 aus dem Critique-Re-Run) vollständig abgeschlossen.** Nächster Schritt: P0/2 (`/login`-Redesign) — noch kein Plan geschrieben, braucht eigenes Brainstorming.

## Phase 6 – Design (abgeschlossen — alle fünf Pläne gemerged, deployed, live verifiziert)

**Ziel:** komplettes visuelles und teilweise strukturelles Redesign für Training, Ernährung, Körper und die drei Analyse-Seiten, nach dem Vorbild eines vom Nutzer bereitgestellten Referenzvideos. Home-Dashboard bleibt bewusst außen vor (eigenes künftiges Vorhaben, braucht erst ein Datenmodell für Trainingstag/Restday).

**Ablauf bisher:**
1. **Referenzvideo analysiert** (`original-3735dd65e03e83c2a88808c611a05afb.mp4`, vom Nutzer bereitgestellt): Frames per ffmpeg extrahiert (2 fps, 42 Frames), exakte Farbwerte per Pixel-Sampling (Python/Pillow) gemessen statt geschätzt. Ergebnis: `docs/superpowers/specs/2026-09-05-phase6-referenzdesign-analyse.md`.
2. **Brainstorming abgeschlossen** (architektonischer Pfad, da bereichsübergreifend): Scope (strukturelle Änderungen erlaubt, Home bleibt Platzhalter), Dark-Mode-only, exakte Farbübernahme aus dem Video, Tailwind CSS als neue Abhängigkeit, lucide-react für Icons, kein erhöhter Nav-Mittelbutton — alles vom Nutzer einzeln bestätigt.
3. **Design-Spec geschrieben und freigegeben:** `docs/superpowers/specs/2026-09-05-phase6-design-design.md`. Enthält alle Design-Tokens (exakte Hex-Werte), sechs Kernkomponenten (Karte, Buttons, Chip, Navigation, Toast, Dialog) und die Farbzuordnung für alle 19 bestehenden Graphen.
4. **Plan 1 (Fundament) geschrieben und freigegeben:** `docs/superpowers/plans/2026-09-05-phase6-plan1-fundament.md`, 6 Tasks (Tailwind-Setup, Karte/Button/Chip, Navigation, Toast, Dialog, Abschluss). Tailwind 4.3.3 und lucide-react 1.41.0 vor Plan-Erstellung testweise installiert und gegen Build/Lint/Typecheck verifiziert (danach zurückgesetzt — die eigentliche Umsetzung passiert erst über den Plan).
5. **Umsetzungsart entschieden und ausgeführt:** `superpowers:subagent-driven-development` (Nutzerentscheidung 05.09.2026), isolierter Worktree/Branch `worktree-phase6-plan1-fundament`. **Plan 1 ist damit vollständig umgesetzt, reviewt und gemerged** — alle 6 Tasks fertig, Details in Punkt 6.
6. **Whole-Branch-Review (stärkstes Modell) und eine Fix-Welle abgeschlossen, danach eine manuelle Browser-Verifikation mit einem weiteren Fund und Fix.** Gemerged über PR #40 (Merge-Commit `b891d0b`), Branch und Worktree entfernt. Details in Punkt 7/8.

**Plan 1 — alle 6 Tasks, je einzeln reviewt:**
- Task 1 (Tailwind-Setup, Design-Tokens): Commit `737a5db`, Review clean, keine Fix-Runde.
- Task 2 (Karten-/Button-Klassen, Chip): Commits `f97fa26`→`c2b85e8`, 1 Fix-Runde — `ChipProps` fehlte im Export, dazu `border-0 m-0` ergänzt gegen Rahmen- und Abstands-Durchschlag aus dem noch nicht migrierten Alt-CSS.
- Task 3 (Navigation): Commits `ed18f90`→`bccabbc`, 1 Fix-Runde — ein `role="list"` auf dem `<nav>`, das schon der Plan-Text selbst versehentlich vorsah, unterdrückte die native Navigations-Landmarke; entfernt.
- Task 4 (Toast): Commits `d1d1c6f`→`871f601`, 1 Fix-Runde — echte Race Condition: ein hängender `setTimeout` eines alten Toasts konnte einen neueren vorzeitig schließen; jetzt Timer per Ref verfolgt und beim Unmount aufgeräumt.
- Task 5 (Dialog): Commit `5d5de6d`, Review clean, keine Fix-Runde — aber siehe Punkt 8, die manuelle Browser-Verifikation fand hier einen echten Bug, den weder Test noch Review sehen konnten.
- Task 6 (Abschluss): Commit `dde70fb` — Bundle gemessen, `docs/domaenenmodell.md` geprüft (keine Änderung nötig, dieser Plan fasst kein DB-Schema an, letzte Migration bleibt `0007`), Status nachgezogen.

**Damit stehen jetzt sechs Design-Bausteine:** `cardClass`/`buttonPrimaryClass`/`buttonSecondaryClass` und `Chip` (`src/lib/ui-classes.ts`, `src/components/Chip.tsx`), die Navigation als schwebende Icon-Pille (`src/components/BottomNav.tsx`, lucide-react-Icons, aktiv/inaktiv über `text-accent`/`text-text-muted` — **bereits sichtbar umgestellt**, weil `BottomNav` app-weit eingebunden ist), `ToastProvider`/`useToast` (`src/components/ToastProvider.tsx`, in `AppLayout.tsx` verdrahtet) und `Dialog` (`src/components/Dialog.tsx`, um das native `<dialog>`-Element gebaut, jsdom-Polyfill in `src/test-setup.ts`). Karte/Button/Chip/Toast/Dialog existieren im Code, werden aber noch **nirgends** in den Seiten selbst verwendet — das ist Aufgabe der Bereichs-Pläne.

**7. Whole-Branch-Review (stärkstes Modell) fand einen Critical-Fund, der alle sechs Tasks betraf und keinem der einzelnen Task-Reviews aufgefallen war:** Tailwind v4 packt seine eigenen Utilities in `@layer utilities`; die Übergangsregeln aus Task 1 in `index.css` standen außerhalb jedes Layers und schlugen dadurch — unabhängig von Spezifität — jede neue Tailwind-Klasse. Konkret unwirksam bis zum Fix: der `border-0`/`m-0`-Fix aus Task 2, `Chip`s aktiv/inaktiv-Unterscheidung, die Rundung/Zentrierung von `Dialog`s Schließen-Button, und **alle vier `BottomNav`-Icons rendern identisch akzentfarben statt aktiv/inaktiv unterschieden** — ein bereits scharf geschalteter Regressionsfehler, kein latentes Problem, weil `BottomNav` app-weit eingebunden ist. Dazu zwei Important-Funde: `tailwindcss`/`@tailwindcss/vite` standen in `dependencies` statt `devDependencies`; eine Doku-Lücke. Alle drei in einer Fix-Welle behoben (Commit `94194cc` — Übergangsregeln in `@layer base` verschoben, Pakete korrekt einsortiert, Doku ergänzt), Scoped Re-Review bestätigte alle drei ADDRESSED ohne neue Breakage. **Kein Test in diesem Plan hätte das je gefunden** — jeder Test prüft nur, ob ein Klassenname im String steht, nie das tatsächliche Rendering; der Reviewer verifizierte den Fund direkt an den Bytes der gebauten `dist/*.css`.

**8. Manuelle Browser-Verifikation danach (weil genau das der obige Fund lehrt: Klassenname-Tests sind blind für Optik) fand einen weiteren, unabhängigen Bug:** `Dialog` zentrierte sich nicht — Tailwinds eigenes Preflight setzt `margin: 0` auf alle Elemente und entfernt damit die native `margin: auto`-Zentrierung des Browser-UA-Stylesheets für `<dialog>`. Bestätigt per `getComputedStyle` (Dialog lag bei `{x:0, y:0}`, oben links angepinnt) auf einer Wegwerf-Testseite (nicht committet). Behoben mit `m-auto` (Commit `725e350`), Re-Review clean, **danach im echten Chrome-Tab visuell bestätigt: Dialog zentriert sich jetzt korrekt.** Bestand vor Task 5s Review unentdeckt, weil jsdom kein echtes Layout rendert.

**Merke für die Bereichs-Pläne:** Ein Klassenname im Test zu sehen beweist nicht, dass er auch wirkt — weder gegen Cascade-Layer-Konflikte noch gegen browsereigene Zentrierung/Layout. Jeder Bereichs-Plan, der diese Bausteine erstmals auf einer echten Seite verwendet, sollte einen kurzen manuellen Browser-Check als letzten Schritt vorsehen, nicht nur die Testsuite.

**Stand nach Task 6: 724 Tests grün** (103 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-C00u4lYw.js` 235,36 kB (75,50 kB gzip), CSS `dist/assets/index-Oq0bSxdG.css` 15,46 kB (3,85 kB gzip) — **kein echter Vergleichswert zur `master`-Baseline**, aus demselben Grund wie schon in Phase 5 Plan 2b/2c dokumentiert: der Worktree hat kein `.env` (gitignored, wird von `git worktree add` nie mitkopiert), deshalb fehlt das komplette Barcode-Scanner-Subsystem (`@zxing/browser`/`@zxing/library`) im Entry-Chunk. Die Erwartung aus dem Plan bestätigt sich trotzdem qualitativ: Tailwind trägt kaum zum Bundle bei (nur die tatsächlich genutzten Utility-Klassen werden generiert), lucide-react bündelt nur die vier tatsächlich importierten Icons, kein Barrel-Import des gesamten Icon-Sets.

**Ein Ding, das beim Weiterbauen gilt:**
- **Uebergangsregeln in `index.css` muessen in `@layer base` stehen, sonst schlagen sie jede Tailwind-Utility-Klasse.** Tailwind v4 packt alle eigenen Utilities in `@layer utilities`, und ein unlayered Element-Selektor gewinnt gegen jede layered Regel unabhaengig von Spezifitaet. Genau dieser Fehler machte in Plan 1 den `border-0`/`m-0`-Fix aus Task 2 wirkungslos und liess alle vier BottomNav-Icons identisch akzentfarben rendern, bis er im Whole-Branch-Review gefunden wurde.
- **Ein Toast, der auslöst waehrend ein Dialog offen ist, ist unsichtbar:** natives `showModal()` hebt den Dialog und sein `::backdrop` in den Browser-Top-Layer, der ueber allem anderen liegt, unabhaengig von z-index — `ToastProvider`s Toast liegt im normalen Layer und wird dahinter unlesbar verblasst. Gefunden in Plan 2a: `ExercisesPage`s Speichern-Fehler blieb dadurch unsichtbar, waehrend der Dialog absichtlich offen blieb; behoben durch Rueckkehr zur Inline-Fehlermeldung im Formular selbst (sichtbar, weil sie im Dialog rendert). Merke fuer 2b/2c/2d: jeder Fehler, der auftreten kann waehrend ein Dialog offen bleibt, gehoert inline in den Dialog-Inhalt, nicht in einen Toast.
- **Der `.env`-lose Worktree-Build enthaelt nicht "die App ohne Barcode-Scanner" — er enthaelt praktisch gar keine Seite.** `src/lib/supabase.ts` wirft beim Modul-Laden sofort, wenn `VITE_SUPABASE_URL` fehlt; Vite ersetzt den Wert beim Build statisch mit `undefined`, der Bundler eliminiert daraufhin den kompletten Importbaum hinter diesem garantierten `throw` — `App.tsx`, jede Seite, jedes Routing. Bestaetigt beim Whole-Branch-Review von Phase-6-Nachschaerfung Plan 3: kein Seitenstring (`"Übungen"`, `"Passwort"`, …) kommt in irgendeinem `.env`-losen Chunk vor. Die in jedem Worktree-Plan seit Phase 6 dokumentierten Entry-Chunk-Zahlen sind deshalb nicht bloss „kein Vergleichswert zur `master`-Baseline", sondern **bedeutungslos als Beleg dafuer, dass ein bestimmter Code-Aenderung mitgebaut wurde** — sie taugen bestenfalls dazu, Build/Lint/Typecheck als grundsaetzlich lauffaehig zu bestaetigen. Die einzige aussagekraeftige Bundle-Messung bleibt der Build im Hauptcheckout mit echter `.env` nach dem Merge.

## Plan 2a – Trainingsbereich im neuen Design (gemerged, PR #41)

**Plan:** `docs/superpowers/plans/2026-09-05-phase6-plan2a-training-plan.md`, 9 Tasks. Alle sieben Trainingsseiten (Dashboard, Pläne, Plan-Editor, Übungen, Live-Modus, Historie, Historien-Detail) verwenden jetzt die in Plan 1 gebauten Bausteine statt der alten unstylisierten Listen und `<p role="alert">`-Meldungen.

**Ablauf:** Kein separates Plan-Brainstorming nötig (Design-Entscheidungen waren schon im Phase-6-Design-Spec getroffen, „Struktur je Bereich" nennt Training explizit) — direkt von `superpowers:writing-plans` in den Plan, dann `superpowers:subagent-driven-development` im Worktree/Branch `worktree-phase6-plan2a-training`. Pre-Flight-Scan clean (keine Task-Konflikte).

**Alle 8 Tasks, je einzeln reviewt:**
- Task 1 (Test-Render-Helfer, `renderWithProviders`): Commit `dde199e`, Review clean, keine Fix-Runde.
- Task 2 (Dashboard): Commit `73b1886` (nach Cherry-Pick, siehe Zwischenfall unten), Review clean, keine Fix-Runde.
- Task 3 (Pläne-Liste): Commit `819866e`, Review clean, keine Fix-Runde.
- Task 4 (Übungsliste, Dialog für „Eigene Übung anlegen"): Commit `b7a8f1f`, Review clean, keine Fix-Runde.
- Task 5 (Plan-Editor, Dialog-Übungssuche — umfangreichster Task): Commit `d10875e`, Review clean, keine Fix-Runde.
- Task 6 (Live-Modus, Chip-Reihe für RIR, Log-Screen-Muster): Commit `635a6bb`, Review clean, keine Fix-Runde.
- Task 7 (Historie): Commit `dbc2458`, Review clean, keine Fix-Runde.
- Task 8 (Historien-Detail): Commit `dad19b2`, Review clean, keine Fix-Runde.
- Task 9 (Abschluss): Bundle gemessen, `docs/domaenenmodell.md` geprüft (keine Änderung nötig), manuelle Browser-Verifikation (siehe unten), Status hier nachgezogen.

**Zwischenfall bei Task 2 (behoben, kein Datenverlust):** der Implementer-Subagent arbeitete versehentlich im Haupt-Checkout (`G:\Code Projekte\fitness-app\fitness-app`) statt im Worktree und committete dort direkt auf `master`. Bemerkt, bevor irgendetwas gepusht wurde. Behoben: Commit per `git cherry-pick` in den Worktree-Branch übernommen (`73b1886`), `master` per `git reset --hard origin/master` wieder auf den Remote-Stand zurückgesetzt, zwei doppelte Untracked-Dateien im Haupt-Checkout gelöscht. **Seither Prozessänderung:** nach jedem Implementer-Report verifiziert der Controller per `git log` im Worktree, dass der Commit auch wirklich dort gelandet ist, bevor das Review-Package erzeugt wird — bei allen folgenden sieben Tasks bestätigt.

**Design-Entscheidungen, vom Controller getroffen (Spec deckte nur die grobe Struktur ab, Datei-für-Datei-Umsetzung war bewusst dem Plan überlassen):**
- **Karten-in-Liste-Muster:** `<li className="block border-b-0">` umschließt `<div className={`${cardClass} w-full`}>` — niemals `cardClass` direkt auf `<li>`. Grund: die Übergangsregel für `li` in `index.css` (`display:flex; justify-content:center; border-bottom:...`) würde sonst durchschlagen (dieselbe Klasse Fund wie Plan 1s Cascade-Layer-Bug, hier vorab vermieden). `<section>` ist von keiner Übergangsregel betroffen, bekommt `cardClass` deshalb direkt.
- **Toast vs. inline:** kurzlebige Aktions-Fehler (Speichern/Löschen/Aktivieren/Starten/Satz-Loggen) → Toast. Formular-Validierung (leerer Pflichtwert) und seitenblockierende Ladefehler bleiben inline — exakt die Regel aus `ToastProvider.tsx`s eigenem Doc-Kommentar.
- **Dialog** für zwei neue Sheet-Abläufe: Übungssuche im Plan-Editor, „Eigene Übung anlegen". **Bewusst kein** Bestätigungs-Dialog vor „Löschen" — hätte die zweimal dokumentierte Projekt-Konvention „kein Rückfragen-Dialog vor dem Löschen" (Phase 3, Phase 4) umgekehrt; vom Nutzer bei der Plan-Freigabe bestätigt.
- **Dialog-mit-Formular-Fund während der Plan-Erstellung (nicht erst bei der Umsetzung):** `Dialog` hält seine Kinder immer gemountet (nur `showModal()`/`close()` steuern Sichtbarkeit). Ein Formular/eine Suche mit eigenem `useState` darin müsste sonst beim Wiederöffnen alte Werte zeigen — in Task 5s Test hätte das sogar einen Test tatsächlich scheitern lassen (`queryByLabelText` prüft nicht auf CSS-Sichtbarkeit). Beide betroffenen Tasks (4, 5) rendern den Formular-/Sucheninhalt deshalb bedingt auf den Öffnen-Status, das `Dialog`-Element selbst bleibt unbedingt gerendert.
- Ein Button je Seite wird `buttonPrimaryClass`, sekundäre/löschende Aktionen `buttonSecondaryClass`, kleine Zeilen-Werkzeuge (rauf/runter) bleiben unstyled.

**Manuelle Browser-Verifikation (alle 8 Punkte grün, keine Funde):** Karte/Button/Chip/Dialog/Toast auf einer Wegwerf-Testseite (nicht committet) im echten Chrome-Tab geprüft — Karten-in-Liste ohne sichtbaren Rahmen, Dialog zentriert sich korrekt (die Plan-1-Regressionsklasse tritt hier nicht erneut auf), Chip-Reihe zeigt aktiv/inaktiv klar unterschieden, Toast per `getComputedStyle` bestätigt echt sichtbar (Screenshot-Tool hatte Timing-Probleme mit dem 4-Sekunden-Auto-Dismiss, kein App-Fehler). Anders als bei Plan 1 diesmal **keine** neuen Bugs gefunden — dieser Plan setzt nur bereits gefixte Bausteine in neuen Kombinationen ein (verschachtelte Karten, Dialog in einer Tages-Karte, Chip in einer Karte).

**Stand nach Task 9: 726 Tests grün** (104 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-0FBfwa9p.js` 235,36 kB (75,50 kB gzip), CSS `dist/assets/index-B1IpXdqo.css` ~15,5 kB — kein echter Vergleichswert zur `master`-Baseline (Worktree ohne `.env`, wie bei Plan 1). Keine Migration in diesem Plan, `docs/domaenenmodell.md` unverändert.

## Plan 2b – Ernährungsbereich im neuen Design (gemerged, PR #42)

**Plan:** `docs/superpowers/plans/2026-09-05-phase6-plan2b-ernaehrung-plan.md`, Commit `0b73bf1` auf `master`, 7 Tasks. Dashboard-Tagesübersicht als Karte, die Barcode-/Produktsuche-Kette (`ProductPicker`/`BarcodeScanner`/`ManualProductForm`/`AddEntryFlow`) und der Bearbeiten-Flow (`FoodEntryList`/`FoodEntryEditForm`) verwenden jetzt die in Plan 1 gebauten Bausteine. Umgesetzt per `superpowers:subagent-driven-development`, isolierter Worktree/Branch `worktree-phase6-plan2b-ernaehrung`.

**Wichtigste Design-Entscheidung, vom Nutzer bei Fortsetzung des Plans bestätigt (direkt umsetzen, keine weitere Design-Rücksprache):** anders als im Trainingsbereich (Plan 2a) bleiben hier **die meisten Schreibfehler inline statt Toast** — `AddEntryFlow`, `ManualProductForm` und `FoodEntryEditForm` liegen alle in einem Dialog, der bei einem Fehler absichtlich offen bleibt, und ein Toast hinter einem offenen `<dialog>` ist unsichtbar (die Lehre aus Plan 2as Critical-Fund, siehe „Ein Ding, das beim Weiterbauen gilt" oben). Nur der Löschen-Fehler in `FoodEntryList` (kein Dialog beteiligt) wird ein Toast — exakt wie in Plan 2as `TrainingHistoryDetailPage`. `CalorieGoalEditor.tsx`/`ProfilePage.tsx` bleiben bewusst außerhalb (gehören zu keinem der vier Bereichs-Pläne).

**Pre-Flight-Scan vor Task 1:** Plan-Code aller sieben Datei-Vorher-Zustände sowie `Dialog.tsx`/`ToastProvider.tsx`/`test-render.tsx`/`test-setup.ts` (Interfaces, Signaturen, jsdom-Polyfill) gegen den tatsächlichen Repo-Stand geprüft — exakte Übereinstimmung, keine Abweichung, kein Cross-Task-Konflikt gefunden.

**Alle 7 Tasks, je einzeln reviewt:**
- Task 1 (`DailySummary` als Karte): Commit `dd4f9bc`, Review clean.
- Task 2 (`ManualProductForm`/`BarcodeScanner`): Commit `89f5446`, Review approved, 1 Minor zurückgestellt (uneinheitliche Einrückung nach dem Karten-Wrapper — rein kosmetisch, kein Format-on-Save im Projekt).
- Task 3 (`ProductPicker`, Barcode-Scan als Hauptaktion): Commit `a7d7881`, Review clean. **Zwischenfall:** der dispatchte Implementer traf einen Session-Rate-Limit (HTTP 429) direkt vor dem Testlauf, hatte den Code aber bereits vollständig und plangetreu geändert — Controller hat den Diff byte-genau gegen den Plan-Text verglichen, dann selbst Tests/Lint/Typecheck gefahren und committet.
- Task 4 (`AddEntryFlow`, kein eigener Dialog): Commit `fffbc4f`, Review clean.
- Task 5 (`NutritionEntriesPage`, „+ Hinzufügen" als Dialog je Mahlzeiten-Abschnitt): Commit `c61d59f`, Review clean — Dialog-mit-Formular-Muster (Kinder bedingt auf Öffnen-Status gerendert, `Dialog` selbst immer gemountet) vom Reviewer explizit als korrekt verifiziert, kein Pfad zu Stale-State.
- Task 6 (`FoodEntryList`/`FoodEntryEditForm`, Karten/Dialog/Toast): Commit `77f7e27`, Review clean. **Echte Cross-Task-Plan-Lücke gefunden und per Ruling behoben:** der Plan-Text listete `src/pages/NutritionEntriesPage.test.tsx` nicht unter Task 6s Dateien, obwohl die neu eingeführte `useToast()` in `FoodEntryList` dort 7 von 13 Tests brechen ließ (Rendern ohne `ToastProvider`). Scope um genau diese Datei erweitert, mechanisch identische Korrektur (`render` → `renderWithProviders`) angewendet, im selben Commit mitcommittet.
- Task 7 (Abschluss): Bundle gemessen, `docs/domaenenmodell.md` geprüft (keine Änderung nötig — Plan fasst kein DB-Schema an, letzte Migration bleibt `0007`), manuelle Browser-Verifikation (siehe unten), Status hier nachgezogen.

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? Yes" — kein Critical, 2 Important (`AddEntryFlow`s „Abbrechen" auf dem `ProductPicker`-Idle-Screen war im neuen Dialog-Kontext ein No-op statt den Dialog zu schließen — kein Regressionsfehler, aber die Dialog-Umstellung machte den bereits bestehenden toten Button zur Falle; `FoodEntryList.test.tsx` hatte keine Assertion, die das Karten-in-Liste-Markup sperrt, obwohl das die fragilste Eigenschaft dieses Plans ist), 3 Minor bewusst geparkt (`ManualProductForm`-Einrückung aus Task 2, ein unbeabsichtigt reichender `index.css`-Selektor ohne beobachteten Effekt, `buttonSecondaryClass`/`cardClass` teilen `bg-surface` — eine Plan-1-Eigenschaft, kein Plan-2b-Verstoß). Beide Important-Findings in einer Fix-Welle behoben (Commit `106d2f5` — `AddEntryFlow` bekommt ein optionales `onCancel`-Prop mit Default `reset`, `SectionBlock` übergibt `onCancel={() => setDialogOpen(false)}`; neuer Test sperrt das `<li>`/`<div>`-Markup), Scoped Re-Review bestätigte beide Findings ADDRESSED ohne neue Breakage.

**Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** (siehe „Codex/GPT als Zweitmeinung" oben) — `codex review -c model="gpt-5.6-sol" --base origin/master` auf dem finalen, bereits gefixten Stand: „The changes consistently apply the new design components while preserving existing behavior. The new dialog and toast integrations include appropriate provider usage and state-reset handling, and no actionable regressions were identified." Deckt sich mit dem Opus-Verdict — zwei unabhängige Modelle, gleiches Ergebnis.

**PR #42 gegen `master` gemergt** (Merge-Commit `82112fa`, alle vier CI-Checks grün: build-test, npm-audit, semgrep, zap-baseline), Worktree und Branch entfernt.

**Manuelle Browser-Verifikation (alle 8 Punkte grün, keine Funde):** über eine Wegwerf-Testseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx`, beide nicht committet, nach der Prüfung entfernt) im echten Chrome-Tab geprüft, da kein Test in diesem Plan echtes Layout rendert. `DailySummary` als abgerundete Karte ohne Rahmen; `FoodEntryList`-Einträge als Karten; Dialog öffnet zentriert mit verschwommenem Hintergrund (die Plan-1-Regressionsklasse tritt hier nicht erneut auf); „Barcode scannen" als voller lila Hauptbutton, Rest sekundär; ungültige Barcode-Nummer zeigt die Fehlermeldung lesbar innerhalb des offenen Dialogs; Dialog schließen und neu öffnen zeigt ein leeres Formular ohne Altwerte/-fehler; „Bearbeiten" öffnet ebenfalls zentriert mit Formular als Karte, „Speichern" als Hauptbutton; ein simulierter Löschfehler zeigt einen echten Toast (`position:fixed`, `top:18px`, sichtbar per `getComputedStyle` bestätigt — Screenshot-Tool hatte erneut Timing-Probleme mit dem 4-Sekunden-Auto-Dismiss, wie schon in Plan 2a, kein App-Fehler); Konsole auf der Testseite durchgängig ohne Fehler oder Warnungen.

**Stand nach Task 7 und Fix-Welle: 730 Tests grün** (104 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-CR0IR7-K.js` 235,36 kB (75,50 kB gzip), CSS `dist/assets/index-D2fk5ca_.css` 15,95 kB (3,98 kB gzip) — kein echter Vergleichswert zur `master`-Baseline (Worktree ohne `.env`, wie bei Plan 1/2a: das komplette Barcode-Scanner-Subsystem fehlt im Entry-Chunk). Keine Migration in diesem Plan, `docs/domaenenmodell.md` unverändert.

**Wiki synchronisiert** (06.09.2026, Commit `a0d307b`): nicht nur Plan 2b, sondern der komplette Phase-6-Rückstand — Design-Spec, Plan 1 (Fundament) und Plan 2a (Training) waren trotz Merge nie gespiegelt worden. Vier neue Seiten (`Phase-6-Design-Spec`, `Phase-6-Plan-1-Fundament`, `Phase-6-Plan-2a-Training`, `Phase-6-Plan-2b-Ernaehrungsbereich`), `Home`/`_Sidebar` verlinkt und der Status-Absatz ergänzt. `Domain-Model.md` unverändert (keiner der drei Pläne fasst das Schema an).

**Aufteilung wie bei Phase 5:** Plan 1 (Fundament) — **fertig und gemerged** —, Plan 2a (Training) — **fertig und gemerged** —, Plan 2b (Ernährung) — **fertig und gemerged** —, Plan 2c (Körper) — **fertig und gemerged** —, Plan 2d (Analyse-Seiten) — **fertig, siehe eigenen Abschnitt unten.** Damit ist Phase 6 (Design) inhaltlich vollständig.

## Plan 2c – Körperbereich im neuen Design (gemerged, PR #43)

**Plan:** `docs/superpowers/plans/2026-09-06-phase6-plan2c-koerper-plan.md`, Commit `27ab45a` auf `master`, 5 Tasks. Dashboard (`BodyPage`, 2-spaltige Karten-Grid für die sieben Messwerte, „Heute eintragen" als Dialog), Verlaufsliste (`BodyEntriesPage`, Karten-in-Liste, „Bearbeiten" als ein geteilter Dialog statt einem je Zeile) und Fotoseite (`BodyPhotosPage`, Formularfeld- und Foto-Karten) verwenden jetzt die in Plan 1 gebauten Bausteine. Umgesetzt per `superpowers:subagent-driven-development`, isolierter Worktree/Branch `worktree-phase6-plan2c-koerper`.

**Design-Entscheidung dieses Plans (anders als bei Ernährung/Training):** die `ProfileWeightSyncError`-Stale-Mirror-Hinweise (Speichern/Löschen hat funktioniert, nur `profiles.aktuelles_gewicht` konnte nicht nachgezogen werden) werden hier ein **Toast statt inline**, obwohl ein Dialog beteiligt ist — weil die aufrufende Seite diesen Fehlertyp als Erfolg behandelt und den Dialog dabei bereits schließt, bevor der Hinweis erscheint. Nur der **echte** Schreibfehler (Dialog bleibt offen) bleibt inline in `BodyEntryForm`, unverändert seit vor diesem Plan. Ausführliche Rationale im Plan-Dokument, Abschnitt „Warum die Stale-Mirror-Hinweise hier Toast werden, nicht inline".

**Design-Entscheidung: ein geteilter Bearbeiten-Dialog statt einem je Zeile** in `BodyEntriesPage` — `editingId` erlaubt ohnehin nur eine offene Bearbeitung gleichzeitig, ein natives `<dialog>` je Zeile wäre unnötige Vervielfachung. Vom Reviewer explizit als stale-state-sicher verifiziert (`editingId` als Singleton garantiert höchstens einen `editingEntry`).

**Alle 5 Tasks, je einzeln reviewt:**
- Task 1 (`BodyEntryForm`, Karten-Wrapper und Buttons): Commit `741cb3a`, Review clean.
- Task 2 (`BodyPage`-Dashboard, 2-spaltige Karten-Grid, „Heute eintragen" als Dialog): Commit `f5fe0ce`, Review clean. **Echte Plan-Lücke gefunden und behoben:** der geplante Reset-bei-Wiedereröffnen-Test nutzte `today()` ohne Fake-Timer-Pinning, während die Test-Fixture ein festes Datum (`'2026-08-24'`) trägt — mit dem echten Systemdatum (06.09.2026) wäre der Test unabhängig vom eigentlichen Dialog-Fix fehlgeschlagen. Implementer hat `vi.useFakeTimers()`/`vi.setSystemTime(...)` nach dem Muster des direkt benachbarten bestehenden Tests ergänzt.
- Task 3 (`BodyEntriesPage`, Karten, geteilter Bearbeiten-Dialog, Toast): Commit `9d38cbd`, Review clean.
- Task 4 (`BodyPhotosPage`, Formular-Karte, Foto-Karten, Toast): Commit `ee10860`, Review clean.
- Task 5 (Abschluss): Bundle gemessen, `docs/domaenenmodell.md` geprüft (keine Änderung nötig), manuelle Browser-Verifikation (siehe unten), Status hier nachgezogen.

**Manuelle Browser-Verifikation (alle 7 Punkte grün, keine Funde):** über eine Wegwerf-Testseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx`, beide nicht committet, nach der Prüfung entfernt) im echten Chrome-Tab geprüft. 2-spaltige Messwerte-Grid ohne sichtbaren Rahmen; „Heute eintragen"-Dialog zentriert mit verschwommenem Hintergrund (die Plan-1-Regressionsklasse tritt hier nicht erneut auf), Formular als Karte, „Speichern" als voller Hauptbutton; Verlaufskarten mit geteiltem Bearbeiten-Dialog ebenso zentriert und vorbelegt; ein simulierter Löschfehler zeigt einen echten Toast (`position:fixed`, `top:18px`, sichtbar per `getComputedStyle` bestätigt); alle zehn Karten im Dashboard/Verlauf/Fotos programmatisch gegengeprüft (`border-bottom-width: 0px`, `border-radius: 27px`, korrekter `bg-surface`-Hintergrund); Konsole durchgängig ohne Fehler oder Warnungen.

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? With fixes" — kein Critical, 1 Important (kein Test sperrte das Karten-in-Liste-Markup auf `BodyPage`/`BodyEntriesPage`/`BodyPhotosPage` — identische Lücke wie bei Plan 2bs Whole-Branch-Review, dort für `FoodEntryList` behoben), 3 Minor bewusst geparkt (`BodyEntriesPage`s Dialog-open-Bedingung doppelt ausgedrückt statt einer Quelle — kein demonstrierter Bug; Toast-vor-`onClose`-Reihenfolge funktioniert heute, aber ohne Fragilitäts-Kommentar für einen künftigen `await`; ein Tippfehler „Alle 4 Tasks" statt 5 in dieser Doku selbst — hier korrigiert). Das Important-Finding behoben (Commit `9892fe1` — je eine strukturelle Assertion in den drei Test-Dateien nach dem Muster von `FoodEntryList.test.tsx`), Scoped Re-Review bestätigte ADDRESSED für alle drei Seiten ohne neue Breakage, keine Tautologien (beide Regressionsrichtungen abgedeckt).

**PR #43 gegen `master` gemergt** (Merge-Commit `066803d`, alle vier CI-Checks grün: build-test, npm-audit, semgrep, zap-baseline), Worktree und Branch entfernt. **Live deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop`, Asset-Hash `index-BrxTVW_f.js` auf `https://vitaloop.web.app` verifiziert).

**Stand nach Task 5 und Fix-Welle: 735 Tests grün** (104 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich (mit echter `.env`: Entry-Chunk 999,89 kB / 272,58 kB gzip). Keine Migration in diesem Plan, `docs/domaenenmodell.md` unverändert (letzte Migration bleibt `0007`).

**Wiki synchronisiert** (06.09.2026, Commit `39b8b21`): neue Seite `Phase-6-Plan-2c-Koerper`, `Home`/`_Sidebar` verlinkt und der Phase-6-Status-Absatz um Plan 2c ergänzt. `Domain-Model.md` unverändert (Plan fasst kein Schema an).

## Plan 2d – Analyse-Seiten im neuen Design (gemerged, PR #44)

**Plan:** `docs/superpowers/plans/2026-09-06-phase6-plan2d-analyse-plan.md`, Commit `e2d2481` auf `master`, 5 Tasks. Alle 19 Graphen (Training T1–T8, Ernährung E1–E6, Körper K1–K5) bekommen die Design-Farbpalette aus dem Phase-6-Design-Spec statt Recharts' Default-Lila; `ChartFrame` (die Karten-Hülle aller Graphen) und `ZeitraumSwitch` (die Zeitraum-Buttons) verwenden jetzt `cardClass` bzw. `Chip` aus Plan 1. Umgesetzt per `superpowers:subagent-driven-development`, isolierter Worktree/Branch `worktree-phase6-plan2d-analyse`.

**Bewusst kein separates Design für Struktur** — anders als 2b/2c ändert dieser Plan keine Seiten-Layouts, nur Farben und die beiden geteilten Bausteine `ChartFrame`/`ZeitraumSwitch`. T8 (`PersonalRecordsList`) und K5 (`PhotoTimeline`) bleiben bewusst unverändert (per Spec Listen, keine Graphen, kein Farbcode nötig).

**Neues Modul `src/lib/analysis/chart-colors.ts`:** sechs literale Hex-Konstanten (`CHART_MINT` #6efde6, `CHART_BLUE` #4f6ca5, `CHART_GREEN` #49be69, `CHART_ORANGE` #ff6f43, `CHART_VIOLET` #8766ed, `CHART_GRID` #5e5f66) plus `CHART_PALETTE` (die ersten fünf, für Graphen mit variabler Balkenzahl per `Cell`-Zyklus). Literale Strings statt CSS `var()`, weil Recharts' `fill`/`stroke` SVG-Präsentationsattribute sind, keine CSS-Eigenschaften.

**Zwei Design-Entscheidungen, vom Controller getroffen (Spec nannte nur die Zielfarben je Graph, nicht die Mehrfarben-Strategie bei variabler Datenmenge):**
- **T5 (Wiederholungen je Satz)** zeichnet bis zu 6 Linien (eine je Satznummer), die Spec nennt nur „Mint" für einen einzelnen Graphen. Ruling: die Farben zyklisch aus `CHART_PALETTE` vergeben (wie T6), nicht alle sechs Linien auf Mint kollabieren — sonst wären die Sätze im Graphen nicht mehr unterscheidbar, was der eigentliche Zweck des Graphen ist.
- **E2 (Makro-Verteilung) färbt namens-basiert** (`MAKRO_FARBEN: Record<string, string>`, Schlüssel = Makro-Name aus `MAKRO_LABEL`) — Eiweiß bleibt immer Mint, unabhängig von der Reihenfolge in den Daten. **E4/T6 (Kalorien je Abschnitt / Volumen je Muskelgruppe) färben index-basiert** (`CHART_PALETTE[index % ...]`) — hier gibt es keine feste Bedeutung je Position, nur „möglichst gut unterscheidbar". Beide Strategien vom Reviewer in Task 2/3 explizit als korrekt unterschieden verifiziert.

**Alle 4 Umsetzungs-Tasks, je einzeln reviewt (Task 5 kein Subagent, siehe unten):**
- Task 1 (`ChartFrame` als Karte, `ZeitraumSwitch` als Chip-Reihe): Commit `f001bde`, Review clean.
- Task 2 (Trainingsgraphen T1–T7, Farbpalette): Commit `a390fe7`, Review clean — T6s `Cell`-Mapping und die T5-Ruling vom Reviewer explizit als korrekt verifiziert.
- Task 3 (Ernährungsgraphen E1–E6, Farbpalette): Commit `3529633`, Review clean — E2 (namens-basiert) vs. E4 (index-basiert) vom Reviewer explizit als korrekt unterschieden verifiziert, `MAKRO_LABEL`-Strings gegen die tatsächliche Quelle geprüft.
- Task 4 (Körpergraphen K1–K4, Farbpalette): Commit `0d18bba`, Review clean — K1s Trend/Rohgewicht-Zuordnung (Trend=Mint als Hauptmetrik, Rohgewicht=Blau als Vergleich, nicht vertauscht) und K2s Fünf-Farben-Reihenfolge vom Reviewer explizit verifiziert.
- Task 5 (Abschluss): Bundle gemessen, `docs/domaenenmodell.md` geprüft (keine Änderung nötig — Plan fasst kein Schema an), manuelle Browser-Verifikation (siehe unten), Status hier nachgezogen.

**Manuelle Browser-Verifikation (alle 6 Punkte grün, keine Funde):** über eine Wegwerf-Testseite (`TestPlayPage.tsx` + temporäre Route in `App.tsx`, beide nicht committet, nach der Prüfung entfernt) im echten Chrome-Tab geprüft. `ZeitraumSwitch` zeigt vier Pillen mit klar unterschiedenem aktiv/inaktiv-Zustand; alle drei geprüften Graphen (T1, T6, T8) erscheinen als eigene abgerundete Karte (`bg-surface` `rgb(35,36,43)`, `border-radius: 27px`, per `getComputedStyle` bestätigt); T1 (Einzelfarben-Graph) zeichnet beide Balken in Mint (`#6efde6`, per DOM-Attribut auf `.recharts-bar-rectangle path` bestätigt, nicht Recharts' Default-Lila); T6 (Mehrfarben-Graph) zeichnet vier klar unterscheidbare Farben in exakter `CHART_PALETTE`-Reihenfolge (Mint/Blau/Grün/Orange); T8 bleibt unverändert eine reine Liste ohne Farbcode; Konsole durchgängig ohne Fehler oder Warnungen.

**Stand nach Task 5: 738 Tests grün** (105 Dateien — zwei neue Testdateien aus Task 1, `ChartFrame.test.tsx`/`ZeitraumSwitch.test.tsx`), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Entry-Chunk `dist/assets/index-ZnqfvGHZ.js` 235,61 kB (75,63 kB gzip), CSS `dist/assets/index-Th1NYJg3.css` 16,02 kB (4,02 kB gzip) — kein echter Vergleichswert zur `master`-Baseline (Worktree ohne `.env`, wie bei allen vorherigen Plänen dieser Phase). Keine Migration in diesem Plan, `docs/domaenenmodell.md` unverändert (letzte Migration bleibt `0007`).

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? With fixes" — kein Critical, 2 Important, 7 Minor. Beide Important-Funde waren echte visuelle Regressionen, die kein Test und keine Einzelchart-Betrachtung auf der Wegwerf-Testseite (Task 5) gesehen hatte, weil dort nur einzelne Charts isoliert standen statt in der echten `TrainingChartList`/`NutritionChartList`/`BodyChartList`-Kette:
- **Alle Graph-Karten stapelten sich ohne Abstand** (`ChartFrame`s `<section>` hat kein eigenes Margin, die Listen-Komponenten rendern nur ein Fragment ohne `space-y-*`) — auf der echten Analyse-Seite wäre das eine durchgehende Fläche statt einzelner Karten gewesen. Behoben: Fragment → `<div className="space-y-4">` in allen drei ChartList-Komponenten.
- **Die vier `ZeitraumSwitch`-Chips lagen ohne Abstand aneinander** (`Chip` trägt `m-0`, der Wrapper hatte kein `flex`/`gap`) — eine echte Verschlechterung gegenüber den alten `<button>`s, die noch CSS-Margin hatten. Behoben: Wrapper-`<div>` bekommt `flex flex-wrap gap-2`.

7 Minor-Funde ebenfalls in derselben Fix-Welle behoben (kein separates Parken nötig, alle mechanisch und risikoarm): ein irreführender Kommentar in `MuscleVolumeChart` korrigiert (Sortierung ist nach Volumen absteigend, nicht nach erstem Auftreten); `MAKRO_LABEL` aus `nutrition-charts.ts` exportiert und in `MacroDistributionChart`s Farbzuordnung verwendet statt eigener Literale mit stillem `?? CHART_MINT`-Fallback (eine künftige Umbenennung fällt jetzt als Typfehler auf, nicht als drei identische Mint-Balken); `MealSectionCaloriesChart`s `Cell`-Key von `punkt.name` (frei editierbarer Profiltext, zwei gleich benannte Abschnitte wären kollidiert) auf `index` umgestellt; `RepsPerSetChart`s Farbliste importiert jetzt `CHART_PALETTE` statt sie zu kopieren; `E2`/`E4`/`T6` haben wieder ein `fill` auf dem `<Bar>` selbst (Tooltip-Farbe folgt sonst nicht der Serie); `ZeitraumSwitch.test.tsx` bekam eine zusätzliche Assertion, die wirklich an der Chip-Umstellung hängt (`rounded-full`/`bg-accent` statt nur `aria-pressed`, was schon der alte `<button>` erfüllt hätte); Tippfehler „106 Dateien" hier auf 105 korrigiert.

Nach der Fix-Welle erneut manuell im echten Chrome-Tab gegen die reale `TrainingChartList` geprüft (nicht nur Einzelcharts wie beim ersten Durchlauf): 18px sichtbarer Abstand zwischen den drei Karten (`space-y-4` greift), 9px Abstand zwischen den vier Chips (`gap-2` greift), beides per `getBoundingClientRect()` bestätigt, Konsole weiterhin sauber. **739/739 Tests grün** (105 Dateien, ein neuer Test durch die Chip-Assertion), Lint/Typecheck sauber.

**Scoped Re-Review bestätigte alle 9 Findings ADDRESSED, keine neue Regression** — unabhängig selbst Lint/Typecheck/Testsuite gefahren (105 Dateien, 739 Tests grün), nicht nur die Commit-Message vertraut.

**Zusätzlich unabhängig von Codex (GPT-5.6-Sol) gegengeprüft** (siehe „Codex/GPT als Zweitmeinung" oben) — `codex review -c model="gpt-5.6-sol" --base origin/master` auf dem finalen, bereits gefixten Stand: „The changes consistently apply the shared card and chip styling and centralize chart colors without altering chart data behavior. No actionable regressions were identified." Deckt sich mit dem Opus-Verdict nach der Fix-Welle — zwei unabhängige Modelle, gleiches Ergebnis.

**PR #44 gegen `master` gemergt** (Merge-Commit `7b7c418`, alle vier CI-Checks grün: build-test, npm-audit, semgrep, zap-baseline), Worktree und Branch entfernt. **Live deployed** (`npm ci && npm run build && firebase deploy --only hosting:vitaloop`, Asset-Hash `index-CIIZdvdp.js` auf `https://vitaloop.web.app` verifiziert, Entry-Chunk 999,96 kB / 272,59 kB gzip mit echter `.env` — praktisch identisch zur Plan-2c-Baseline).

**Wiki synchronisiert** (06.09.2026, Commit `126d86b`): neue Seite `Phase-6-Plan-2d-Analyse-Seiten`, `Home`/`_Sidebar` verlinkt und der Phase-6-Status-Absatz auf „vollständig" aktualisiert. `Domain-Model.md` unverändert (Plan fasst kein Schema an).

**Damit ist Phase 6 (Design) vollständig abgeschlossen** — alle fünf Pläne (Fundament, Training, Ernährung, Körper, Analyse-Seiten) gemerged, live deployed und im Wiki dokumentiert. Nächster Schritt: Phase 7 (Härtung) — Spec/Plan noch nicht geschrieben.

## Phase 4 – Körperbereich (abgeschlossen)

**Stand:** Alle 11 Tasks umgesetzt und je einzeln reviewt, danach Whole-Branch-Review auf dem stärksten Modell, eine Fix-Welle und ein Scoped Re-Review — alle Findings adressiert, keine neue Breakage. Gemerged über PR #26 (Merge-Commit `63aced2`), Branch entfernt. Alle vier CI-Checks grün (build-test, Semgrep, npm audit, ZAP). **421 Tests grün**, Lint, `tsc -b --noEmit` und `npm run build` sauber (nur die bekannte Bundle-Größen-Warnung, gehört zu Phase 5).

**Was es jetzt gibt:** `/body` als Dashboard mit aktuellen Werten und Veränderung, `/body/entries` als Verlaufsliste mit Korrigieren und Löschen, `/body/photos` als Foto-Zeitleiste. Erfasst werden Gewicht, fünf Umfänge und Körperfettanteil, ein Eintrag je Tag (`upsert` auf `user_id, datum`). Fotos liegen im privaten Bucket `body-photos`, sichtbar nur über kurzlebige signierte Links.

**Manuelle Verifikation gegen Produktion: alle zehn Schritte grün.** Nachgewiesen wurden unter anderem: Kalorienziel folgt dem Gewicht (82,5 kg → 1672 kcal, Mifflin-St-Jeor); ein Eintrag mit *früherem* Datum lässt `profiles.aktuelles_gewicht` unverändert; das Löschen des neuesten Eintrags zieht es auf den verbliebenen zurück; Foto 3000×2000/547 kB kommt als 1600×1067/163 kB unter `body-photos/{user-id}/{uuid}.jpg` an, signierte URL liefert 200, derselbe Pfad ohne Signatur 400; nach dem Löschen ist die Datei auch im Bucket weg (`NoSuchKey`); Konsole ohne Fehler. Auch geprüft: „Heute eintragen" öffnet **vorbelegt**, wenn für den Tag schon Werte da sind (der Critical-Fund aus dem Schluss-Review).

**Migration `0006` ist mit dem Merge auf Produktion gelaufen.** Sie legt den privaten Bucket `body-photos` an, samt Policies je Nutzer und seit der Fix-Welle mit `file_size_limit` (5 MB) und `allowed_mime_types`. Es gibt keine lokale Supabase-Instanz, deshalb ist `0006_body_photos_bucket.test.ts` (Textvergleich je Policy-Block) die einzige Prüfung, die diese Migration je bekommt — beim Ändern mitziehen.

**Wiki:** synchronisiert und gepusht (`Domain-Model`, `Phase-4-Design-Spec`, `Phase-4-Implementation-Plan`, `Home`, `_Sidebar` — Commit `b4cabcf`).

**Drei Sachen, die beim Weiterbauen gelten:**
- **`ProfileWeightSyncError`.** `useBodyMetrics` wirft ihn, wenn der Eintrag geschrieben wurde, aber `profiles.aktuelles_gewicht` nicht nachgezogen werden konnte — auch, wenn schon das Lesen des neuesten Gewichts fehlschlägt. Wer den Hook benutzt, fängt ihn ab und meldet „gespeichert, aber Profilgewicht veraltet", niemals „nicht gespeichert". `BodyEntryForm` behandelt ihn als Erfolg und schließt; die sichtbare Meldung gehört auf die Seite, die den Hook hält.
- **Formulare für einen Tag müssen vorbelegen.** Der Upsert schreibt alle sieben Spalten. Ein leeres Formular auf einen Tag, der schon Werte hat, löscht sie still — genau dieser Fehler steckte im Dashboard und wurde in der Fix-Welle behoben (`entry={rows.find(...)}`).
- **Gewichtsgrenzen sind 30–300 kg**, in `src/lib/body-metrics.ts` und `ProfilePage.tsx` identisch. Die Spec nennt noch 20–500; das Spec-Dokument ist ein datierter Entwurfsstand und wurde bewusst nicht rückwirkend geändert.

**Bewusst offen gelassen** (vom Schluss-Review als „darf warten" eingestuft): `formatDate` steht wortgleich in drei Seiten; der `onSave`-Wrapper in `BodyPage` und `BodyEntriesPage` ist nahezu identisch; das Ändern des Datums beim Korrigieren kann einen anderen Eintrag desselben Tages überschreiben; das Profil-Update meldet nicht, wenn es null Zeilen trifft; `Number('0x50')` ergibt 80; Foto-Löschen ohne `busy`-Guard und ohne Bestätigen (Projektkonvention).

## Phase 5 – Analysebereich (abgeschlossen — alle vier Pläne gemerged und gegen Produktion verifiziert)

### Sofort-Einstieg für einen neuen Chat

**Alle vier Pläne (1, 2a, 2b, 2c) sind gemerged UND gegen Produktion manuell verifiziert.** Alle 19 geplanten Graphen aus Training (T1–T8), Ernährung (E1–E6) und Körper (K1–K5) existieren im Code und wurden live geprüft — der Analysebereich ist damit vollständig abgeschlossen.
1. Plan 2a: Gemerged über PR #33 (Merge-Commit `4df8a03`), Branch/Worktree entfernt. Alle 11 Tasks einzeln abgenommen, danach Whole-Branch-Review auf Opus (2 Critical, 2 Important, 2 kleinere Nachträge — alle in einer Fix-Welle behoben), Manuelle Verifikation 29.08.2026 abgeschlossen. Details siehe „Plan 2a" unten.
2. `TrainingChartList.tsx` ist der einzige Ort, an dem noch ein Trainingsgraph eingebunden wird, `NutritionChartList.tsx` das Analogon für Ernährung, `BodyChartList.tsx` das Analogon für Körper — jeweils Dashboard und Analyse-Seite, jeder Chart hinter `React.lazy`.
3. **Plan 2b (Ernährung, E2–E6) ist fertig: gemerged, Whole-Branch-Review clean nach einer Fix-Welle, manuelle Verifikation abgeschlossen.** Details im eigenen Abschnitt „Plan 2b" weiter unten.
4. **Der Plan-Text von Plan 2b war anfangs unvollständig** (nur Task 1 geschrieben, obwohl Ziel/File-Structure fünf Graphen versprachen) — wurde während der Umsetzung nach dem Vorbild von Plan 2c um die Tasks 2–8 ergänzt (Commit `96791a6` auf dem Feature-Branch, vor dem Merge). Für neue Pläne: vor dem Start prüfen, ob wirklich alle versprochenen Tasks existieren, nicht nur Task 1.
5. **Plan 2c (Körper, K2–K5) ist gemerged über PR #39 (Squash-Merge-Commit `279e532`), Branch/Worktree entfernt, manuelle Verifikation am 05.09.2026 abgeschlossen.** Alle 8 Tasks einzeln abgenommen, danach Whole-Branch-Review auf Opus (kein Critical, ein Important — vier statt einer sequenziellen Abfrage im Körper-Hook — vor dem Merge per `Promise.all` behoben und sauber re-reviewt). Details siehe „Plan 2c" unten.

**Genau hier weitermachen:**
Kein Folge-Schritt für Phase 5 mehr offen — der Analysebereich ist inhaltlich und in der Verifikation vollständig abgeschlossen. Siehe „Status / Fortschritt" oben für den nächsten Schritt (Hosting, dann Phase 6 Design).

**Noch aufzuräumen:** die synthetischen Daten aus der Plan-1-Verifikation stehen weiter in der Produktions-Datenbank (7 `body_metrics` 05.05.–27.08., 12 `workout_sessions` 15.05.–26.08., 30 `food_entries` 18.–27.08. auf ein einziges Produkt). Vor echtem Gebrauch löschen.

### Plan 2c – Körpergraphen (gemerged, PR #39)

Vier neue Graphen im Körperbereich: **K2** Umfänge im Verlauf (die fünf Umfänge aus `MEASUREMENT_FIELDS`, je eine `Line`), **K3** Änderungsrate (kg/Woche, gerechnet auf `gewichtsTrend` — derselben EWMA-Trendlinie, die K1 zeichnet, nicht auf den Rohgewichten), **K4** Gewicht über Kalorien (Wochenaggregate: mittlere Tagesaufnahme der Woche gegen Änderung des Wochenmittelgewichts, Punktwolke), **K5** Fortschrittsfotos als Zeitleiste (neuestes Foto oben, mit dem Gewicht **desselben** Tages beschriftet, ein Foto ohne Wiegung bleibt sichtbar und trägt kein Gewicht). Zusammen mit K1 (Plan 1) sind das jetzt **fünf** Körpergraphen, alle in `src/lib/analysis/registry.ts` angemeldet und über `BodyChartList.tsx` gerendert (ein `switch` über die Registry-IDs, analog zu `TrainingChartList`/`NutritionChartList`) — der einzige Ort, an dem noch ein Körpergraph eingebunden wird.

`useBodyAnalysis(userId, zeitraum)` (`src/hooks/use-body-analysis.ts`) lädt jetzt **drei** Quellen statt einer: `body_metrics` (die Messwerte), `food_entries` mit eingebettetem `products(kalorien)` (die Tagessummen für K4) und `body_photos` samt gebündelt signierten Links (die Zeitleiste für K5, dieselbe Signierung wie `useBodyPhotos`) — alle drei seitenweise paginiert und auf den Zeitraum begrenzt. Das Körper-Dashboard feuert deshalb **drei** Abfragen (plus ggf. eine `createSignedUrls`), sobald mindestens ein Körpergraph angehakt ist, unabhängig davon, welcher — anders als beim Training (zwei Graphen = zwei Abfragen): K4 braucht die Ernährung, K5 die Fotos, die übrigen drei Graphen lesen beide ungenutzt mit. Bewusste Entscheidung, im Plan-Ledger festgehalten (`.superpowers/sdd/2026-08-29-phase5-plan2c-koerpergraphen/progress.md`): den Hook nicht nach angehakten Graph-IDs zu verzweigen, weil er dann wissen müsste, welcher Graph welche Tabelle braucht.

Kein neues Hilfsmodul für dieses Plan: `seitenweiseLaden` liegt weiterhin in `src/lib/paged-query.ts`, die Wochenhilfsfunktionen `wochenStart`/`wochenLabel` weiterhin in `src/lib/analysis/wochen.ts` — beide bereits von Plan 2b dorthin extrahiert. Der ursprüngliche Plan-2c-Text sah eigene Module unter `src/lib/analysis/paged-query.ts` und `src/lib/analysis/woche.ts` vor; der Pre-Flight-Scan des Plans fand die schon vorhandenen Pfade und die Tasks 1 und 6 wurden entsprechend umgeleitet (siehe die Rulings im Ledger).

**Bundle nach Task 8** (`npm run build`, Worktree ohne `.env` — derselbe Sonderfall wie in Plan 2b, das komplette Barcode-Scanner-Subsystem fehlt deshalb im Entry-Chunk, keine echte Vergleichszahl zur `master`-Baseline): Entry-Chunk `dist/assets/index-BQ_mSZvT.js` 235,41 kB (75,48 kB gzip). Die vier neuen Körpergraphen liegen einzeln hinter `React.lazy` und sind winzig: `BodyMeasurementsChart` 1,02 kB, `WeightChangeRateChart` 0,96 kB, `WeightVsCaloriesChart` 12,34 kB (grösser, weil sie zusätzlich Streudiagramm-Bausteine zieht), `PhotoTimeline` 0,78 kB, dazu ein `body-charts`-Sammel-Chunk mit 1,87 kB für die reinen Rechenfunktionen. Recharts bleibt bestätigt in den gemeinsamen `CartesianChart`/`Bar`/`Line`/`Legend`/`ReferenceLine`-Chunks ausserhalb des Entry-Chunks. Kein neuer offener Punkt aus diesem Plan.

**Stand: 706 Tests grün** (98 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Keine Migration in diesem Plan. `docs/domaenenmodell.md` um die Körperanalyse-Notizen ergänzt (drei Abfragen statt einer, Fotolinks nie gespeichert, K5-Tagesmatch, K3 aus der K1-Trendlinie). **Wiki synchronisiert** (`Domain-Model`, neue Seite `Phase-5-Plan-2c-Koerpergraphen`, `Home`, `_Sidebar` — gepusht, Commit `2cdb119`).

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? Yes" — kein Critical, ein Important (`useBodyAnalysis` wartete auf drei voneinander unabhängige Abfragen streng nacheinander statt parallel — vier Roundtrips, wo der Körperbereich vor diesem Plan nur einen kostete), vor dem Merge per `Promise.all` behoben (Commit `1bd2c4b`), Scoped Re-Review bestätigte die Behebung ohne neue Breakage. Sieben Minor-Funde bewusst zurückgestellt (u. a. drei verbliebene `formatDate`-Kopien trotz der `datumsLabel`-Extraktion, K2s gemeinsame Zentimeter-Achse flacht kleine Umfänge gegen große ab, K4s berechnetes Wochen-Label wird im Tooltip nicht angezeigt) — alle im Plan-Ledger vor dem Löschen protokolliert, keiner davon merge-blockierend. Gemerged über PR #39 (Squash-Merge, Commit `279e532`), Branch und Worktree entfernt.

**Manuelle Verifikation gegen Produktion abgeschlossen** (05.09.2026, alle zehn Schritte grün). Die vorhandenen Produktionsdaten (7 `body_metrics` 05.05.–27.08., abfallend, ohne Vorzeichenwechsel) deckten K2 zwar ab, aber nicht K3s Vorzeichenwechsel oder K4s Kalorienkontrast — dafür testweise drei zusätzliche `body_metrics`-Einträge in einem isolierten Zeitfenster vor der echten Historie angelegt (02.03. 84,0 kg → 09.03. 85,0 kg → 16.03. 83,5 kg), dazu zwei Ernährungseinträge (09.03.-Woche 2800 kcal, 16.03.-Woche 120 kcal) und zwei Fotos (09.03. mit Eintrag, 20.03. ohne). Alle nach der Prüfung wieder vollständig gelöscht (Einträge, Produkte, Fotos samt Storage-Datei).

Bestätigt: **K2** zeigt die Umfänge korrekt, ein Tag mit nur Gewicht (die drei März-Einträge) fehlt vollständig in der Linie (Achse beginnt bei 05.05., nicht bei 02.03.) — genau das von der Spec geforderte Verhalten. **K3** zeigt den Vorzeichenwechsel exakt wie von Hand vorgerechnet (positiv bei 09.03., negativ bei 16.03., beide Werte stimmen mit der EWMA-Trendlinien-Rechnung überein). **K4** zeigt die niedrige Kalorienwoche unten links (negative Änderung) und die hohe oben rechts (positive Änderung), klar getrennt; keine Woche ohne Ernährungseintrag erzeugt einen Punkt bei 0 kcal. **K5** beschriftet Fotos exakt tagesgenau („85 kg" für 09.03., „kein Gewicht erfasst" für 20.03.), neuestes zuerst. **Dashboard-Abfragezahl bestätigt:** mit zwei Körpergraphen angeheftet (K2+K5) feuern genau drei Tabellenabfragen (`body_metrics`, `food_entries`, `body_photos`), kein `createSignedUrls` (Testfotos lagen außerhalb des 90-Tage-Dashboard-Fensters) — die harte Eigenschaft „kein Häkchen → keine Analyseabfrage" hält (nur die reguläre absteigend sortierte `useBodyMetrics`-Abfrage lief weiter). Häkchen-Persistenz über Reload bestätigt. `/login` lädt keinen Recharts-Chunk, `/body/analyse` lädt ihn mit 200. Konsole ohne Fehler oder Warnungen.

### Plan 2b – Ernährungsgraphen (gemerged, PR #38)

Fünf neue Graphen im Ernährungsbereich: **E2** Makro-Verteilung heute (Eiweiß/Kohlenhydrate/Fett als Energie-Anteile, nicht Gramm-Anteile — sonst sähe ein fettreicher Tag ausgeglichen aus), **E3** Makro-Verlauf (dieselben drei über die Zeit, in Gramm statt Prozent), **E4** Kalorien je Mahlzeiten-Abschnitt (dieselbe Abschnitts-Logik wie die Eintragsliste, `visibleSections`: ein besetzter unbenannter Slot bleibt als „Abschnitt N" sichtbar, Unzugeordnetes läuft unter „Ohne Zuordnung"), **E5** Wochenschnitt (Kalorien je Kalenderwoche, gemittelt über Tage **mit** Eintrag statt über alle sieben Tage), **E6** Kalorienbilanz (Aufnahme minus Trainingsverbrauch je Tag, subtrahiert `gesamt_kalorien` der Sessions; ein Tag mit Session ohne Ernährungseintrag fällt weg, ein Tag mit Eintrag ohne Session zählt mit Verbrauch 0). Zusammen mit E1 (Plan 1) sind das jetzt **sechs** Ernährungsgraphen, alle in `src/lib/analysis/registry.ts` angemeldet und über `NutritionChartList.tsx` gerendert (ein `switch` über die Registry-IDs, analog zu `TrainingChartList`) — der einzige Ort, an dem noch ein Ernährungsgraph eingebunden wird.

`useNutritionAnalysis(userId, zeitraum)` liest neben den Einträgen (`food_entries` mit eingebettetem `products`) jetzt auch die Trainingskalorien mit (`workout_sessions`, nur `gestartet_am`/`gesamt_kalorien`, für E6) — zwei Abfragen, beide seitenweise paginiert, keine Beziehung zwischen den Tabellen, also kein Join. Das Ernährungs-Dashboard feuert deshalb **zwei** Abfragen, sobald mindestens ein Ernährungsgraph angehakt ist, unabhängig davon, welcher — E6 braucht die Sessions, die übrigen fünf lesen sie nur ungenutzt mit.

Zwei Hilfsmodule sind jetzt zwischen Training und Ernährung geteilt statt verdoppelt: `seitenweiseLaden` liegt seit diesem Plan in `src/lib/paged-query.ts` (vorher nur in `use-training-analysis.ts`), die Wochenhilfsfunktionen `wochenStart`/`wochenLabel` in `src/lib/analysis/wochen.ts` (vorher privat in `training-charts.ts`). Die Makro-Rechnung (`entryMakro`/`sumMakro`, Energie- statt Gramm-Anteile) liegt jetzt in `src/lib/entry-calories.ts` und wird von `DailySummary` und den Graphen E2/E3 gemeinsam benutzt statt einmal in `DailySummary` und einmal in `nutrition-charts.ts` zu stehen.

**Bundle nach Task 8** (`npm run build`): Ohne `.env` (Worktree-Default, da `.env` gitignored ist und von `git worktree add`/`git clone` nie mitkopiert wird) misst der Entry-Chunk nur 231,71 kB (74,53 kB gzip) — das ist kein echter Vergleichswert, sondern ein Build ohne `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, dem dadurch das komplette Barcode-Scanner-Subsystem (`@zxing/browser`/`@zxing/library`, erreichbar über `NutritionEntriesPage → AddEntryFlow → ProductPicker → BarcodeScanner`) fehlt. Mit echter `.env` (per Experiment gegengeprüft, inkl. Repro auf `master`) lauten die tatsächlich vergleichbaren Zahlen: `master`-Baseline 985,06 kB (268,52 kB gzip) → dieser Branch 987,48 kB (268,93 kB gzip), also **+2,42 kB / +0,41 kB gzip** — wächst kaum, wie erwartet, weil jeder neue Chart einzeln hinter `React.lazy` liegt. Recharts bleibt bestätigt außerhalb des Entry-Chunks (weiterhin in den gemeinsamen `CartesianChart`/`Bar`/`Line`/`Legend`/`ReferenceLine`-Chunks), die fünf neuen Ernährungsgraphen selbst sind winzig (0,72–1,06 kB je Komponente, dazu ein `nutrition-charts`-Sammel-Chunk mit 1,95 kB). Kein neuer offener Punkt aus diesem Plan; das Bundle bleibt aus den früheren Phasen über der Warnschwelle, gehört weiter in die Härtungsphase.

**Stand: 655 Tests grün** (93 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Keine Migration in diesem Plan. `docs/domaenenmodell.md` um die Ernährungsanalyse-Notizen (E2–E6, zwei Abfragen statt Join, geteilte Hilfsmodule) ergänzt — **Wiki bereits synchronisiert** (`Domain-Model`, neue Seite `Phase-5-Plan-2b-Ernaehrungsgraphen`, `Home`, `_Sidebar` — gepusht, Commit `decc790`).

**Whole-Branch-Review (Opus) und eine Fix-Welle abgeschlossen:** Verdict „Ready to merge? With fixes" — kein Critical, 2 Important (ein Test prüfte Achsentext statt gezeichneter Marken entgegen der Projekt-Konvention; ein Sechs-Graphen-Seitentest hatte zu knappes Timeout-Budget für kalten Cache), 3 Minor behoben (E3 fehlte der „geloeschtes Produkt überspringen"-Guard, den E1/E2 schon hatten; zwei Doku-Tippfehler). Scoped Re-Review bestätigte alle 5 Findings adressiert, keine neue Breakage. Gemerged über PR #38 (Squash-Merge), Branch und Worktree entfernt.

**Manuelle Verifikation gegen Produktion abgeschlossen** (04.09.2026, alle zehn Schritte grün): E2 zeigt Gramm-Labels exakt wie `DailySummary` (55/45/95 g), aber Energie- statt Gramm-Anteile (Fett ~40 % trotz nur 23 % Gramm-Anteil) — der entscheidende Spec-Fund. E4 summiert neue Einträge korrekt in ihre Abschnitte. **E6 bestätigt exakt: 1017 kcal Bilanz = 1025 kcal Aufnahme − 8 kcal einer echten Live-Session.** Dashboard feuert genau zwei Tabellenabfragen unabhängig vom angehakten Graphen; ohne Häkchen keine Analyseabfrage. `/login` lädt keinen Recharts-Chunk. Ein einmaliger Ladefehler beim allerersten Seitenaufruf war ein reproduzierbarer Kaltstart-Flake des Dev-Servers (zweiter Aufruf sofort fehlerfrei), kein Code-Bug. Testdaten (2 Einträge, 1 Session, 1 Plan) danach vollständig gelöscht.

### Plan 2a – Trainingsgraphen (gemerged, PR #33)

Sieben neue Graphen im Trainingsbereich: **T2** Kraftverlauf je Übung (geschätztes 1RM, Epley), **T3** Volumen je Übung, **T4** bestes Satzgewicht, **T5** Wiederholungen je Satz, **T6** Volumen je Muskelgruppe, **T7** Dauer und Kalorien je Session, **T8** persönliche Rekorde (Liste, kein Recharts). Zusammen mit T1 (Plan 1) sind das jetzt **acht** Trainingsgraphen, alle in `src/lib/analysis/registry.ts` angemeldet und über `TrainingChartList.tsx` gerendert (ein `switch` über die Registry-IDs). T2–T5 bekommen zusätzlich eine Übungsauswahl (`mitUebungsauswahl`), vorbelegt mit der am häufigsten trainierten Übung; auf dem Dashboard ausgeblendet.

`useTrainingAnalysis` liest `workout_sessions` und danach `workout_session_sets` über die Session-IDs im Zeitraum, mit `exercises(name, muskelgruppen_primaer)` eingebettet. Volumen wird bei zwei Muskelgruppen je zur Hälfte verteilt, nicht doppelt angerechnet. Aufwärmsätze zählen in keinem der neuen Graphen mit.

**Bundle nach Task 11** (`npm run build`): Entry-Chunk `dist/assets/index-BA_sHCOY.js` 984,53 kB (268,30 kB gzip) — praktisch unverändert gegenüber Plan 1 (977 kB), weil jeder neue Chart einzeln hinter `React.lazy` liegt (eigene Chunks von 0,26–1,34 kB je Komponente, dazu ein gemeinsamer `training-charts`-Chunk mit 4,08 kB). Recharts selbst bleibt in den gemeinsamen `CartesianChart`/`Bar`/`Line`/`Legend`-Chunks außerhalb des Entry-Chunks. Weiterhin über Vites Warnschwelle (500 kB) — bekannt, gehört in die Härtungsphase.

**Endstand nach Whole-Branch-Review, Fix-Welle und CI-Fix: 606 Tests grün** (85 Dateien), Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich. Die Sätze-Abfrage in `use-training-analysis.ts` paginiert jetzt (`.range()`, analog zu `use-exercises.ts`) und chunkt Session-IDs zu 100 je Anfrage — vorher lief sie ungechunkt und ungedeckelt, Risiko genau des Fehlers, der in Phase 3 schon einmal auftrat.

### Manuelle Verifikation Plan 2a (29.08.2026, alle zehn Schritte grün)

Der Account hatte keine Sätze (nur die synthetischen Plan-1-Sessions ohne `workout_session_sets`), deshalb testweise ein echter Plan mit zwei Übungen angelegt (Barbell Bench Press, drei Arbeitssätze plus ein Aufwärmsatz; Barbell Squat, ein Satz) und über eine echte Live-Session erfasst.

- Alle **acht** Graphen erscheinen auf `/training/analyse` mit Titel und Häkchen.
- **Der entscheidende Fix bestätigt:** T2, T3, T4, T5 zeigen die Übungsauswahl weiterhin, obwohl T2/T4/T5 wegen zu weniger Tage „Noch nicht genug Daten" melden — die Auswahl verschwindet nicht mehr mit dem Chart (Critical-Fund #1 des Schluss-Reviews). Umschalten auf die kaum trainierte Übung (Squat, ein Satz) und zurück funktioniert, keine Sackgasse.
- T3 (Volumen je Übung) und T6 (Volumen je Muskelgruppe) per Hover geprüft: Bankdrücken 1630 kg (Summe der drei Arbeitssätze, inklusive eines versehentlichen Duplikat-Satzes durch einen eigenen Fehlklick — der 40-kg-Aufwärmsatz ist korrekt **nicht** enthalten, sonst wären es 2030 kg). Muskelgruppen-Volumen: chest 1630 kg, quadriceps 500 kg, keine Überschneidung.
- T8 (persönliche Rekorde) zeigt beide Übungen korrekt nach höchstem geschätzten 1RM (Epley), nicht nach höchstem Gewicht: Bankdrücken 101,3 kg aus 80 kg × 8 (nicht aus 82,5 kg × 6, das nur 99,0 kg ergibt) — bestätigt, dass T8 wirklich rechnet statt nur das schwerste Gewicht zu übernehmen.
- Zwei Graphen angehakt (T3, T6), `/training` geöffnet: beide erscheinen ohne Zeitraum-Umschalter, dafür mit Übungsname im Titel („Volumen je Übung – Barbell Bench Press …", bestätigt Important-Fund #3). Netzwerkanalyse: genau zwei Abfragen (`workout_sessions`, `workout_session_sets`, beide paginiert mit `.range()`, je doppelt geloggt durch React-StrictMode im Dev-Modus — kein Produktionsverhalten).
- Alle Häkchen abgewählt, `/training` neu geladen: kein Graph, **keine** Analyseabfrage (nur die unabhängige Tag-Rotations-Abfrage lief weiter).
- `/login`: kein Recharts-Chunk im Netzwerk-Log. Konsole ohne Fehler oder Warnungen.
- Testdaten danach vollständig gelöscht (Session über „Session löschen", Plan über „Löschen" in Meine Pläne).

### Manuelle Verifikation Plan 1 (27.08.2026, alle neun Schritte grün)

- `profiles.analyse_auswahl` stand auf `["T1","E1","K1"]`; Dashboards zeigen die Graphen ohne Zeitraum-Knöpfe, die Analyseseiten mit.
- **Der entscheidende Schritt:** Häkchen bei E1 abgewählt → DB steht auf `["T1","K1"]`, Graph verschwindet vom Ernährungs-Dashboard und bleibt nach vollem Reload weg; wieder anhaken bringt ihn zurück. Die Auswahl liegt also wirklich im Profil.
- Ein Dashboard ohne angehakten Graphen feuert **keine** Analyseabfrage — nachgewiesen im Netzwerk-Log (nur die Tagesabfrage lief).
- Zeitraum: 30 Tage → `zeitpunkt=gte.2026-07-28`, „alles" → gar kein Filter; die Achse beim Gewichtsverlauf sprang von `01.08.–27.08.` auf `05.05.–27.08.`.
- E1 zeichnet die Ziel-Linie auf 1672 kcal, also auf `effectiveCalorieGoal(profile)`. T1 zeigt die Wochen korrekt mit Nulllücken (KW29 = 2, KW30/31 = 0, KW32–35 = 2).
- `React.lazy` greift: `/login` lädt keinen Recharts-Chunk, beim Wechsel auf ein Dashboard kommt `deps/recharts.js` mit 200. Konsole ohne Fehler und Warnungen.

**Testdaten:** Der Account hatte keine Historie (0 Zeilen `body_metrics`, 0 `workout_sessions`), deshalb wurden am 27.08.2026 **synthetische Daten in die Produktions-DB geschrieben**: 7 `body_metrics` (05.05.–27.08., 88,0 → 82,5 kg), 12 `workout_sessions` (15.05.–26.08.) und 30 `food_entries` (18.–27.08., alle auf das Produkt HARIBO CHERRY-COLA). Ein Alt-Eintrag mit `menge = 50000` (50 kg, 167.000 kcal, hat die Y-Achse von E1 gesprengt) wurde gelöscht. **Vor echtem Gebrauch aufräumen**, sonst verfälschen die erfundenen Zeilen jeden späteren Graphen.

**Zwei Randbeobachtungen ohne Befundcharakter:** `/login` ist auch eingeloggt erreichbar (kein Redirect aufs Dashboard); die Recharts-Einblendanimation braucht im Hintergrund-Tab einen zweiten Frame, Screenshots direkt nach dem Laden zeigen sonst ein leeres Koordinatensystem.

**Stand:** Plan 1 vollständig — alle 15 Tasks umgesetzt und je einzeln reviewt, danach Whole-Branch-Review auf dem stärksten Modell, eine Fix-Welle, ein Scoped Re-Review und zwei nachgezogene Rulings. Gemerged über PR #27 (Merge-Commit `5cd7b19`), Branch entfernt. **525 Tests grün**, Lint, `tsc -b --noEmit` und `npm run build` sauber.

**Zuschnitt gegenüber den Eckpunkten geändert: der Home-Bereich ist NICHT dabei.** `HomePage.tsx` ist ein Platzhalter und `day_status` wird von keiner Stelle beschrieben — H1–H3 hätten weder Dashboard noch Daten. Home-Dashboard und Trainingstag/Restday-Kalender werden ein eigenes Vorhaben. Phase 5 deckt Training, Ernährung und Körper ab: 19 Graphen, davon 3 in Plan 1.

**Was es jetzt gibt:** `/training/analyse`, `/nutrition/analyse`, `/body/analyse` mit Zeitraum-Umschalter (30/90/365/alles, Vorgabe 90) und je einem Graphen: T1 Trainingsfrequenz, E1 Kalorien pro Tag gegen Ziel, K1 Gewichtsverlauf mit Trendlinie. Ein Häkchen am Graphen heftet ihn ans Dashboard; die Auswahl liegt in `profiles.analyse_auswahl` (Migration `0007`). Dashboards zeigen fest 90 Tage ohne Umschalter und lösen **keine** Abfrage aus, solange nichts angehakt ist. **Plan 2** (die restlichen 16 Graphen) ist noch nicht geschrieben.

**Migration `0007` ist mit dem Merge auf Produktion gelaufen.** Sie fügt `profiles.analyse_auswahl jsonb not null default '["T1","E1","K1"]'` hinzu, sonst nichts; Policies bleiben unangetastet.

**Bundle:** Entry-Chunk 977 kB / 266 kB gzip — praktisch der Stand vor Recharts, weil die Chart-Komponenten an ihren Verwendungsstellen nachgeladen werden. Weiterhin über Vites Warnschwelle; das ist der Rest der App und gehört in die Härtungsphase.

**Fünf Dinge, die beim Weiterbauen gelten:**
- **Graph-Tests prüfen gezeichnete Marken, nie Achsentexte.** Recharts' Tick-Skipping ist eine Layout-Heuristik, die in jsdom anders ausfällt. Balken: Anzahl der Rechtecke. Linien: `M`/`L`-Befehle im `d` der Kurve — aber Achtung, `type="monotone"` liefert ab drei Punkten `M…C…C…`, die Zählung stimmt nur bei genau zwei.
- **Recharts zeichnet für einen Nullwert gar keine Marke**, und **verwirft eine `ReferenceLine` über dem Wertebereich der Y-Achse** — deshalb trägt E1 `ifOverflow="extendDomain"`. Ohne das sieht niemand seine Ziel-Linie, der unter dem Ziel isst.
- **Der `getBoundingClientRect`-Stub in `src/test-setup.ts` muss auf `recharts-responsive-container` begrenzt bleiben.** Pauschal belegt eine Legende die ganze Zeichenfläche und die Linien bleiben ohne Fehlermeldung leer.
- **Jedes `findBy*` hinter einer `React.lazy`-Grenze braucht `{ timeout: 5000 }`.** Die Vorgabe von 1000 ms reicht auf einem belasteten Rechner nicht für den dynamischen Import — sonst flakt CI.
- **Das Kalorienziel kommt immer aus `effectiveCalorieGoal(profile)`, nie aus `taegliches_kalorienziel`.** Das Rohfeld ist im Normalfall leer; das Ziel wird gerechnet.

**Bewusst offen gelassen:** `chartsFor`/`CHARTS` werden noch nicht von den Seiten genutzt (die sechs Verwendungsstellen nehmen jetzt immerhin ID-Konstanten aus der Registry); drei aufeinanderfolgende Häkchen-Klicks innerhalb eines Schreibvorgangs können den mittleren verlieren; die Zeitraum-Untergrenze vergleicht ein lokales Datum gegen `timestamptz` (ein Teiltag am Fensteranfang); `useProfile` läuft auf der Ernährungs-Analyseseite doppelt; `useChartSelection` liegt in `ChartPicker.tsx` statt unter `src/hooks/`; `tagesLabel` steht doppelt.

**Ziel:** Interaktive Graphen über alle vier Bereiche, damit sich Fortschritt und Schwachstellen ablesen lassen.

**Entschieden:**
- **Bibliothek: Recharts.** Ausschlaggebend ist SVG-Rendering, nicht die Größe: unsere Tests laufen in jsdom, ein SVG-Chart lässt sich auf Achsen, Datenpunkte und Beschriftung prüfen, ein Canvas-Chart (Chart.js, ECharts) wäre dort eine leere Box und nur mockbar. ~136 kB gzip obendrauf, deshalb **Analyse-Seiten per `React.lazy`** nachladen — zieht das für die Härtungsphase vorgemerkte Code-Splitting vor.
- **Alle Graphen werden gebaut, keine Vorauswahl.** Je Bereich eine Unterseite `/analyse`, die alle Graphen dieses Bereichs zeigt. Das Dashboard zeigt nur die vom Nutzer angehakten — der Picker steuert die Startansicht, nicht die Verfügbarkeit. Passt zur Regel „Dashboards nur das Wichtigste".
- **Technik:** Registry, in der sich jeder Graph mit ID, Bereich, Titel und Komponente anmeldet. Die Auswahl des Nutzers ist eine Liste von IDs in einer `jsonb`-Spalte auf `profiles`. Keine neue Tabelle, kein Drag-and-drop-Framework.
- Umfang realistisch zwei Pläne, nicht einer.

**Geplante Graphen** (Nummern aus der Vorauswahl, für die Spec):
- Training: T1 Trainingsfrequenz · T2 Kraftverlauf je Übung (geschätztes 1RM, Epley) · T3 Volumen je Übung · T4 bestes Satzgewicht · T5 Wiederholungsverlauf je Satz · T6 Volumen je Muskelgruppe · T7 Dauer und Kalorien je Session · T8 persönliche Rekorde
- Ernährung: E1 Kalorien pro Tag gegen Ziel · E2 Makro-Verteilung heute · E3 Makro-Verlauf · E4 Kalorien je Mahlzeiten-Abschnitt · E5 Wochenschnitt · E6 Kalorienbilanz inkl. Trainingsverbrauch
- Körper: K1 Gewichtsverlauf mit geglätteter Trendlinie (exponentiell gewichteter Mittelwert — Tagesgewicht schwankt durch Wasser um 1–2 kg, ungeglättet liest man Rauschen als Fortschritt) · K2 Umfänge im Verlauf · K3 Änderungsrate kg/Woche · K4 Gewicht über Kalorien · K5 Fortschrittsfotos als Zeitleiste
- Home: H1 Aktivitätsraster aus `day_status` · H2 Wochen-Kurzform · H3 zwei Sparklines

**Die neuen Datenfelder sind umgesetzt** (Branch `feat-analysefelder`, Migration `0005_analysis_fields.sql`) — bewusst vor Phase 4, damit ab sofort Historie entsteht. Entschieden und gebaut:
- RIR-Skala **0–5** („wie viele hättest du noch geschafft", 0 = keine), nicht RPE — direkt beantwortbar ohne Umrechnen. Sechs Knöpfe im Live-Modus, nichts vorausgewählt, erneuter Tipp auf denselben Wert löscht ihn wieder.
- **Aufwärmsätze zählen nicht auf das Satzziel.** `satz_nummer` bleibt eine reine Reihenfolge-Nummer über alle Sätze; die Zählung „Satz 1 von 3" wird in der Oberfläche aus den Arbeitssätzen abgeleitet. Die Datenbank nummeriert nichts um.
- Nach einem gespeicherten Satz springt der Aufwärm-Schalter zurück auf „aus" — ein vergessener Schalter würde echte Sätze still als Aufwärmen ablegen, der teurere der beiden Fehler.
- Ballaststoffe, Zucker und **Salz (nicht Natrium)** aus Open Food Facts, dazu von Hand eingebbar in beiden Produktformularen.
- `koerperfettanteil` ist nur die Spalte; die Eingabe entsteht in Phase 4 mit dem Körperbereich.
- **Review-Nachlauf (Branch `fix-dezimalwerte`):** PR #24 wurde wieder vor der Review gemerged. Die Review danach fand drei Punkte, einer davon ein **Produktionsfehler seit Phase 1/2**: sämtliche `type="number"`-Felder hatten kein `step`, der Browser nimmt dann `step=1` und blockiert jeden Dezimalwert als `stepMismatch` — ohne `noValidate` bricht das den ganzen Submit ab, mit Browser-Tooltip statt unserer Meldung. Betroffen war nicht nur Salz (0,8 g), sondern auch **Größe und Gewicht im Profil** (82,5 kg) und die Menge beim Eintragen. Jetzt `step="any"` auf allen Feldern, die echt gebrochen sein dürfen; ohne `step` bleiben nur `Alter` und die Zielwerte im Plan-Editor (Sätze, Wiederholungen, Pause) — das sind die einzigen `integer`-Spalten unter den Zahlenfeldern. Dazu: die Fehlermeldung nannte nur „Makros", prüft aber inzwischen alle Gramm-Angaben; und die RIR-Korrektur in der Historie akzeptierte `2.6`, was Postgres als `smallint` still auf `3` gerundet hätte. Der Scoped Re-Review bestätigte alle drei Fixes ohne Regression und fand **denselben Rundungsfehler an vier Geschwisterfeldern** (Wiederholungen in der Satz-Korrektur sowie Sätze/Wiederholungen/Pause im Plan-Editor) — ebenfalls behoben. **Merke:** Die einzigen `integer`-Spalten hinter Zahlenfeldern sind `profiles.alter`, `workout_session_sets.wiederholungen`/`rir` und die drei Zielwerte; alles andere ist `numeric` und darf Nachkommastellen haben.
- Nebenbei aufgeräumt: die Spaltenliste für `products` stand an drei Stellen und wird jetzt aus `product-lookup.ts` geteilt; der Nährwert-Vergleich in der Produktbearbeitung läuft über die geparsten Schlüssel statt Feld für Feld, damit ein später ergänzter Nährwert nicht still aus der Prüfung fällt.

**Ursprüngliche Festlegung, alle vier vom Nutzer bestätigt** — gehören erfasst, *bevor* die abhängigen Graphen gebaut werden, sonst haben sie keine Historie:
- `workout_session_sets.rir` (Anstrengungsgrad je Satz, Feld im Live-Modus)
- `workout_session_sets.ist_aufwaermsatz` — **ohne diesen Schalter sind alle Volumen-Graphen systematisch zu hoch**, weil Aufwärmsätze mitzählen
- `body_metrics.koerperfettanteil`
- `products`: Ballaststoffe, Zucker, Salz, samt Übernahme aus Open Food Facts

## Phase 3 – Trainingsbereich (abgeschlossen)

- Spec: `docs/superpowers/specs/2026-08-21-phase3-trainingsbereich-design.md`
- Plan: `docs/superpowers/plans/2026-08-21-phase3-trainingsbereich-plan.md` (16 Tasks)
- PR #21 (`feat-phase3-trainingsbereich` → `master`) gemerged, Merge-Commit `7420145`. Alle 16 Tasks fertig, jeder Task ein Commit (`4961729`..`0098f65`), danach zwei Fix-Runden.
- Stand: **311/311 Tests grün**, Lint ohne Fehler und Warnungen, `tsc -b --noEmit` sauber, `npm run build` erfolgreich.

Umgesetzt: Übungsdatenbank importierbar (free-exercise-db, 873 Übungen, MET-Wert je Kategorie); Trainingspläne mit mehreren benannten Tagen (z. B. Push/Pull/Legs) samt Plan-Editor mit Umsortieren; automatische Tag-Rotation aus der zuletzt abgeschlossenen Session; Live-Trainingsmodus mit sofort gespeicherten Sätzen, Pausen-Timer über Zielzeitpunkt und automatischem Sprung zur nächsten Übung; Kalorienberechnung über die MET-Formel beim Abschließen; Trainingshistorie mit Detailansicht, nachträglicher Satz-Korrektur und Löschen. Routen: `/training`, `/training/plans`, `/training/plans/:planId`, `/training/exercises`, `/training/session/:sessionId`, `/training/history`, `/training/history/:sessionId`.

Migration `0004_training_days.sql`: legt `workout_plan_days` an, benennt `workout_plan_exercises` in `workout_plan_day_exercises` um (mit `workout_plan_day_id` statt `workout_plan_id`) und hängt `workout_sessions` ebenfalls an den Tag. Die Tabellen waren leer, deshalb keine Datenmigration — **vor dem Merge in Supabase gegenprüfen**, da die Migration bei Merge nach `master` automatisch auf Produktion läuft.

**Beim Umsetzen gefunden und abweichend vom Plan gelöst** (der Plan-Code hätte an mehreren Stellen nicht funktioniert):
- `upsert(..., { onConflict: 'name' })` im Import-Skript wäre gescheitert — `exercises.name` hat keinen Unique-Constraint. Stattdessen ersetzt das Skript den Import-Satz (erst `insert`, dann Löschen der alten Zeilen per ID in Chunks); ein Unique-Index auf `name` würde verhindern, dass zwei Nutzer dieselbe Übung anlegen.
- `import.meta.url === \`file://${process.argv[1]}\`` ist unter Windows immer falsch — `main()` wäre nie gelaufen.
- Rotation: die Abfrage nach der letzten Session filterte nicht auf beendete Sessions; Postgres sortiert `null` bei `desc` zuerst, eine abgebrochene Session hätte die Rotation dauerhaft verstellt. Jetzt `.not('beendet_am', 'is', null)`.
- Drei Seiten schrieben Zahlenfelder pro Tastendruck in die DB (Plan-Editor, Satz-Korrektur) — jetzt Draft mit Commit auf `blur`, wie im `CalorieGoalEditor`.
- Alle neuen Hooks haben den `requestId`-Guard (Out-of-order-Reloads, State nach Unmount), im Plan-Code fehlte er durchgängig.
- Fehlgeschlagene Writes wurden auf allen Seiten stumm geschluckt (unbehandelte Rejections) — jetzt überall sichtbare Meldung; ein fehlgeschlagener Satz startet keine Pause, ein fehlgeschlagenes Löschen navigiert nicht weg.
- Der Test-Helper des Plans (`async function PageUnderTest`) ist in React 19 nicht renderbar (3×), die Timer-Tests hingen unter eingefrorenen Fake-Timern, und der `PauseTimer` verstieß mit `Date.now()` im Render und Ref-Zuweisung im Render gegen zwei Lint-Regeln.

**Erledigt nach dem Merge:** Wiki synchronisiert (`Domain-Model`, `Phase-3-Design-Spec`, `Phase-3-Implementation-Plan`, `Home`, `_Sidebar` — gepusht, Commit `9c69ef3`), Branch `feat-phase3-trainingsbereich` lokal und remote entfernt.

**Übungsimport erledigt:** 873 Zeilen in `exercises`, alle mit gesetztem `met_wert`, `created_by = null`. Erneuter Import (falls der Datensatz je aktualisiert wird) über `npm run import-exercises` mit `SUPABASE_SERVICE_ROLE_KEY` und `VITE_SUPABASE_URL` in der Umgebung — der Key gehört **nicht** in den Chat, bewährt hat sich eine gitignorierte Datei (`.env.import.local`, greift über das `*.local`-Muster) plus `node --env-file=.env.import.local scripts/import-exercises.ts`, danach löschen.

**Migration `0004` ist auf Produktion angewendet** (automatisch beim Merge): `workout_plan_days` existiert, `workout_plan_exercises` ist weg (umbenannt), `workout_plan_day_exercises.workout_plan_day_id` vorhanden, `workout_sessions.workout_plan_day_id` vorhanden, Funktion `activate_workout_plan` vorhanden.

**Manual-Verification abgeschlossen, 9 von 9 Schritten grün** (Dev-Server gegen die Produktions-Supabase, per Browser durchgeklickt): Plan anlegen → zwei Tage → Übungen aus der importierten Bibliothek zuordnen → Zielwerte setzen (nach vollem Reload persistent) → aktivieren („aktiv"-Marker, läuft über `activate_workout_plan`) → `/training` schlägt Tag 1 vor → Session mit zwei Sätzen, Pausen-Timer zählt herunter, nach Ablauf automatischer Sprung → abschließen mit rechnerisch exakten 9 kcal (MET 5 × 135 kg × ~48 s) → Rotation springt auf Tag 2 → Historie zeigt Plan/Tag/Datum/kcal, Sätze mit Dezimalgewicht korrekt, Korrektur nach Reload persistent → Session löschen, Rotation fällt auf Tag 1 zurück → Gewicht im Profil geleert: „—" und deaktivierter Abschluss-Button. Konsole ohne Fehler und Warnungen. Testdaten (Plan und beide Sessions) danach wieder gelöscht.

**Dabei bestätigt, dass die Fixes aus den Review-Runden im echten Betrieb greifen:** Zielwerte schreiben erst beim Verlassen des Feldes, „nicht beendet" statt „0 kcal" bei unbeendeter Session, vollständige Übungsliste durch die Paginierung (letzter Eintrag alphabetisch vorhanden), Aktivierung als ein Statement.

**Kosmetische Funde aus der Verifikation (behoben; PR #22 gemerged, Merge-Commit `ba95507`; Review-Fixes in PR #23 offen):**
- Nach dem letzten Zielsatz zeigt das Formular jetzt „Alle Sätze erfasst" statt „Satz 3 von 2"; weitere Sätze bleiben erfassbar (`SetForm` in `src/pages/WorkoutSessionPage.tsx`, ein Test dazu).
- Basis-Layout in `src/index.css` ergänzt. Die App hat nirgends `className`, deshalb rein elementbasiert: Buttons, Eingaben und Links mindestens 44 px hoch, alleinstehende Links als eigene Zeile über die volle Breite, Listen ohne Bullets mit Trennlinie und Abstand, Bottom-Nav als sticky Leiste, `main` mit Innenabstand. Wirkt app-weit, nicht nur im Trainingsbereich. **Optisch noch nicht im Browser gegengeprüft.**
- Review fand fünf Findings, keine blockierende Korrektheitslücke (Commit `b637b1c`): 44 px Mindest**breite** für Links (das Profil-Emoji im Header war nur ~36 px breit), `env(safe-area-inset-bottom)` auf der Bottom-Nav (Home-Indicator verdeckte das untere Drittel der Tabs), `ziel_saetze = 0` ist erlaubt und hätte „Alle Sätze erfasst" vor dem ersten Satz gezeigt, `role="list"` auf allen neun `<ul>` (Safari nimmt einer Liste ohne Marker die Listensemantik), Zeilen-Selektor unabhängig von der Verschachtelung.
- Scoped Re-Review fand **zwei Nachzügler in den Fixes** (Commit `4fecea8`): die Null-Ziel-Prüfung gab es an zwei Stellen, `pauseOver` hatte den Guard nicht und wäre bei `ziel_saetze = 0` nach dem ersten Satz sofort weitergesprungen — jetzt ein `targetReached()` für beide; und `main a:not(li a)` war zu weit gefasst und hat den Link im Satz „Für ein Tagesziel Profil vervollständigen." umbrochen — Absatz-Links jetzt ausgenommen.
- **Achtung:** PR #22 wurde gemerged, während der Review noch lief — auf `master` liegt der Stand vor den Review-Fixes. Die drei Commits aus Review und Re-Review (`b637b1c`, `4fecea8`, `3c9cb52`) hängen am selben Branch `fix-training-layout` und liegen als **PR #23** vor. Erst nach dessen Merge ist der Bereich vollständig.
- Stand danach: 313/313 Tests grün, Lint sauber, Build erfolgreich.

**Merke für die Bedienung:** Die Profilseite speichert über den „Speichern"-Button, **nicht** bei `blur` — anders als die Zielwert- und Satzfelder im Trainingsbereich.

**Whole-Branch-Review und zwei Fix-Runden abgeschlossen** (Commits `799bc23`, `1e212d0`, `59c7449`): 12 Findings aus der Review behoben, danach hat der Scoped Re-Review **zwei High-Regressionen in den Fixes selbst** gefunden — beide behoben:
- Das Wiederaufnehmen einer offenen Session hatte keine Zeitgrenze und hebelte die Dauer-Korrektur wieder aus (Session von Montag am Freitag fortgesetzt → ~96 h, ~38 000 kcal). Jetzt 6-Stunden-Fenster.
- Das Löschen des alten Import-Satzes per ID-Liste hätte ~32 KB Query-String erzeugt (Gateway lehnt ab) — da der Insert vorher committet, wäre die Bibliothek dauerhaft **verdoppelt** worden. Jetzt 100er-Chunks, das ID-Lesen zusätzlich paginiert.

Ebenfalls erledigt: `on delete set null` auf `workout_sessions.workout_plan_day_id`, Unique-Index `(workout_plan_day_id, exercise_id)`, `activate_workout_plan()` als Postgres-Funktion (ein Statement statt zwei Writes), Paginierung mit Fehlerzustand in `useExercises`, kompensierende Writes mit eigener Fehlermeldung, ISO-Zeitstempel als geparste Instants statt als Strings.

**Bewusst offen gelassen (keine Fehler, sondern Entscheidungen):**
- Kein Rückfragen-Dialog vor dem Löschen von Plan oder Session.
- Bundle bei 968 kB (264 kB gzip), über Vites Warnschwelle — Code-Splitting gehört in Phase 5.
- Die Dauer einer Session läuft bis zum letzten Satz; eine Session ohne jeden Satz schreibt `0` statt `null` und erscheint in der Historie als „0 kcal" statt „nicht beendet".
- Übungen werden beim Wiederaufnehmen einer Session nur innerhalb von 6 Stunden fortgesetzt (`RESUME_WINDOW_HOURS` in `src/hooks/use-workout-session.ts`); danach beginnt eine neue Session, die alte bleibt als „nicht beendet" in der Historie stehen.

**Nach der Manual-Verification:** Phase 4 (Körperbereich & Health-Integration) — Spec und Plan dafür noch zu erstellen (`superpowers:brainstorming` → `superpowers:writing-plans`).

## UI-Struktur (gilt ab Phase 3)

Die vier Dashboards zeigen **nur das Wichtigste**. Detaillisten — Produkte, Übungen, Verlauf — gehören auf eigene Unterseiten je Bereich, nicht aufs Dashboard. Neue Bereiche von Anfang an so planen, statt später aufzubrechen.

Nachzuholen: `NutritionPage` zeigt aktuell Tagesübersicht, Ziel-Editor, Eintragsliste und Hinzufügen-Flow auf einer Seite; die Produkt-/Eintragsliste gehört auf eine eigene Seite.

## Phase 1 – Grundgerüst & Security-Basis (abgeschlossen)

- Spec: `docs/superpowers/specs/2026-08-18-phase1-grundgeruest-design.md`
- Plan: `docs/superpowers/plans/2026-08-18-phase1-grundgeruest-plan.md`
- PR #7 (`worktree-phase-1-grundgeruest` → `master`) gemerged, Merge-Commit `5cd0f69`. Feature-Branch lokal und remote gelöscht, Worktree entfernt.
- GitHub-Remote: `git@github.com:SueleymanOezel/fitness-app.git`, Default-Branch `master`, **gepusht**.
- Supabase-Projekt: `https://zqliubzvzbnaogqcmypg.supabase.co`, mit GitHub-Repo verbunden, "Deploy to production"-Toggle **aktiviert**, Production-Branch = `master` — DB-Migration wird ab jetzt bei Merge nach `master` automatisch angewendet.

**Task-Status (Plan hat 9 Tasks):**
- [x] Task 1: Projekt-Scaffold & Test-Tooling — fertig, Review approved (Commit `74a0257`)
- [x] Task 2: Supabase-Client mit Env-Validierung — fertig, Review approved (Commit `9dc9ac2`)
- [x] Task 3: useSession-Hook — fertig, Review approved (Commit `0c35565`)
- [x] Task 4: ProtectedRoute-Komponente — fertig, Review approved nach 1 Fix-Runde (Commit `23c6d2d`; behebt Root-Cause-Bug in Task 1's Test-Setup: globales `afterEach(cleanup)` in `src/test-setup.ts` fehlte, wichtig für Task 5/6)
- [x] Task 5: LoginPage — fertig, Review approved (Commit `b103a23`)
- [x] Task 6: Platzhalter-Dashboards, Navigation, Routing — fertig, Review approved (Commit `249ba6e`)
- [x] Task 7: Datenbank-Migration (Basisschema + RLS) — fertig, Review approved (Commit `c01c4b8`; Security-Review hat 3 konkrete Angriffsszenarien gegen die RLS-Policies durchgespielt, alle korrekt blockiert)
- [x] Task 8: CI/CD-Pipeline (Semgrep, npm audit, ZAP) + Dependabot — fertig, Review approved (Commit `7bfb16f`)
- [x] Task 9: Finale lokale Verifikation — fertig, Review approved (Commit `cefcef2`; build/lint/test alle grün, 20/20 Tests)

**Finale Whole-Branch-Review:** abgeschlossen (Verdict: "ready to merge with fixes"). Security-Kern bestätigt solide (kein service_role/Secrets im Code, RLS fail-closed auf allen 12 Tabellen, kein Push passiert, Build/Lint/Typecheck grün). 6 Important- und 9 ausgewählte Minor-Findings in EINER gebündelten Fix-Runde behoben (Commits `ec843ef`..`9b2a4c7`): Routing-Remount-Bug (geteilte Layout-Route mit `Outlet`), Signup-Dead-End bei aktivierter E-Mail-Bestätigung, CI-Trigger/Push-Flow-Mismatch (Manual-Verification-Schritt in der Plan-Datei jetzt auf PR-Flow statt Direct-Push-auf-main umgestellt), veraltete Semgrep-Action ersetzt, RLS-Policies gehärtet (Indizes, unique-Constraints, `auth.role()` durch `to authenticated` ersetzt), u.a.
Scoped Re-Review: alle 15 Findings ADDRESSED, keine neue Critical/Important-Regression. Aktueller Test-Stand: 23/23 Tests grün. Ein Minor-Finding geparkt (LoginPage-Login-Zweig zeigt bei einer theoretischen MFA-Response ohne Session keine Fehlermeldung — in der aktuellen reinen E-Mail/Passwort-Flow unerreichbar, nur relevant falls später MFA dazukommt).

**Phase 1 ist vollständig abgeschlossen**, inklusive Manual-Verification (Signup mit E-Mail-Bestätigung, Login, alle vier Dashboard-Tabs, Logout, `profiles`-Zeile via `handle_new_user`-Trigger — alles bestätigt funktionsfähig gegen die echte Produktions-Supabase-Instanz).

## Phase 2 – Ernährungsbereich (abgeschlossen)

- Spec: `docs/superpowers/specs/2026-08-18-phase2-ernaehrungsbereich-design.md`
- Plan: `docs/superpowers/plans/2026-08-18-phase2-ernaehrungsbereich-plan.md` (14 Tasks)
- Umgesetzt per Subagent-Driven-Development in isoliertem Worktree; PR #8 gemerged (Merge-Commit `2adc284`), Worktree und Branch danach entfernt, SDD-Workspace gelöscht.
- Neue Dependency: `@zxing/browser` (Kamera-Barcode-Scan)

**Task-Status (14 von 14 Tasks fertig, alle reviewed):**
- [x] Task 1: Migration `0002` (Kalorienziel-Felder + Barcode-Unique-Index) — Commit `55b7387`
- [x] Task 2: `lib/nutrition-goal.ts` (Mifflin-St-Jeor-Berechnung) — Commit `8f16c8f`
- [x] Task 3: `lib/open-food-facts.ts` (Barcode-Lookup) — Commit `61b0ce7` (1 Minor geparkt)
- [x] Task 4: `lib/product-lookup.ts` (lokaler Cache + OFF-Fallback) — Commit `6b23c06`
- [x] Task 5: `hooks/use-profile.ts` — Commit `21521d7`
- [x] Task 6: `hooks/use-food-entries.ts` — Commits `21521d7..4d95a20` (2 Fix-Runden: echter `react-hooks/set-state-in-effect`-Lint-Fehler behoben, echter UTC-vs-lokal-Tag-Grenze-Bug in `todayRange()` gefunden und behoben)
- [x] Task 7: `components/CalorieGoalEditor.tsx` — Commit `5143d4d`
- [x] Task 8: `components/DailySummary.tsx` — Commit `a312bdf`
- [x] Task 9: `components/FoodEntryList.tsx` — Commit `d7566db`
- [x] Task 10: `components/ManualProductForm.tsx` — Commit `ddef760`
- [x] Task 11: `components/BarcodeScanner.tsx` (+ `@zxing/browser`) — Commits `ddef760..c0c7eb4` (2 Fix-Runden: Vitest-Mock-Inkompatibilität sauber im Test statt mit Production-Workaround gelöst, unnötige Typ-Abweichung zurückgenommen)
- [x] Task 12: `components/AddEntryFlow.tsx` — Commits `c0c7eb4..ffde50a` (1 Fix-Runde: echter Dead-End-Bug behoben — `looking-up`-Status blieb bei Netzwerkfehler beim Barcode-Lookup hängen, kein Cancel möglich)
- [x] Task 13: `pages/NutritionPage.tsx` (Dashboard zusammengesetzt) — Commit `3df68f3`, alle 8 Cross-Task-Interfaces vom Reviewer unabhängig verifiziert
- [x] Task 14: Finale lokale Verifikation — fertig. `npm test` 76/76 grün (19 Dateien), `npm run lint` ohne Fehler, `npm run build` (tsc -b + vite build) erfolgreich. `docs/domaenenmodell.md` um die fünf neuen `profiles`-Spalten und den `products_barcode_unique`-Index ergänzt, Quellenzeile auf Stand Phase 2 gesetzt, nach `../fitness-app.wiki/Domain-Model.md` gespiegelt (Wiki noch **nicht** committet/gepusht).

**Finale Whole-Branch-Review:** abgeschlossen. Verdict zunächst "not ready" wegen **eines Critical**: `product-lookup.ts` setzte beim Cachen eines Open-Food-Facts-Treffers kein `created_by`, und die einzige INSERT-Policy auf `products` ist `created_by = auth.uid()` — jeder Scan eines noch nicht gecachten Produkts wäre von RLS abgelehnt worden und hätte den Nutzer ins manuelle Formular geworfen, obwohl die Nährwerte gerade abgerufen waren. Alle 76 Tests waren grün, weil die Mocks jeden Insert gelingen ließen. Dazu 8 Important (Barcode ungeprüft in die OFF-URL — der Scanner dekodiert auch QR-Codes; OFF-Nährwerte ungeprüft; Menge- und Kalorienziel-Feld schrieben pro Tastendruck in die DB, `Number('')` = 0 → Datenverlust; Kamera-Leak beim Unmount vor dem Start-Promise; ewiges "Lädt…" bei fehlender `profiles`-Zeile; fehlende Plausibilitätsprüfung vor dem Insert in die geteilte `products`-Tabelle) und 10 Minor.
Als solide bestätigt: keine Secrets, kein Logging, keine Drittanbieter-Namen, kein XSS/SQLi, Migration 0002 fail-closed, Mifflin-St-Jeor korrekt, Cross-File-Typen konsistent.

- Fix-Runde 1 (Commit `cd09414`): alle 15 Findings behoben, 10 neue Tests genau für die Pfade, die die Lücken verdeckt hatten.
- Scoped Re-Review: **15/15 ADDRESSED**, keine Regression durch die Fixes (Draft-States und requestId-Guards explizit gegengeprüft, `maybeSingle()`-Semantik gegen die installierte postgrest-Version verifiziert). Zwei Important-Residuen gefunden.
- Fix-Runde 2 (Commit `afb0d06`): beide Residuen behoben — supabase-js *resolved* statt zu werfen, dadurch wurden alle Schreibfehler stumm geschluckt (`AddEntryFlow`s Fehlerzweig war toter Code, ein fehlgeschlagenes Menge-Update ließ den getippten Wert stehen); Profil-Updates sind jetzt serialisiert (Blur + Klick auf "Berechnen lassen" feuerten zwei ungeordnete PATCHes).

**Stand:** 94/94 Tests grün, Lint ohne Fehler und ohne Warnungen, Build sauber.

**Offener Follow-up (bewusst nicht in Phase 2 gelöst):** Cache-Zeilen aus dem Barcode-Scan tragen jetzt den `created_by` des ersten Scanners. Zusammen mit `products_update_own` (aus Phase 1, ohne `with check`) heißt das: wer einen Barcode zuerst scannt, kann die Nährwerte dieses geteilten Produkts danach für alle ändern. Für Phase 5 (Härtung) vormerken — Policy-Design, kein Bugfix.

**Abgeschlossen nach dem Merge:** Wiki synchronisiert (`Domain-Model`, `Phase-2-Design-Spec`, `Phase-2-Implementation-Plan`, `Home`, `_Sidebar` — gepusht), Worktree/Branches entfernt, SDD-Workspace gelöscht.

**Manual-Verification abgeschlossen** (gegen die echte Produktionsinstanz): Kalorienziel berechnet sich korrekt aus dem Profil; Barcode-Lookup legt Produkt und Eintrag an; zweiter Lookup derselben Nummer kommt aus dem lokalen Cache; unbekannte Nummer führt ins manuelle Formular; implausible Werte (`-300` kcal) werden abgelehnt, ohne zu schreiben; Menge ändern und Eintrag löschen funktionieren; Summen stimmen (`kcal × Menge / 100`).

**Damit ist Phase 2 vollständig abgeschlossen.**

Dabei gefunden und in PR #11 behoben:
- Kamera-Vorschau blieb schwarz — StrictMode führt den Effect zweimal auf derselben Instanz aus, und zxing bindet jeden Reader an dasselbe `<video>`; die erste Session löste auf, nachdem die zweite das Element übernommen hatte, und riss es mit ihrem `stop()` ab. Sessions sind jetzt verkettet.
- Barcode-Nummer lässt sich eintippen (gleicher Lookup-Pfad wie der Scan) — genau damit wurde Phase 2 dann verifiziert.

**Kamera-Scan am Handy verifiziert:** Scan erkennt den Barcode und findet das Produkt. Auf Laptop-Webcams bleibt er unzuverlässig (Fixfokus, zu wenig Pixel für einen EAN-13 → `ChecksumException`) — Optik-, kein Code-Problem. `TRY_HARDER` **nicht** aktivieren: es lässt die 1D-Reader jeden Frame rotiert erneut lesen und wirft dabei in `@zxing/browser` 0.2.1 „Could not create a Canvas element.", was jeden Frame vor dem Dekodieren killt.

## Dev-Server am Handy testen

`npm run dev:mobile` — HTTPS mit selbstsigniertem Zertifikat, gebunden an `0.0.0.0` (nicht `host: true`, das bindet nur auf `::` und ist per IPv4 nicht erreichbar). `npm run dev` bleibt http auf localhost.

Voraussetzungen und typische Stolpersteine, alle real aufgetreten:
- **Nur einen Dev-Server gleichzeitig starten.** Zwei Vite-Instanzen teilen sich `node_modules/.vite`, die zweite optimiert die Dependencies neu und entwertet die gehashten URLs der ersten → im Browser MIME-Fehler („expected JavaScript, got text/html").
- **Firewall:** einmalig als Admin `New-NetFirewallRule -DisplayName "Vite dev 5173 (LAN)" -Direction Inbound -Protocol TCP -LocalPort 5173 -Action Allow -Profile Private -RemoteAddress LocalSubnet`, und das WLAN-Profil muss `Private` sein.
- **VPN aus.** Das Uni-VPN pusht eine Route für `192.168.2.0/24` über sein eigenes Gateway mit Top-Metrik — Antwortpakete ans Handy verschwinden dann im VPN-Netz (Timeout). Die Adresskollision besteht, weil Uni- und Heimnetz denselben privaten Bereich nutzen.
- **Aktive Adresse prüfen:** `Get-NetIPAddress` — eine `Deprecated`-Adresse gehört zu einem getrennten Adapter und ist tot. PC am Kabel und Handy im WLAN ist kein Problem, solange beide im selben Subnetz hängen.
- Im Browser die URL mit `https://` eintippen; ohne Schema versucht er http, worauf der Mobile-Server nicht antwortet.

## CI-Pipeline — war seit Phase 1 wirkungslos (behoben)

Der Workflow filterte auf `pull_request: branches: [main]`, der Default-Branch ist aber `master`. Er hat deshalb **nie** ausgelöst — weder bei PR #7 (Phase 1) noch bei PR #8 (Phase 2). Semgrep, npm audit, ZAP und die Testsuite waren tote Konfiguration; beide Phasen sind ohne CI-Nachweis auf `master` gelandet, Migration `0002` wurde ungeprüft auf Produktion angewendet. Behoben mit PR #9 (Merge-Commit `b2fdef7`).

Der erste echte Lauf deckte sofort drei Dinge auf, die lokal unsichtbar waren:
- Semgrep: alle Actions hingen an veränderlichen Tags (`@v4`) → jetzt auf 40-stellige Commit-SHAs gepinnt (Version im Trailing-Kommentar, Dependabot aktualisiert die SHAs weiterhin).
- ZAP: Scan war sauber (`FAIL-NEW: 0, PASS: 60`), der Job scheiterte am Artefakt-Upload — `action-baseline@v0.12.0` nutzt die abgeschaltete Artifact-API v3 → auf v0.15.0 gehoben.
- Zwei Test-Schwächen: eine synchrone Assertion auf State, der außerhalb von `act()` gesetzt wird (bestand lokal, fiel in CI um), und zwei Mocks ohne Promise, an denen `.catch()` warf (vitest meldete die Tests als bestanden und ließ den Run trotzdem durchfallen).

**Merke:** `.claude/` ist git-ignored, aber vitest globbt Worktree-Kopien trotzdem mit — nach dem Mergen einer Phase den Worktree entfernen, sonst laufen doppelte, veraltete Testdateien mit.
